# Mirwada — note tra sessioni

Questo file e' la memoria del progetto tra un'iterazione di ralph e l'altra.
Ogni iterazione APPENDE una sezione datata. Non riscrivere le sezioni vecchie.

Formato di ogni sezione:
  [AAAA-MM-GG] US-NNN — titolo
  - Cosa fatto:
  - Come verificato:
  - Note ambiente (comandi, percorsi, problemi noti):
  - Prossima storia:

---

[SETUP] Pacchetto iniziale

- Cosa fatto: spina dorsale dati creata prima di ogni riga di codice di gioco.
  22 pathway / 220 sequenze in data/pathways/, schemi in data/schema/,
  registro chiuso di 31 primitive, vocabolario chiuso di 82 tag,
  validator in tools/validate_data.py, PRD fase 1 e prd.json (18 story).
- Come verificato: `python tools/validate_data.py` esce 0.
  `python tools/generate_pathways.py` riporta 22 pathway / 220 sequenze.
- Note ambiente:
  - Il validator NON richiede pip install: i controlli sono espliciti.
    Gli schemi in data/schema/ sono documentazione formale del formato.
  - generate_pathways.py e' idempotente: preserva abilities, acting_actions
    e potion gia' popolati. Si puo' rilanciare senza perdere lavoro.
  - Engine assunto: Godot 4.x. Se si passa a Unity, va riscritto solo
    006_PRD/prd-fase-1-fondamenta.md e il prd.json corrispondente.
- Prossima storia: US-001.

[SETUP-2] Riduzione a 10 Pathway + gold standard Twilight Giant

- Cosa fatto:
  - Scope ridotto da 22 a 10 Pathway scegliendo 4 GRUPPI COMPLETI
    (Lord of Mysteries, Eternal Darkness, Demon of Knowledge, Goddess of
    Origin). Criterio: il cambio di Pathway della fase 7 funziona solo tra
    vicini dello stesso gruppo, quindi gruppi a meta' avrebbero rotto quel
    sistema. I 12 differiti sono in data/pathways_deferred/, completi.
  - Aggiunto il campo `concept` a tutte e 100 le Sequenze attive: una riga che
    dichiara COSA FA IL GIOCATORE a quel livello. E' la specifica da cui si
    scrivono le abilita'. Il validator lo esige non vuoto.
  - Twilight Giant COMPLETO come gold standard: 20 abilita' dalla Sequenza 9
    alla 0, modificatori, azioni di recitazione, ingredienti, rituali.
    Zero righe di codice dedicate: e' la prova che l'architettura regge.
  - 3 primitive marcate `deferred` (weather_control, probability_shift,
    rule_bind): nessun Pathway attivo le richiede. Il validator RIFIUTA
    un'abilita' che le usi. 28 attive su 31.
  - Il validator ora verifica anche che i gruppi siano completi.
- Come verificato: `python tools/validate_data.py` esce 0.
  `python tools/generate_pathways.py` riporta 10 attivi / 100 sequenze.
- Note ambiente: per riattivare un Pathway, spostare il file da
  pathways_deferred/ a pathways/ e aggiornare ACTIVE in
  tools/generate_pathways.py. Riattivare SEMPRE il gruppo intero, mai il
  singolo Pathway: il validator fallisce sui gruppi incompleti.
- Prossima storia: US-001.

[SETUP-3] Acting Method ridotto + stress test primitive + balance.json

- Cosa fatto:
  - ENGINE DECISO: Godot 4.x. Motivo determinante: attrito di iterazione.
    Niente compilazione C#, niente file .meta nei commit, hot-reload JSON
    immediato. In un ciclo "scrivi -> verifica a schermo" ripetuto centinaia
    di volte, l'attrito conta piu' della maturita' del tooling.
  - ACTING METHOD, VERSIONE RIDOTTA (scelta dell'utente): creato
    data/schema/tracked_events.json, vocabolario CHIUSO di 12 eventi.
    Ogni azione di recitazione e' ora un contatore su un evento con filtri,
    non una descrizione a parole. Costo: 1 story per un EventTracker generico
    invece di ~40 rilevatori speciali.
    Riscritte tutte le 31 azioni del Twilight Giant e le 3 del Fool.
  - Il validator ora verifica: evento nel vocabolario, filtri ammessi per
    quell'evento, target numerico presente, e che le azioni di una Sequenza
    SOMMINO ad almeno 1.0 (altrimenti il giocatore resta bloccato per sempre
    a quella Sequenza — bug che nessun test di gioco avrebbe trovato).
  - STRESS TEST DEL REGISTRO: scritta un'abilita' di alta Sequenza per ognuno
    dei 9 Pathway non implementati, scegliendo i casi piu' difficili.
    Risultato: 12 primitive usate, ZERO fuori registro. Il vincolo delle 28
    regge. Rischio chiuso.
  - Il test ha pero' trovato 3 capacita' del MOTORE non coperte dal registro,
    ora documentate in 006_PRD/design-pathways.md e inserite nella roadmap:
    (1) eseguire un'abilita' non posseduta (Error), (2) oggetti con
    stored_ability_id (Hermit), (3) evocazioni persistenti da serializzare
    (Death, Paragon). Tutte e tre sarebbero emerse in fase 5 con il motore
    gia' scritto.
  - Creato data/balance.json: curve globali (muro di Sequenza, hp,
    spiritualita', soglie di follia, fondamenta). I numeri sono PLAUSIBILI,
    NON BILANCIATI. Vanno riscritti dopo il primo playtest.
- Come verificato: python tools/validate_data.py esce 0. 32 abilita' totali,
  10 pathway, 100 sequenze, 40 file di dati.
- Note ambiente: aggiungere un evento a tracked_events.json e' CODICE, non
  dati. Va discusso come per le primitive.
- Prossima storia: US-001.

[SETUP-4] Sistema audio

- Cosa fatto:
  - CORREZIONE DI UN BUCO DEL PIANO: l'audio era nei non-goals della fase 1,
    ma design-pathways.md prometteva gia' i "sussurri della follia". Sistema
    mai progettato, meccanica gia' promessa. Ora c'e' data/audio.json.
  - L'audio in questo gioco NON e' decorazione, e' un canale informativo:
    (1) tell sonori degli attacchi nemici, con anticipo_ms nei dati: permette
        di reagire a un nemico fuori schermo. Requisito di leggibilita'.
    (2) layer della follia su 4 soglie, con bus dedicato "whisper" separato
        cosi' che l'accessibilita' lo silenzi senza toccare la meccanica.
        A soglia 55 i sussurri usano i nomi degli NPC incontrati e delle
        Ancore del giocatore.
    (3) percezione per Sequenza: salendo si sentono layer nuovi e si rivelano
        informazioni (spiriti, densita' mistica, segreti adiacenti).
  - pathway_palette: 10 palette timbriche invece di ~200 suoni dedicati.
    Stesso principio delle primitive applicato all'audio.
  - Musica a LAYER per zona (base + stem su tensione/combat/boss): meno
    minuti da comporre e nessun taglio brusco entrando in combattimento.
    Rituale interrotto = taglio secco al silenzio.
  - Due story aggiunte alla FASE 1 (US-019 bus audio + feedback di colpo,
    US-020 tell sonori). In fase 1 e non piu' avanti perche' il feedback di
    colpo e' meta' del feel di un action 2D: tarare il combat senza audio
    significa tararlo due volte. Fase 1 passa da 18 a 20 story.
  - Il validator verifica: palette presente per ogni Pathway attivo,
    anticipo_ms > 0 su ogni tell, soglie di follia ordinate.
- LIMITE DICHIARATO: non posso produrre audio. Il sistema lo progetto, i file
  no. Per la fase 1 bfxr/sfxr/Chiptone bastano e sono lo stile giusto per la
  pixel art. Per la fase 6 serve un musicista, o stem CC0 compatibili con la
  struttura a layer. data/audio.json e' gia' la specifica da consegnargli.
- Come verificato: python tools/validate_data.py esce 0.
- Prossima storia: US-001.

[SETUP-5] Animazioni data-driven + generatore di placeholder

- Cosa fatto:
  - data/animations.json: specifica delle animazioni per personaggio, nemico
    e pet. Il TIMING DEL COMBAT vive qui, non negli script: frame di anticipo,
    attivi, recupero, iframe del dash (iframe_da/iframe_a), finestra di parata
    perfetta, eventi per frame (footstep, tell_audio, tell_visivo,
    hitbox_on/off, ability_release).
    Gli iframe sono espressi in FRAME e non in secondi, cosi' restano
    agganciati all'animazione anche cambiando gli fps.
  - DECISIONE IRREVERSIBILE PRESA: 4 direzioni, non 8. Le diagonali riusano
    gli sprite orizzontali. Dimezza i frame da disegnare ed e' invisibile in
    un top-down. Come i 32x32, cambiarla dopo significa ridisegnare tutto.
  - tools/generate_placeholders.py: genera 324 frame PNG reali dalla
    specifica, con bordi diagnostici (ROSSO hitbox attiva, GIALLO anticipo,
    CIANO invulnerabile, VERDE parata perfetta, GRIGIO recupero) e numero di
    frame leggibile. Serve a tarare il combat GUARDANDO lo schermo: per questo
    scopo e' meglio dell'arte definitiva, che nasconde le fasi.
    Verificato visivamente: attack_heavy mostra 4 gialli + 2 rossi + 2 grigi,
    coerente con la specifica; dash mostra i frame 0-2 in ciano e il 3 no.
  - US-021 aggiunta alla fase 1 (macchina di animazione data-driven).
    Fase 1: 21 story.
  - Il validator ora verifica: indici di frame esistenti, hitbox_on/hitbox_off
    in pari (una hitbox che non si chiude resta attiva per sempre), e avvisa
    sugli attacchi senza anticipo salvo dichiarazione anticipo_in_stato.
- LIMITE DICHIARATO: non disegno pixel art. Le animazioni le programmo, i
  disegni no. Le quattro strade per l'arte vera (pack CC0, pack a pagamento,
  artista su commissione, impararla) sono in 006_PRD/prd-fase-1-fondamenta.md.
  La decisione NON va presa adesso: va presa quando il gioco e' divertente
  coi placeholder. Il budget si calcola dal budget_frame in animations.json:
  156 frame per un personaggio completo, 30-60 al giorno per un artista.
- Come verificato: validate_data.py esce 0, generate_placeholders.py produce
  324 frame in assets/placeholder/.
- Prossima storia: US-001.

[SETUP-6] Allineamento 22->10 e correzione del comando ralph

- Cosa fatto: audit dello starter contro le regole reali delle skill /prd e
  /ralph e contro tools/ralph.sh. Trovati 7 residui della riduzione da 22 a 10
  Pathway (SETUP-2) mai propagati fuori dai file di dati, piu' un errore nel
  comando di avvio. Nessun dato toccato: solo prd.json, README, PRD e CLAUDE.md.

  1. prd.json US-002: il criterio di accettazione chiedeva "22 pathway
     caricati, 220 sequenze" su dati che ne contengono 10 e 100. Ralph avrebbe
     scritto un test destinato a fallire e bruciato iterazioni a inseguirlo.
     Corretto in 10/100. ERA BLOCCANTE.
  2. prd.json US-001: unica story delle 21 senza "Typecheck passes", che la
     skill /ralph impone su ogni story. Aggiunto.
  3. 006_PRD/prd-fase-1-fondamenta.md riga 50: stesso residuo 22/220 del
     punto 1, corretto in 10/100.
  4. README: "rigenera la spina dorsale dei 22 pathway" -> 10. Verificato
     contro l'output reale di generate_pathways.py (10 attivi / 100 sequenze).
  5. README, comando ralph: mancava --test-cmd. ERA BLOCCANTE. ralph.sh
     rileva il comando di test da solo, ma il fallback in assenza di
     package.json e' "uv run pytest tests/ -v": questo progetto e' Godot +
     GDScript, non ha ne' npm ne' pytest ne' cartella tests/, quindi ogni
     iterazione avrebbe fallito la verifica su un comando impossibile da
     soddisfare. Il comando corretto ora e' nel README, con la variante da
     usare dopo US-016 e la spiegazione del perche'.
  6. prd.json US-004: il titolo dice "8 direzioni" mentre CLAUDE.md e
     animations.json fissano 4 direzioni. Non era una contraddizione reale
     (movimento a 8, sprite a 4, le diagonali riusano gli orizzontali) ma il
     criterio non lo diceva e ralph poteva generare 8 set di sprite. Criterio
     riscritto con il rimando esplicito a convenzioni.direzioni.
  7. CLAUDE.md: "Fase corrente: 1 (18 story)" -> 21. Le story sono passate da
     18 a 21 in SETUP-4 (audio) e SETUP-5 (animazioni), il conteggio no.

- Come verificato:
  - python tools/validate_data.py esce 0, 0 warning 0 errori.
  - python tools/generate_pathways.py riporta 10 attivi / 100 sequenze e NON
    modifica alcun file (idempotenza verificata con diff sull'intero data/).
  - prd.json riletto come JSON: 21 story, tutte passes=false, nessuna story
    priva di "Typecheck passes", priority sequenziali 1-21.
  - Cercati residui "22 pathway"/"220 sequenze" in tutto il repo: resta solo
    design-pathways.md riga 90, che e' la tabella di confronto "22 vs 10" e
    deve restare com'e'.
  - Nessun file in data/ modificato da questa sessione.

- Note ambiente:
  - Primo commit del progetto: fino a ora lo starter non era mai stato
    versionato. Questa sessione e' remota (container Linux), quindi ralph NON
    e' stato lanciato: richiede Docker Desktop, che qui non esiste. Come da
    prompt originale, il comando resta pronto nel README e va lanciato a mano.
  - La skill ponytail in ~/.claude/skills/ponytail e' una CARTELLA VUOTA nel
    repo claude-config: il contenuto (github.com/DietrichGebert/ponytail) non
    e' mai stato committato. Stesso problema per agent-reach, book-to-skill e
    shannon. Il regime ponytail resta comunque applicato perche' e' scritto
    per esteso nella sezione 2 di questo CLAUDE.md.
  - Incoerenza tra le skill dell'utente, non di questo progetto: la skill
    /nuovo-progetto prescrive id story "US-1NN" con la prima cifra a indicare
    la fase, ma ralph.sh filtra le fasi sul campo "fase": N, non sull'id.
    Questo progetto usa US-001..US-021 + campo fase, che e' l'unico schema che
    ralph.sh sappia effettivamente leggere. Da correggere nella skill.

- Resta aperto: docs/documentazione.py e diario.py previsti dallo scaffolding
  di /nuovo-progetto non sono presenti. Sembra una scelta (sono pensati per i
  progetti DdC con documentazione normativa), non una dimenticanza. Da
  confermare con l'utente. Richiederebbero python-docx.

- Prossima storia: US-001.

[SETUP-7] Repo dedicata: da IvanOstroumov/NightOwl a YasserOudabashi/Sequenza

- Cosa fatto: il progetto era agganciato a IvanOstroumov/NightOwl, che NON e'
  un repo dell'utente. Spostato su una repo dedicata, privata:
  https://github.com/YasserOudabashi/Sequenza
  Cambiato il remote origin e registrata la repo in CLAUDE.md (sezione Repo),
  come prescrive il template di /nuovo-progetto.
- Come verificato: git remote -v punta a Sequenza.git; validate_data.py esce 0.
- Note ambiente: la repo NON e' stata creata da questa sessione e i commit NON
  sono stati pushati. La GitHub App di Claude non e' installata sull'account
  YasserOudabashi: da qui la lettura dei repo dell'utente da 403, il push da
  403 e create_repository da "Resource not accessible by integration". Leggibile
  era solo NightOwl, perche' pubblico e agganciato alla sessione.
  Creazione della repo e push vanno fatti a mano dal PC dell'utente; la storia
  completa e' stata consegnata come git bundle.
  Da fare anche: aggiungere Sequenza alla tabella "Repository che uso" in
  ~/.claude/CLAUDE.md.
- Prossima storia: US-001.

[2026-09-02] US-001 — Setup progetto Godot e struttura cartelle

- Cosa fatto: creato project.godot (Godot 4.3), scenes/main.tscn come scena
  vuota di avvio, cartelle scenes/ scripts/ tests/ (data/ e assets/ c'erano
  gia'). Viewport 640x360 con stretch "viewport" e aspect "keep", finestra
  reale 1280x720 cioe' il viewport x2, texture filter Nearest.
  Il .gitignore per Godot era gia' nello starter, criterio soddisfatto.
- Come verificato: NON leggendo il file scritto ma rileggendo le impostazioni
  da ProjectSettings dentro il motore, con uno script SceneTree eseguito in
  headless. Tutti e 10 i controlli OK, exit 0.
  `godot --headless --path . --import` e `--quit`: exit 0, zero errori.
- Note ambiente:
  - Godot NON e' installato in questa sessione remota: e' stato scaricato il
    binario 4.3-stable linux.x86_64 dalle release ufficiali. Su altra macchina
    serve Godot 4.3 nel PATH.
  - TRAPPOLA GDSCRIPT, vale per tutte le story successive: questo progetto
    tratta come errore, non come warning, l'inferenza di tipo da un Variant.
    `var x := dict[key]` NON compila ("The variable type is being inferred
    from a Variant value... Warning treated as error"). Va scritto
    `var x: TipoEsplicito = dict[key]`. Trovato eseguendo, non leggendo.
  - Verifica a schermo NON fatta: sessione headless, nessuna finestra. La
    resa visiva della pixel art (nitidezza, scaling a interi) va confermata
    aprendo il progetto nell'editor.
- Prossima storia: US-002.

[2026-09-02] US-002, US-003, US-007, US-012, US-013

- Cosa fatto: la spina dorsale del codice data-driven, cinque story.
  US-002 GameData (autoload): carica 24 file e li espone come indici piatti
    per id. Id ignoto -> {}, mai null, mai crash. Errori raccolti e stampati
    con riga e messaggio del parser: mai fallimento silenzioso.
  US-003 hot-reload con F5 in debug. _upsert aggiorna i Dictionary SUL POSTO,
    quindi chi si e' tenuto un riferimento vede i valori nuovi.
  US-007 StatsComponent: base + modificatori per id, valori iniziali dalle
    curve di balance.json (non numeri nel codice).
  US-012 AbilityEngine: cooldown e costo prima di eseguire, poi dispaccio su
    tabella tipo->Callable. + primitiva projectile con nodo vero.
  US-013 melee_arc, buff_stat, heal, dash.

- Come verificato: 41 test headless, tutti passano, exit 0, zero leak.
  `godot --headless --path . --script res://tests/run_tests.gd`
  validate_data.py esce 0.

- QUATTRO TRAPPOLE TROVATE ESEGUENDO, non leggendo. Valgono per tutte le
  story successive, tenerle a mente:
  1. Il progetto tratta come ERRORE l'inferenza di tipo da Variant:
     `var x := dict[key]` non compila. Sempre il tipo esplicito.
  2. NIENTE class_name sugli script che i test caricano: risolvere una
     classe globale da uno script caricato a runtime dal runner manda
     Godot 4.3 in STALLO, senza alcun messaggio. Usare preload.
  3. Assegnare un'istanza gia' liberata a una variabile tipata Node e' un
     errore che interrompe il ciclo a meta'. Leggere come Variant,
     validare con is_instance_valid, poi tipare.
  4. Gli autoload NON esistono dentro _init() di uno SceneTree script:
     il runner aspetta un frame prima di partire.

- DECISIONE: le scadenze degli effetti sono una lista processata in
  _process, non await su SceneTreeTimer. Gli await lasciavano timer orfani
  a fine suite e non erano testabili. tick_effects(delta) e' pubblica
  apposta: i test simulano 8 secondi invece di aspettarli.

- BUCO NEL PRD, da decidere: il criterio di US-013 cita fool_velo_illusorio,
  che compone illusion + buff_stat, ma illusion non e' fra le 5 primitive
  di questa fase. Risolto facendo distinguere al motore "non ancora
  implementata" (avviso, prosegue) da "fuori registro" (errore). Il
  criterio come scritto restava pero' ambiguo.

- NON VERIFICATO A SCHERMO: sessione headless, nessuna finestra. Restano da
  confermare nell'editor la resa della pixel art (US-001) e la pressione
  fisica di F5 (US-003).

- Prossima storia: US-004. Richiede verifica a schermo: farla con l'editor
  aperto, non alla cieca.

[2026-09-02] SETUP-8 + naming — repo Mirwada, HEAD riparato, lore doc

- Cosa fatto:
  1. .git/HEAD era corrotto (conteneva il reflog, non un ref): HEAD detached
     "points at nothing", git rev-parse HEAD -> 0000000. Nessun commit perso
     (fsck: 0 dangling, tip db0b57a intatto, working tree identico).
     Riparato con: git symbolic-ref HEAD refs/heads/<branch>.
  2. Branch rinominato in `main` (era un nome di sessione claude/*).
  3. Nome del gioco fissato: **Mirwada**. Rinominato SOLO il titolo in
     project.godot (config/name), README.md h1, CLAUDE.md h1 + sezione Repo,
     prd.json project, progress.txt h1. `Sequenza`/`Sequenze` come termine di
     gameplay (lo scalino di progressione) NON e' toccato: non e' il titolo.
  4. Repo dedicata creata: github.com/YasserOudabashi/mirwada (privata).
     `origin` spostato da IvanOstroumov/NightOwl (vuoto, mai usato) a mirwada.
     Primo push dei 9 commit. Aggiunta alla tabella repo in ~/.claude/CLAUDE.md.
  5. .gitignore: escluso l'eseguibile Godot scaricato in locale
     (Godot_v*-stable_win64.exe/) e graphify-out/.
  6. Nuovo 006_PRD/design-lore.md: nomi di gioco, protagonista, NPC.
     Protagonista: nome di default **Enel**, rinominabile dal giocatore alla
     creazione personaggio. NPC: Mirco, Sidon + inventati. NESSUN CODICE:
     i nomi vivono nei dati/i18n, che non esistono ancora (arrivano con la
     fase NPC/dialoghi della roadmap). Per ora sono decisioni di design.

- Come verificato: Godot 4.3 headless (eseguibile locale) -> 41/41 test
  passano, exit 0. python tools/validate_data.py -> esce 0. graphify
  rigenerato. git log -> 9 commit + 1, push ok.

- Resta aperto: i18n dal giorno 1 e' una regola di CLAUDE.md ma NON esiste
  ancora nessun file di locale; il primo testo UI arriva con US-017 (HUD).
  La creazione personaggio (scelta nome protagonista) non ha una story:
  agganciarla alla fase con NPC/dialoghi/salvataggio.
- Prossima storia: US-004 (invariata).

[2026-09-02] AUDIT + DESIGN — risanamento, 5 design doc, docx

Sessione grossa su richiesta dell'utente: analisi completa del progetto
(pathway, conflitti, sicurezza) + design di NPC/mondo/biomi/UI/VFX.

- Audit (3 passate parallele: codice, dati, documenti). Trovato e RISANATO
  in questa sessione (commit "fix: risanamento dati e motore"):
  1. melee_arc mai liberato -> vita 0.25s + queue_free, test.
  2. heal ignorava il param bersaglio -> onorato, test.
  3. tag_danno: i dati passano una STRINGA, gli script tipavano Array ->
     crash latente al primo attacco reale. Contratto fissato a stringa +
     NUOVO vocabolario chiuso data/schema/damage_tags.json (8 voci) validato
     su tag_danno/tag_bloccati/filtri. Ha subito pescato "crescita" usato
     come tag di danno in mother -> corretto in veleno.
  4. Filtro sequenza_min RINOMINATO sequenza_bersaglio_max: la semantica era
     invertita (0 = piu' forte) e l'EventTracker di fase 2 sarebbe nato rotto.
  5. moon_phase mescolava momenti del giorno (alba, crepuscolo) e fasi lunari
     (eclissi) -> NUOVO data/schema/time.json con i due assi separati
     momento/fase_lunare; rituali del TG migrati; validator li valida.
  6. 90 sequenze vuote passavano il validator (il check della somma era
     bypassato da acting_actions:[]) -> flag "stub": true esplicito (89/100,
     fool_9 completata con madness_on_force 1.0); le non-stub ora ESIGONO
     acting_actions e madness_on_force > 0; rituali non-stub esigono
     location_tags; somma progresso > 1.0 = warning.
  7. Registro primitive: param "tipo" collidenti rinominati
     (tipo_illusione/tipo_modifica/tipo_meteo), bersaglio aggiunto a shield;
     il validator ora RIFIUTA parametri fuori registro (classe di bug
     "danno silenziosamente a zero" chiusa).
  8. Sinergie: errore su abilita' prodotta inesistente (stub esente:
     sinergia_inganno_probabilita marcata stub), warning sulle irraggiungibili
     coi pathway attivi (2 su 3 lo sono: gruppi differiti).
  9. foundation senza floor -> minimo: 0 in balance.json.
  10. generate_pathways.py: il merge perdeva i campi falsy a ogni
      rigenerazione -> ora il vecchio file vince sempre; template aggiornato
      (stub, momento/fase_lunare); docstring 22/220 stale corretta.
      VERIFICATO: doppia rigenerazione = zero diff (hash identici).

- Come verificato: python tools/validate_data.py -> 0 errori, 8 warning
  informativi VOLUTI (5 somme TG > 1.0 da decidere in fase 5, conteggio
  stub, 2 sinergie irraggiungibili). Godot 4.3 headless -> 43/43 test
  (41 + 2 nuovi: vita del melee_arc, heal con bersaglio non-self).

- Design: 5 documenti nuovi in 006_PRD/ (approvati come piano dall'utente,
  deroga alla regola "niente PRD anticipati" dichiarata nel master):
  * design-master.md — baseline onesta dall'audit, sicurezza del save
    (atomico, mai bytes_to_var, campi non fidati -> criteri aggiunti a
    US-015), matrice di proprieta' che scioglie le sovrapposizioni tra
    pathway (mondo spirituale=Death, furto a 3 sapori, 4 fonti di evocazione),
    riscrittura proposta di darkness_1 senza probability_shift, contratti
    dei sistemi promessi (Ancore, strutture, stored_ability_id, valuta),
    story per fase, decisioni aperte con raccomandazione.
  * design-world.md — 5 regioni, UNA PER GRUPPO di pathway (Mirwada citta'
    hub; Marche del Crepuscolo; Valle della Madre; Archivio Sepolto;
    Frontiera delle Porte), vocabolario di 24 location_tags (gli 8 del TG
    invariati), 6 modi di gating, tempo, densita' mistica, fast travel
    come potere e mai come menu.
  * design-npc-quest.md — roster 8 NPC formalizzato, dialoghi che riusano il
    vocabolario condizioni delle abilita', quest = contatori EventTracker
    travestiti (zero verbi nuovi), fazioni, tre atti agganciati ai tier,
    tre finali (apoteosi/consumazione con eredita'/rinuncia).
  * design-ui-libro.md — TUTTA la UI e' un libro diegetico: il libro E' il
    salvataggio (scaffale=slot, frontespizio=creazione personaggio con
    default Enel, la follia macchia le pagine), 8 tipi di pagina data-driven
    in data/ui/book.json, colophon=impostazioni con le 6 opzioni
    accessibilita' di audio.json, voltata_ms nei dati.
  * design-vfx.md — stile manhwa (Legend of the Northern Blade) tradotto in
    32x32: tratto a pennello, neri pieni, impact frame a inversione agganciati
    agli hitstop_ms esistenti, distorsione disattivabile, 10 palette visive
    SPECCHIO 1:1 di pathway_palette audio, VFX per PRIMITIVA (28 effetti,
    non 200 sprite), vocabolario chiuso dei tratti. ability.schema.json NON
    si tocca.

- prd.json: fase 1 ora 26 story (aggiunte US-022 loader typeof, US-023 purge
  cooldown/indici, US-024 stats da balance, US-025 test onesti, US-026 CI);
  US-015 arricchita coi criteri di sicurezza del save.
- Scaffolding docx creato (mancava dallo /nuovo-progetto): docs/documentazione.py
  + diario.py, entrambi generano senza errori.
- README: stato 6/26, indice documenti aggiornato coi 5 design doc.

- Resta aperto: decisioni Appendice B del design-master (nomi regioni, libro
  diegetico, antagonista, eredita', taglio story fase 5 in 3 per pathway);
  le somme acting > 1.0 del TG da normalizzare o dichiarare volute (fase 5);
  la verifica a schermo resta mai fatta (US-004 con l'editor aperto).
- Prossima storia: US-004 (invariata). Le story di risanamento US-022..026
  possono andare in coda alla fase o intrecciarsi (US-022 prima di US-015).

[2026-09-02] US-004 — Movimento del giocatore a 8 direzioni

- Cosa fatto:
  - scripts/player.gd (CharacterBody2D): Input.get_vector sui 4 assi
    move_left/right/up/down, che normalizza gia' la diagonale. Velocita' verso
    il target con move_toward (accelerazione), verso zero con move_toward
    (attrito): mai assegnazione diretta di velocity, quindi non e' istantanea.
    ACCELERAZIONE/ATTRITO sono costanti dichiarate — il "feel" si tara con
    US-021; la velocita' MAX viene da StatsComponent.get_stat("velocita"),
    cioe' da balance.json.
  - scenes/player.tscn: Player + Sprite2D (offset y -8, region_enabled) +
    CollisionShape2D (cerchio r5, y -4, vicino ai piedi = origine
    "piedi_centro" di animations.json) + StatsComponent.
  - Sprite placeholder a 4 direzioni: _aggiorna_sprite() sceglie riga dello
    spritesheet per direzione (down/up/left/right, ordine di
    animations.json.convenzioni.direzioni) e ciclo di frame walk(6)/idle(4)
    via region_rect. In obliquo vince l'orizzontale: nessuno sprite
    diagonale, come da convenzione. E' throwaway: US-021 lo sostituisce con
    la macchina data-driven che legge frames/fps dal JSON.
  - project.godot: nuova sezione [input] con move_* su WASD (physical_keycode,
    ok su tastiere non-QWERTY) + frecce. Aggiunta a mano preservando i
    commenti: ProjectSettings.save() da script li avrebbe cancellati tutti.
  - scenes/main.tscn: istanza di Player al centro del viewport (320,180).
    Nessuna camera: a viewport 640x360 il centro e' gia' inquadrato. La
    camera e' US-005.

- Come verificato A SCHERMO (finestra reale, NON headless): script di verifica
  usa e getta che carica main.tscn, preme ogni direzione ~0.7s e salva uno
  screenshot per direzione. Osservato:
  * la tacca di direzione del placeholder punta a destra/su/sinistra/giu'
    coerentemente con l'input -> lo sguardo a 4 direzioni funziona;
  * da fermo velocity torna a (0,0) -> l'attrito porta a zero;
  * partito da (320,180), dopo right->up->left->down chiuso a (299,181.5):
    gli assi si compensano, nessuna deriva anomala.
  Screenshot in user://us004/ (app_userdata/Mirwada), non versionati.

- Regressione: godot --headless --script res://tests/run_tests.gd -> 43/43.
  python tools/validate_data.py -> exit 0 (8 warning informativi preesistenti).

- Note ambiente:
  - Eseguibile Godot: ./Godot_v4.3-stable_win64.exe/Godot_v4.3-stable_win64_console.exe
    (la variante _console.exe stampa su stdout; l'altra e' muta). Cartella
    gia' gitignorata.
  - Screenshot da CLI: serve una FINESTRA. get_viewport().get_texture() e'
    vuoto in --headless. Lo script di verifica gira senza --headless.
  - ProjectSettings.save() riscrive project.godot da zero: cancella TUTTI i
    commenti e omette i valori uguali al default (window/stretch/aspect="keep"
    era sparito). Per toccare project.godot: Edit a mano, non lo script.

- Resta aperto:
  - velocita' base = 90 px/s (fallback hardcoded in stats_component.gd):
    ~7s per attraversare lo schermo, troppo lenta. E' esattamente il numero
    che US-024 deve spostare in balance.json — alzarlo li'.
  - ACCELERAZIONE 1400 su vel_max 90 = spunto quasi istantaneo. Rivedere
    quando la velocita' sara' un dato e ci sara' un tilemap per giudicare
    il feel (dopo US-006).
- Prossima storia: US-005 (camera con smoothing e limiti).

[2026-09-02] US-005 — Camera con smoothing e limiti

- Cosa fatto:
  - scripts/game_camera.gd (extends Camera2D): in _ready attiva
    position_smoothing_enabled + position_smoothing_speed = 5.0.
  - I LIMITI sono della ZONA, non della camera: apply_zone_limits(Rect2)
    imposta limit_left/top/right/bottom dal rettangolo giocabile in
    coordinate mondo; clear_zone_limits() li riapre (~infiniti). Sara' la
    zona di US-006 a chiamarlo nel proprio _ready. Finche' nessuno lo fa,
    la camera segue senza vincoli.
  - scenes/player.tscn: aggiunto un Camera2D figlio del Player con questo
    script.
  - project.godot [rendering]: 2d/snap/snap_2d_transforms_to_pixel = true e
    snap_2d_vertices_to_pixel = true. Vedi sotto.

- IL TREMOLIO IN DIAGONALE (criterio esplicito di US-005): con la camera che
  segue con smoothing la sua posizione e' frazionaria a ogni frame. Prima
  dello snap, i pixel del mondo statico sfarfallavano fotogramma per
  fotogramma durante il moto obliquo — verificato confrontando due frame
  consecutivi (jitter_a/jitter_b): la griglia di riferimento appariva a
  luminosita' e nitidezza diverse nei due frame. Con i due snap_2d_*_to_pixel
  attivi i due frame consecutivi mostrano la griglia identica e nitida: il
  tremolio sparisce. Con la pixel art e' anche la resa corretta (tutto resta
  sulla griglia di pixel interi).

- Come verificato A SCHERMO (finestra reale): script usa e getta che carica
  main.tscn, aggiunge una griglia di punti in coordinate mondo (senza
  riferimento fisso la camera che segue darebbe screenshot identici), poi:
  * 6 screenshot durante un lungo moto in diagonale -> la griglia scorre,
    il giocatore resta al centro: la camera segue;
  * 2 frame consecutivi in moto -> griglia identica e pulita: niente jitter;
  * apply_zone_limits(rettangolo stretto) + spinta contro il bordo ->
    la camera si ferma al limite mentre il giocatore prosegue oltre
    (i limiti vincolano la camera, non il giocatore): confermato, nello
    screenshot zona_bordo_sx il giocatore e' nell'angolo e la camera e'
    bloccata. Screenshot in user://us005/, non versionati.

- Regressione: 43/43 test headless, validator exit 0.

- Note ambiente: snap_2d_transforms_to_pixel puo' rendere "a scatti" uno
  sprite che si muove lentamente. A 640x360 e' l'aspetto voluto. Se in
  futuro desse fastidio su qualche elemento specifico, si disattiva per
  quel CanvasItem con texture_filter/transform locali, non globalmente.

- Resta aperto: i limiti di zona non hanno ancora un chiamante — arriva con
  US-006 (la zona di test). La velocita' 90 px/s resta lenta (US-024).
- Prossima storia: US-006 (tilemap e collisioni dell'area di test).

[2026-09-02] US-006 — Tilemap e collisioni dell'area di test

- Cosa fatto:
  - tools/build_tileset.gd (one-shot): costruisce assets/placeholder/tileset.tres
    da tileset.png (4 tile: 0 pavimento, 1 muro, 2 ostacolo, 3 acqua). Un
    solo physics layer (collision_layer/mask = 1); poligono di collisione a
    tile pieno solo su muro e ostacolo. Output COMMITTATO, non rigenerato dal
    motore. Va rilanciato solo se cambia tileset.png.
  - scripts/test_zone.gd (extends TileMapLayer): dipinge a runtime bordo di
    muri + due muri interni con varco + pareti di corridoio + 7 ostacoli
    sparsi, su una griglia 40x30. Poi sposta il giocatore alla cella di
    spawn (7,15) e passa alla camera i limiti della zona
    (Rect2(0,0, 1280,960)) via apply_zone_limits di US-005.
  - scenes/main.tscn: TestZone (TileMapLayer col nuovo script) + Player.
  - Il formato dati delle zone vere (regions.json, design-world.md) NON e'
    stato introdotto: qui e' un banco di prova per le story successive, non
    contenuto di gioco. Lo schema in test_zone.gd e' volutamente codice.

- Come verificato A SCHERMO (finestra reale): script usa e getta che carica
  main.tscn e spinge il giocatore contro i muri con MOLTI frame (le prime
  prove fallivano solo perche' 140 frame non bastavano ad arrivare al muro —
  in finestra il loop gira piu' lento del previsto):
  * spinta a sinistra -> x minima 37.0 = faccia del muro (x=32) + raggio (5):
    collisione al pixel;
  * spinta in alto -> y minima 41.0 = faccia (y=32) + raggio (5) + offset
    del CollisionShape (-4): coerente;
  * screenshot overview / contro_muro_sx / contro_muro_su: si leggono bordo,
    muro interno con varco, corridoio, stanze, ostacoli; la camera si clampa
    all'angolo della zona quando il giocatore e' nel corner.
  Screenshot in user://us006/, non versionati.

- Regressione: 43/43 test headless, validator exit 0.

- Note ambiente:
  - Pillow ora INSTALLATO (pip install pillow, 12.3.0): generate_placeholders.py
    ora gira. Prima falliva con "Serve Pillow".
  - Headless e finestra girano a cadenze diverse rispetto ai frame di idle:
    `for i in N: await process_frame` NON e' N/60 secondi. Per le verifiche
    di movimento servono loop generosi (300-400) o misurare a convergenza.
  - I colori dei tile placeholder (pavimento marrone scuro, muro quasi nero,
    ostacolo marrone) hanno POCO contrasto: la mappa si legge ma a fatica.
    Migliorabile alzando il contrasto in generate_placeholders.py — non
    blocca US-006, ma utile prima delle story di combattimento.

- Resta aperto: contrasto tileset placeholder; velocita' 90 px/s (US-024);
  i limiti di zona ora hanno un chiamante (test_zone), il TODO di US-005 e'
  chiuso.
- Prossima storia: US-008 (attacco in mischia con arco). NB: US-007 gia'
  chiusa; l'ordine di priorita' salta a US-008.

[2026-09-02] US-021 spezzata; US-021 (a) — dati e AnimationSpec

- DECISIONE UTENTE: US-021 (macchina di animazione data-driven) va PRIMA del
  blocco combat US-008..011, cosi' il timing degli attacchi nasce gia' nei
  dati e non si riscrive. US-021 come scritta era pero' troppo grande per una
  iterazione (GameData + builder SpriteFrames + macchina a stati + player +
  segnali + test): spezzata in due nel prd.json.
  * US-021 (priority 7, QUESTA) = meta' dati, zero rendering.
  * US-021B (priority 8, nuova) = SpriteFrames a runtime + nodo macchina di
    animazione che emette i segnali per frame, integrato nel player.
  Le 4 story di combat sono passate a priority 9-12.

- Cosa fatto (US-021 a):
  - GameData carica data/animations.json come gli altri file singoli (ora
    25 file, non 24). Nuove API: get_animation(cat, nome),
    animation_convention(key), animation_categories(), animation_names(cat).
    ANIM_CATEGORIES = ["personaggio","nemico_base","pet"], le stesse che
    generate_placeholders.py conosce.
  - scripts/animation_spec.gd: RefCounted (niente class_name) che avvolge una
    voce di animations.json ed espone la vista tipata —
    frame_count/fps/loops/is_directional/duration_sec, phase_of(frame) ->
    anticipo|attivi|recupero|'', events_at/has_event_at, iframe_window()/
    is_invulnerable_at(frame), perfect_parry_frames()/is_perfect_parry_at(frame),
    prefix_state() (anticipo_in_stato). E' da qui che US-021B, US-008, US-009
    e US-010 leggeranno il timing.

- TRAPPOLA (vale per tutto il codice che legge numeri dal JSON): JSON.parse
  legge OGNI numero come float. `[0.0, 1.0].has(1)` con 1 int restituisce
  FALSE in Godot 4.3, e cosi' `Array == Array` fra float e int. AnimationSpec
  converte a int i frame di fase e di parata prima di confrontarli. Stesso
  problema che mordera' chiunque confronti un indice di frame letto dai dati.

- Come verificato: 54 test headless (43 + 11 nuovi in test_animation_spec.gd:
  campi base, fasi di attack_light, eventi hitbox, i-frame del dash, finestra
  di parata, anticipo_in_stato, e una coerenza frame/eventi su TUTTE le
  animazioni). validator exit 0. test_hot_reload aggiornato 24 -> 25 file
  (era un numero magico, esattamente il tipo di fragilita' di US-025).
  Nessuna verifica a schermo: US-021 (a) non tocca il rendering, quella parte
  e' US-021B.

- Prossima storia: US-021B (macchina di animazione e segnali, con verifica a
  schermo), poi US-008.

[2026-09-02] US-021B — macchina di animazione data-driven (b): rendering e segnali

- Cosa fatto:
  - scripts/animation_machine.gd (extends AnimatedSprite2D, niente class_name):
    * configura(categoria) costruisce lo SpriteFrames a runtime dagli
      spritesheet assets/placeholder/<cat>_<nome>.png. Una sotto-animazione
      per direzione ("walk_down"..) sulle direzionali, una sola ("death")
      sulle altre. fps/loop presi da AnimationSpec, cioe' dai dati.
    * riproduci(stato, direzione) sceglie "<stato>_<dir>" o "<stato>";
      stato inesistente = no-op, nessun crash.
    * segnali: evento_frame(nome) per gli eventi per frame (hitbox_on/off,
      footstep, tell_audio, tell_visivo, ability_release), fase_cambiata(fase)
      al passaggio anticipo->attivi->recupero, animazione_finita(stato).
    * spec_corrente() restituisce l'AnimationSpec dello stato in corso: da
      qui US-009 leggera' iframe_window e US-010 finestra_perfetta.
    * si riaggancia a GameData.data_reloaded: F5 ricostruisce lo SpriteFrames,
      quindi cambiare un fps o un indice nel JSON e premere F5 cambia la resa
      senza ricompilare.
    * le connessioni dei segnali stanno in configura(), non in _ready(), cosi'
      la macchina e' testabile fuori dall'albero.
  - scripts/player.gd: _aggiorna_sprite() (region_rect a mano) SOSTITUITO da
    _aggiorna_animazione() che chiama _anim.riproduci("walk"|"idle", dir).
    Via le const TEX_IDLE/TEX_WALK/FRAMES_*/RIGA e _fase_anim.
  - scenes/player.tscn: il nodo Sprite2D e' ora un AnimatedSprite2D
    "AnimationMachine" con lo script, stesso offset (0,-8).

- Come verificato:
  * 6 test nuovi in test_animation_machine.gd (60 totali): SpriteFrames
    costruito, conteggi frame/fps/loop dai dati, evento_frame emesso ai
    frame giusti di attack_light, sequenza di fase anticipo->attivi->recupero,
    spec_corrente espone la finestra i-frame del dash, stato ignoto no-op.
  * A SCHERMO (finestra reale): camminando a destra i frame di walk_right
    avanzano 0->1->2 (l'animazione gira davvero), cambiando input diventa
    walk_up, da fermo torna a idle_up. Screenshot in user://us021b/.
  * validator exit 0.

- Note ambiente: nei test i frame si fanno avanzare a mano (m.frame = N):
  AnimatedSprite2D avanza col suo timer interno in _process, che in headless
  non scorre. Il setter di `frame` emette comunque frame_changed.

- Resta aperto: US-021 e' chiusa in entrambe le meta'. Le hitbox di US-008 e
  gli i-frame di US-009 si agganceranno ai segnali di AnimationMachine.
- Prossima storia: US-008 (attacco in mischia con arco).

[2026-09-02] US-008 — Attacco in mischia con arco

- Cosa fatto:
  - scripts/hurtbox.gd (Area2D generico, layer 3): zona vulnerabile. subisci()
    instrada il danno sullo StatsComponent del genitore ed emette colpito().
    set_invulnerabile() per gli i-frame di US-009 e la parata di US-010.
    Ignora i colpi dal proprietario (niente autodanno).
  - scripts/hitbox.gd (Area2D, mask 3): arco offensivo. attiva()/disattiva()
    lo accendono; colpisce ogni hurtbox toccata UNA volta per attivazione.
    Forma e grafica placeholder = settore circolare (angolo, raggio)
    costruito a runtime, nella direzione guardata.
  - scripts/player.gd: stato _attaccando. Input "attacco" -> _inizia_attacco()
    orienta la hitbox e lancia l'animazione "attack_light". La hitbox si apre
    su hitbox_on e si chiude su hitbox_off — eventi che AnimationMachine
    emette DAI FRAME di animations.json (attack_light: anticipo [0,1],
    attivi [2], recupero [3,4]). animazione_finita -> _attaccando=false:
    non si ri-attacca finche' non e' finita, quindi nemmeno in recupero.
    Hitstop: Engine.time_scale a 0.05 per hitstop_ms reali, poi ripristino.
  - player.tscn: nodi Hurtbox e Hitbox.
  - project.godot: azione "attacco" (J + tasto sx del mouse).
  - data/balance.json: nuova sezione "combattimento" — danno/stagger/angolo/
    raggio dell'attacco leggero + hitstop_ms. Il TIMING resta in
    animations.json; qui solo i numeri che non sono timing.

- Come verificato:
  - A SCHERMO (finestra reale): manichino con StatsComponent+Hurtbox alla
    destra del giocatore. Guarda a destra, attacca -> hp 100 -> 88 (danno 12,
    esatto da balance.json), lo screenshot al frame "attivi" (bordo rosso del
    placeholder) mostra l'arco a settore nella direzione guardata. Fasi viste
    dall'animazione: anticipo -> attivi -> recupero. Ri-premere "attacco"
    durante l'attacco non lo fa ripartire (hp scende di 12 una volta sola).
    time_scale torna a 1 dopo l'hitstop. Screenshot in user://us008/.
  - 4 test headless in test_combat.gd (64 totali): danno instradato,
    invulnerabile annulla, niente autodanno, segnale colpito. validator 0.

- Note ambiente: la finestra scorre piu' lenta di 60fps nei loop di verifica;
  per vedere un attacco intero servono ~250 iterazioni di await process_frame,
  non 40. L'hitstop allunga ancora la durata reale.

- Resta aperto: hitstop e shake passeranno a data/audio.json con US-019
  (combat_feedback). L'arco vero (angolo, non cerchio) e' gia' un settore
  ConvexPolygonShape2D; melee_arc.gd della primitiva abilita' resta separato.
- Prossima storia: US-009 (schivata con i-frame).

[2026-09-02] US-009 — Schivata con frame di invulnerabilita'

- Cosa fatto:
  - animation_machine.gd: nuovo segnale finestra_cambiata(nome, attiva),
    emesso quando il frame corrente entra/esce da iframe_da..a ("iframe") o
    da finestra_perfetta ("parata_perfetta"). Simmetrico a fase_cambiata.
    US-010 usera' lo stesso segnale per la parata.
  - player.gd: start_dash(spec) — entry point condiviso fra l'input
    "schivata" (Spazio / Shift sx) e la primitiva "dash" di ability_engine.
    Direzione = input corrente o, se fermo, get_facing(). Durante il dash
    velocity = _dash_vel (distanza/durata) e move_and_slide; il resto del
    movimento e' bypassato. _dash_cd conta alla rovescia: niente spam.
    finestra_cambiata("iframe") -> _hurtbox.set_invulnerabile() + tinta ciano
    dello sprite come indicatore visivo. _fine_dash forza comunque
    invulnerabile=false.
  - project.godot: azione "schivata".
  - data/balance.json combattimento.schivata: distanza/durata/cooldown.
    La finestra di i-frame NON e' qui: sono i frame iframe_da/a di
    animations.json personaggio.dash (0..2 su 4).
  - hitbox.gd: settore >= 180 gradi ora diventa un CircleShape2D invece di
    un ConvexPolygonShape2D concavo (che Godot rifiutava lasciando la hitbox
    senza forma). Serve agli attacchi ad ampio raggio dei nemici.

- Come verificato:
  - A SCHERMO: dash dx ~99 px (distanza 96 da balance + scivolata), il
    segnale iframe si apre, lo sprite si tinge di ciano (piu' il bordo ciano
    del placeholder), premere di nuovo "schivata" durante il dash non fa
    nulla e schivata_pronta() e' false subito dopo. i-frame reale: una
    hitbox nemica da 25 danni toglie 25 hp a giocatore fermo, ZERO hp al
    giocatore che la attraversa in dash mentre e' invulnerabile.
    Screenshot in user://us009/.
  - test_animation_machine.gd: 1 test nuovo (finestra_cambiata iframe).
    65 test totali, validator 0.

- GDSCRIPT, trappola nei test di verifica: una lambda cattura le variabili
  locali PER VALORE — riassegnare una var esterna da dentro la lambda non si
  propaga fuori. Usare un Dictionary (tipo riferimento) per raccogliere flag.

- Prossima storia: US-010 (parata e rottura di postura).

[2026-09-02] US-010 — Parata e rottura di postura

- Cosa fatto:
  - scripts/postura_component.gd (Node riusabile, lo montera' il nemico di
    US-011): postura/postura_max, erodi(q), a zero -> vulnerabile per
    durata_vulnerabile s (segnale postura_rotta), poi torna piena
    (vulnerabilita_finita). Fuori dalla rottura si ricarica di
    recupero_al_secondo dopo 0.8 s di pausa. Tutti i numeri da
    balance.json.combattimento.postura.
  - scripts/hurtbox.gd: enum Parata {NESSUNA, BLOCCO, PERFETTA}. subisci()
    ora passa per la parata: PERFETTA -> danno e stagger a 0 + segnale
    parata_riuscita(true, attaccante); BLOCCO -> danno * (1-riduzione) +
    parata_riuscita(false, ...). Lo stagger di ogni colpo (non parato
    perfetto) va sulla PosturaComponent del bersaglio via _cerca("erodi").
    Rinominato _stats() -> _cerca(metodo) generico.
  - scripts/player.gd: input "parata" (K / tasto dx mouse) TENUTO. Alla
    pressione set_parata(BLOCCO) + animazione "parry"; finestra_cambiata
    ("parata_perfetta") alterna PERFETTA/BLOCCO sui frame finestra_perfetta
    [0,1] di animations.json. Al rilascio set_parata(NESSUNA). Su parata
    perfetta erode la postura dell'attaccante di
    danno_postura_parata_perfetta. Non si attacca/schiva mentre si para.
  - project.godot: azione "parata". balance.json: sezioni parata e postura.

- Come verificato:
  - 7 test in test_postura.gd (71 totali): erodi, rottura + segnale,
    recupero, parata perfetta annulla, blocco dimezza, stagger -> postura.
  - A SCHERMO: tenendo "parata" lo stato passa NESSUNA -> PERFETTA (frame
    iniziali) -> BLOCCO (finestra chiusa, guardia ancora su) -> NESSUNA al
    rilascio; l'animazione e' "parry" e il placeholder mostra il bordo verde
    (finestra perfetta). Postura erosa a zero -> vulnerabile -> recupero a
    100. Screenshot in user://us010/. validator 0.

- Note: la negazione/riduzione del danno via hitbox reale non e' stata
  verificata a schermo (la finestra perfetta e' ~0.11 s e centrarci una
  hitbox nei loop lenti di verifica non e' affidabile) ma e' coperta dai
  test unitari. Il nemico vero che la sfrutta arriva con US-011.
- Prossima storia: US-011 (nemico base con telegrafia).

[2026-09-02] US-011 — Nemico base con telegrafia

- Cosa fatto:
  - scripts/enemy.gd (CharacterBody2D): macchina a stati esplicita
    IDLE / INSEGUIMENTO / ANTICIPO / ATTACCO / RECUPERO / STAGGER / MORTO.
    IDLE->INSEGUIMENTO su raggio_aggro, ->ANTICIPO su raggio_attacco.
    ANTICIPO = tell: tinta rossa + animazione "anticipo" (durata dal JSON,
    nemico_base.anticipo, 4 frame a 8 fps); a fine animazione -> ATTACCO,
    hitbox su hitbox_on/off, poi RECUPERO (recupero_s) -> IDLE.
    PosturaComponent.postura_rotta -> STAGGER finche' vulnerabilita_finita.
    StatsComponent.died -> _su_morte: emette morto(self), NON queue_free;
    disattiva collisione e hurtbox, riproduce "death".
  - scenes/enemy.tscn: AnimationMachine("nemico_base") + StatsComponent +
    PosturaComponent + Hurtbox (layer 3) + Hitbox (mask 3). collision_layer 2.
  - scenes/player.tscn: Player nel gruppo "player" (il nemico lo trova cosi').
  - scenes/main.tscn: un'istanza di Enemy a (430,496), fuori dall'aggro
    iniziale.
  - data/balance.json: sezione nemico_base (raggi, danno, recupero, postura).
    La durata del tell resta in animations.json.

- Come verificato:
  - 3 test in test_enemy.gd (74 totali): parte IDLE coi componenti,
    morte -> segnale + nodo non distrutto, postura rotta -> STAGGER.
  - A SCHERMO: il giocatore si avvicina, il nemico passa
    INSEGUIMENTO -> ANTICIPO (screenshot: nemico tinto di rosso) -> ATTACCO
    (fa 10 danni al giocatore) -> RECUPERO. Colpito dal giocatore perde hp;
    portato a 0 va in MORTO, riproduce "death", resta nella scena, emette
    "morto". Screenshot in user://us011/.
  - validator 0.

- Nota: con i placeholder il tell rosso e' un cambio di tinta sottile (il
  corpo del nemico e' gia' rosso). L'arte vera lo rendera' netto; la meccanica
  e la durata sono dati.
- Prossima storia: US-014 (validator dei dati nei test).

[2026-09-02] US-014 — Validator dei dati integrato nei test

- Cosa fatto: tests/test_data_valida.gd esegue tools/validate_data.py con
  OS.execute (prova python, python3, py) e fa fallire la suite se esce != 0,
  allegando l'output. Se nessun interprete Python c'e', push_warning e passa
  (una macchina di solo-Godot non deve rompersi; la CI di US-026 avra' Python).
  README: nota che la suite include il validator.
- Come verificato: 75 test passano con dati integri. Rotto di proposito
  data/tags.json -> test_data_valida FALLISCE ("uscito con codice 1: 155
  errori...") e con lui i test che dipendono dai tag. Ripristinato.
- Prossima storia: US-015 (salvataggio e caricamento versionati).

[2026-09-02] US-015 + US-016 — salvataggio versionato, infrastruttura test

US-015 — scripts/save_system.gd (autoload SaveSystem):
- salva(slot, dati): assembla il doc {schema_version, salvato_il,
  nome_personaggio, tempo_gioco, posizione [x,y], statistiche,
  evocazioni []}, scrive su <slot>.json.tmp e poi rename_absolute atomico.
  evocazioni: lo slot per i summon durata -1 c'e' anche se i summon non
  esistono ancora (design-pathways.md).
- carica(slot): SOLO JSON.parse_string. Radice non-oggetto -> errore.
  schema_version > VERSIONE_CORRENTE (2) -> rifiuto, file non toccato.
  < corrente -> catena _migra() (1->2 aggiunge evocazioni:[]).
  Ogni campo passa da _campo(typeof, default) o _posizione(): tipo
  sbagliato -> default sano + push_warning, mai crash.
- file corrotto -> {ok:false, reason}, stato_slot() = CORROTTO, MAI
  cancellato. cancella(slot) e' l'unica via, sempre esplicita.
- scripts/game_state.gd (autoload GameState): tiene tempo_gioco e
  nome_personaggio (default "Enel"), snapshot() raccoglie posizione e
  hp/spiritualita' dal giocatore vivo, applica() li rimette. F9 salva /
  F10 carica lo slot 0 in debug (finche' non c'e' la UI a libro).
- 9 test in test_save_system.gd: round-trip, .tmp non residuo, json rotto
  non cancella, versione futura rifiutata senza toccare il file,
  migrazione da v1, campi non fidati -> default, slot vuoto, round-trip
  via GameState. 83 test totali.
- TRAPPOLA GDSCRIPT: un valore di enum (`enum Slot { ..., OK, ... }`) e'
  accessibile anche NON qualificato e SHADOWA la costante Error `OK` (0).
  `err != OK` diventava `err != Slot.OK` (1). Rinominato Slot.OK -> VALIDO.
  Stesso rischio con `const OK := "..."`: rinominato in R_OK.
- Nessuna verifica a schermo richiesta dai criteri. Boot headless col nuovo
  autoload: nessun errore (gli "Unreferenced static string"/"Pages in use"
  sono il rumore di Godot ucciso da `timeout`, non del codice).

US-016 — infrastruttura test headless: era gia' quasi tutta in piedi
(tests/run_tests.gd, exit 0/1, comando in README e CLAUDE.md). Con
test_save_system.gd ora ci sono test per tutti e 4 i sistemi richiesti dal
criterio (GameData, StatsComponent, AbilityEngine, save/load). README:
sezione test aggiornata.

- Prossima storia: US-017 (HUD minimo).

[2026-09-02] US-017 — HUD minimo + i18n dal giorno 1

- Cosa fatto:
  - i18n: assets/i18n/strings.csv (keys,it,en) -> Godot genera
    strings.it.translation / strings.en.translation (committati).
    project.godot [internationalization]: translations + fallback "it".
  - stats_component.gd: nuovo segnale spiritualita_changed(sp, sp_max),
    simmetrico a hp_changed. Il setter emette solo su cambiamento reale.
    (US-024 chiedeva questo segnale: gia' fatto qui.)
  - scenes/hud.tscn + scripts/hud.gd (CanvasLayer): due ProgressBar +
    Label, trova il giocatore nel gruppo "player", si aggancia a
    hp_changed / spiritualita_changed. Ogni stringa da tr() — HUD_HP,
    HUD_SPIRITUALITA, HUD_HP_VALORE ("{0} / {1}").
  - HUD aggiunto a main.tscn.

- Come verificato:
  - 4 test in test_i18n_hud.gd (87 totali): locale it/en caricati e le
    chiavi traducono, chiave ignota -> se stessa, stats emette
    spiritualita_changed, la barra hp del HUD segue il segnale.
  - A SCHERMO: "Salute 60 / 100" e "Spiritualità 30 / 50" con barre
    riempite, testo italiano (locale di default), le barre calano quando il
    giocatore subisce danno. Screenshot user://us017/.

- Nota: stile del HUD spartano (ProgressBar di default). Lo stile vero e' la
  UI a libro di design-ui-libro.md, fase piu' avanti. Qui conta l'aggancio
  ai segnali e l'i18n.
- Prossima storia: US-018 (scena di debug con manichino).

[2026-09-02] US-018 — Scena di debug con manichino

- Cosa fatto:
  - scripts/ability_engine.gd: esegui_primitiva(tipo, prim, caster) —
    esegue UNA primitiva saltando costo e cooldown (origine "debug:<tipo>").
    Per la scena di debug e i test.
  - scripts/dummy.gd + scenes/dummy.tscn: manichino Node2D con
    StatsComponent + Hurtbox; _process rimette hp al massimo ogni frame
    ("hp infiniti"). Conta i colpi ricevuti.
  - scripts/debug_scene.gd + scenes/debug/debug_scene.tscn: TestZone +
    Player + 2 Enemy + Dummy + HUD + un CanvasLayer con pannello (hp, sp,
    cooldown dash, ultima primitiva, legenda tasti). Tasti 1-5 eseguono
    projectile / melee_arc / buff_stat / heal / dash sul giocatore.
  - NON in release: la scena vive sotto scenes/debug/ (i preset di export
    devono escludere quella cartella — export_presets.cfg non e' ancora
    committato) e in piu' _ready fa queue_free() se non OS.is_debug_build().

- Come verificato:
  - 3 test in test_debug_primitive.gd (90 totali): buff_stat/heal isolati,
    primitiva ignota -> ok:false senza crash.
  - A SCHERMO: la scena carica con giocatore + 2 nemici + manichino + HUD +
    pannello. Premendo 1..5 le cinque primitive si eseguono in ordine
    (pannello: "ultima primitiva" cambia; heal 50 -> 65). Il manichino
    messo a 5 hp torna a 100 il frame dopo. Screenshot user://us018/.
- Prossima storia: US-019 (bus audio e feedback di colpo).

[2026-09-02] US-019 — Bus audio e feedback di colpo

- Cosa fatto:
  - GameData carica data/audio.json (get_audio(sezione)). 26 file ora
    (test_hot_reload aggiornato 25 -> 26).
  - scripts/audio_manager.gd (autoload AudioManager):
    * _costruisci_bus() crea master/music/sfx/ambience/ui/whisper da
      audio.json.buses con volume e routing (parent -> Master).
    * feedback(nome) legge audio.json.combat_feedback[nome] e fa partire
      INSIEME: sfx (sul bus sfx, pitch random +-pitch_var), hitstop
      (Engine.time_scale a 0.05 per hitstop_ms, in tempo reale via
      Time.get_ticks_msec + ripristino in _process — NON await su timer,
      che non scorre nei test), shake (segnale shake_richiesto se
      shake > 0).
    * accessibilita': imposta_accessibilita()/accessibilita(). riduci_hitstop
      moltiplica l'hitstop per 0.35; disattiva_shake sopprime il segnale.
    * SFX PLACEHOLDER SINTETIZZATI in codice (non posso produrre file):
      _sintetizza(freq, durata, rumore) -> AudioStreamWAV, un burst con
      inviluppo esponenziale. Frequenza diversa per categoria: parry_perfect
      (1040 Hz, 0.26 s) suona davvero diverso da parry_normal (620, 0.09).
      Si sostituiscono con file veri senza toccare il codice.
  - scripts/game_camera.gd: shake a "trauma" agganciato a
    AudioManager.shake_richiesto (accumula, decade, ampiezza col quadrato).
    Usa offset, indipendente dal position_smoothing.
  - player.gd: l'hitstop LOCALE e' stato RIMOSSO. Ora _su_colpo_inflitto ->
    feedback("hit_light"), _su_danno_subito -> feedback("damage_taken"),
    _su_parata_riuscita -> feedback("parry_perfect"|"parry_normal"),
    start_dash -> feedback("dash"). data/balance.json: tolto hitstop_ms
    (ora vive in audio.json, come chiedeva l'audit).
  - enemy.gd: feedback("hit_light") quando piazza un colpo,
    feedback("posture_break") sulla rottura di postura.

- Come verificato:
  - 5 test in test_audio.gd (95 totali): bus creati, feedback emette lo
    shake col valore dei dati, disattiva_shake lo sopprime, l'hitstop
    abbassa time_scale ed e' in corso subito dopo, voce ignota non esplode.
  - A SCHERMO: colpendo un nemico -> shake_richiesto emesso, time_scale
    scende a 0.05 (hitstop), offset camera fino a ~2.4 px poi decade.
    Con disattiva_shake: 0 eventi shake. parry_perfect e parry_normal hanno
    sfx e hitstop diversi nei dati. Screenshot user://us019/.

- Nota: gli sfx sono sintetizzati, non file. Per la fase 6 servono file veri
  (bfxr/sfxr per la fase 1, poi un musicista): audio.json e' gia' la
  specifica, i nomi sfx_* sono i placeholder da rimpiazzare.
- Nota cosmetica: il runner headless ora stampa "3 resources still in use at
  exit" DOPO quit(0) — sono gli AudioStreamWAV sintetizzati, rilasciati in
  ritardo rispetto al sottosistema risorse nella chiusura brusca del runner.
  L'exit code resta 0 (verificato), i test passano, il gioco vero non e'
  toccato. _exit_tree di AudioManager li azzera ma non anticipa abbastanza.
- Fase 1: 21/26 story originali + tutte le 5 di risanamento ancora aperte
  (US-022..026). Mancano US-020 (tell sonoro nemici) e US-026 (CI, P2).
- Prossima storia: US-020 (tell sonoro degli attacchi nemici).

[2026-09-03] US-020 — Tell sonoro degli attacchi nemici

- Cosa fatto:
  - scripts/audio_manager.gd: nuovo segnale tell_emesso(pos_mondo, categoria),
    carica audio.json.telegraph, metodo tell(id, pos_mondo) -> durata_s.
    Il tell suona SEMPRE, a prescindere dall'inquadratura (non silenziabile
    dalle opzioni audio: e' leggibilita', non atmosfera) e RESTITUISCE
    anticipo_ms/1000: e' il tell a fissare la durata della fase di anticipo.
    5 sfx placeholder sintetizzati distinti aggiunti a SFX_SPEC (sfx_tell_*):
    freq/durata/rumore tutti diversi (light 880Hz corto, heavy 150Hz lungo,
    unblock 330Hz rumorosissimo, ranged 560Hz, ritual 110Hz molto lungo).
  - scripts/enemy.gd: entrando in ANTICIPO chiama _fai_partire_tell() (che
    fa partire il tell e legge la durata) e ora _physics_process conta
    _anticipo_left col delta -> ATTACCO. RIMOSSO il trigger
    animazione_finita("anticipo") -> ATTACCO: l'anim 'anticipo' e' solo
    cosmetica, la durata reale e' anticipo_ms. Quale tell usa il nemico e'
    un dato: balance.json.nemico_base.telegraph = "wind_up_light".
  - scripts/tell_indicator.gd + scenes/tell_indicator.tscn (CanvasLayer +
    Control): ascolta tell_emesso e disegna, per DURATA 0.6s:
    * nemico inquadrato + indicatore_visivo_tell -> anello che si stringe
      sulla sua posizione, colore per categoria (schivabile giallo,
      parabile ciano, solo_schivata rosso, coprirsi arancio, interrompere
      magenta);
    * nemico fuori schermo + indicatore_direzione_suono -> freccia sul
      bordo schermo che punta verso la minaccia.
    Le due opzioni si rileggono da audio.json.accessibilita a ogni tell.
    Aggiunto a main.tscn e debug_scene.tscn.
  - tools/validate_data.py: nemico_base.telegraph deve puntare a un id di
    audio.json.telegraph (altrimenti il nemico non ha tell ne' durata).
- Come verificato:
  - 112 test headless (108 + 4): nemico fuori schermo produce comunque il
    tell (tell_emesso scatta con una categoria); anticipo_ms fissa la durata
    (300ms -> ANTICIPO a 0.2s, ATTACCO a 0.4s); l'indicatore scade da solo;
    la freccia direzionale resta dentro lo schermo per ogni angolo.
    validator exit 0.
  - A SCHERMO (finestra reale, script usa e getta): tell on-screen -> anello
    ciano che si espande e sfuma vicino al player; tell off-screen -> freccia
    rossa sul bordo alto-destra verso la minaccia; nemico vero in ANTICIPO ->
    tinta rossa (tell visivo US-011) + anello giallo (categoria schivabile).
    Screenshot in user://us020/.
- NOTA scope: story piu' grande della norma (10 file, ~150 righe di codice
  di produzione) perche' introduce un sistema nuovo (tell audio + indicatore
  visivo + indicatore direzionale + integrazione nel nemico). Coeso, il grosso
  e' un file nuovo isolato (tell_indicator.gd). Non spezzata: e' l'ultima
  story del blocco combat e i pezzi non hanno senso separati.
- Prossima storia: US-026 (CI minima) — ultima della fase 1.

[2026-09-03] US-026 — CI minima  → FASE 1 CHIUSA

- Cosa fatto:
  - .github/workflows/ci.yml: un job su push e pull_request.
    1. actions/setup-python 3.12
    2. python tools/validate_data.py (fallisce il job se esce != 0)
    3. scarica Godot 4.3 headless dalle release ufficiali (sudo install in
       /usr/local/bin/godot)
    4. godot --headless --import (|| true: l'import 4.3 non sempre esce 0,
       il gate vero e' la suite)
    5. godot --headless --script res://tests/run_tests.gd (fallisce il job
       se esce != 0)
  - README: badge CI in cima, sezione Stato riscritta ("Fase 1 chiusa,
    112 test, CI su ogni push"), sezione ralph ripulita dai riferimenti a
    US-016 (chiusa da un pezzo).
- Come verificato IN LOCALE (non posso far girare GitHub Actions da qui):
  simulato il flusso della CI spostando via .godot/ (gitignorato, si
  rigenera): `--import` esce 0 su checkout pulito, poi validator exit 0 e
  112/112 test verdi. .godot ripristinato. NOTA: l'import rigenera i due
  assets/i18n/*.translation con un header/hash diverso ma contenuto
  identico (stessa dimensione) — sui CI non tocca il repo, in locale
  ripristinati con git checkout.
- FASE 1 — FONDAMENTA: CHIUSA. 27 story (21 originali con US-021 a/b +
  US-022..026 di risanamento), 112 test headless, validator 0, CI.
  Il PRD della fase 2 (Pathway core, ~22 story) si genera con /prd quando
  si riprende — vedi 006_PRD/roadmap.md. Punto di controllo dell'intero
  progetto: fase 5.
- Prossima storia: nessuna in fase 1. Fase 2: generare il PRD con /prd,
  poi convertirlo in prd.json con /ralph.

[2026-09-02] US-024 — Stats di partenza interamente da balance.json

- Cosa fatto:
  - data/balance.json: nuova sezione "stats_base" con velocita/difesa/evasione.
    Non dipendono dalla Sequenza (hp_max e spiritualita_max si', quelli
    restano sulle curve). velocita ALZATA da 90 a 140 px/s: a 90 servivano
    ~7s per attraversare i 640px dello schermo, lento — era il TODO lasciato
    aperto da US-004/005/006. PLAUSIBILE, non bilanciato.
  - scripts/stats_component.gd: i tre numeri hardcoded (90/0/0, ex riga ~55-57)
    RIMOSSI. configure_from_balance legge stats_base da GameData.get_balance.
    I fallback sono ora una const _FALLBACK unica, e se GameData manca il
    metodo emette push_warning invece di partire zitto con valori plausibili
    (criterio esplicito).
  - Segnale spiritualita_changed: gia' aggiunto in US-017 (lo chiedeva anche
    US-024). Nessuna modifica, criterio gia' soddisfatto.
- Come verificato: 97 test headless (95 + 2 nuovi in test_stats_component:
  stats_base viene dai dati, _FALLBACK copre ogni STAT_KEYS). I test che
  citavano velocita 90 ora leggono l'atteso da balance.json via GameData
  (_atteso()), non piu' numero magico. validator exit 0 (8 warning
  informativi preesistenti).
- Note: il "SCRIPT ERROR: Nonexistent 'float' constructor" nel log della
  suite e' PREESISTENTE (verificato con git stash), non una regressione:
  e' un test che sonda di proposito un path con dato assente.
- Prossima storia: US-022 (loader JSON robusto ai dati corrotti).

[2026-09-02] US-022 — Loader JSON robusto ai dati corrotti

- Cosa fatto:
  - scripts/game_data.gd: nuovi helper di coercizione difensiva (_as_array,
    _object_list, _dict_or_empty, _array_or_empty, _num_or). Un JSON
    sintatticamente valido ma strutturalmente storto non arriva piu' a
    un'assegnazione tipata (in GDScript un errore che interrompe il ciclo di
    caricamento a meta').
    * _load_pathways/_load_abilities/_load_synergies iterano su _object_list:
      'sequences'/'abilities'/'synergies' come oggetto invece che lista ->
      errore + lista vuota; elemento non-oggetto -> errore + scartato uno a
      uno, gli altri si caricano.
    * _load_single ha un 4o argomento key_type: se la chiave attesa ha la
      forma sbagliata (tags come oggetto, primitives come lista...) il file
      e' SCARTATO con un errore, non caricato a meta'.
    * i getter che facevano `var x: Dictionary = _dati.get(k, {})` (get_primitive,
      has_tag, get_balance, curve_value, tag_count, get_animation,
      animation_convention, animation_names, get_audio) ora passano da
      _dict_or_empty/_array_or_empty/_num_or: nessun crash se il valore
      interno fosse del tipo sbagliato.
  - scripts/ability_engine.gd: helper _num (numero o fallback, accetta anche
    stringhe numeriche) e _flag (bool o fallback). Il costo, il cooldown e
    tutti i parametri delle 5 primitive passano da _num/_flag: `float({})`
    solleva "Nonexistent constructor" e va evitato. In execute(): se
    'primitive' non e' una lista -> push_error + warning + abilita' senza
    effetti (non un crash); ogni elemento non-oggetto della lista -> scartato
    con warning.
  - tests/test_i18n_hud.gd: DRIVE-BY. `float(s.get("spiritualita_max"))`
    chiamava Object.get() su una proprieta' inesistente -> null -> il
    "SCRIPT ERROR: Nonexistent 'float' constructor" che sporcava OGNI run
    della suite (segnalato in US-024). Corretto in s.get_stat(...). Ora la
    suite gira pulita.
  - 5 fixture in tests/fixtures/ (fuori da data/, non matchano test_*.gd):
    radice_lista, sequences_oggetto, elementi_non_oggetto, tags_oggetto.

- Come verificato: 104 test headless (95 + 7 nuovi in test_loader_robusto +
  2 in test_stats_component da US-024). I 7 nuovi: radice non-oggetto,
  lista attesa ma oggetto, elementi non-oggetto saltati uno a uno, single
  file di forma sbagliata scartato, getter non esplodono su chiave ignota,
  ability_engine con 'primitive' non-lista, ability_engine con costo di tipo
  sbagliato. validator exit 0. Il SCRIPT ERROR nella suite e' sparito.
- Note: i test che usano gli helper chiamano poi reload() per riazzerare
  _errors del singleton. I test ability_engine iniettano in _abilities via
  gd.get("_abilities") e ripuliscono con erase().
- Prossima storia: US-023 (pulizia cooldown e indici).

[2026-09-02] US-023 — Pulizia di cooldown e indici

- Cosa fatto:
  - scripts/ability_engine.gd: _cooldowns aveva una voce per ogni coppia
    (caster, abilita') e non la toglieva mai -> una partita lunga accumulava
    una voce per ogni entita' morta. sweep_cooldowns() (pubblica) rimuove le
    voci scadute e quelle il cui caster non e' piu' valido
    (is_instance_id_valid sull'id incapsulato nella chiave). Chiamata da
    _process ogni _SWEEP_OGNI_MS (5s). cooldown_entries() accessorio per i test.
  - scripts/game_data.gd: _prune(index, visti). Ogni _load_* raccoglie in un
    Dictionary gli id effettivamente visti su disco, poi _prune toglie
    dall'indice quelli non piu' presenti: un id cancellato/rinominato nei
    JSON SPARISCE dopo F5 invece di restare in memoria. Le voci ancora
    presenti NON si toccano: _upsert ne aggiorna il contenuto sul posto,
    quindi l'identita' dei riferimenti tenuti dai sistemi resta (US-003).
    _load_single fa gia' clear()+merge, non serve prune.
- Come verificato: 107 test headless (104 + 3):
  * test_ability_engine: cooldown di caster liberato purgato; cooldown
    scaduto purgato anche con caster vivo.
  * test_hot_reload: id fantasma iniettato nell'indice sparisce dopo reload,
    i dati veri restano (stesso ability_count di prima dell'iniezione),
    zero errori. test_reload_conserva_l_identita_dei_riferimenti ancora
    verde: il prune non rompe l'identita' delle voci sopravvissute.
  validator exit 0.
- Prossima storia: US-025 (test onesti e non fragili).

[2026-09-02] US-025 — Test onesti e non fragili

- Cosa fatto:
  1. test_game_data.test_ogni_abilita_riferita_esiste: la lista 'mancanti'
     veniva costruita e MAI controllata (asseriva solo ability_count == 32).
     Ora assert_eq(mancanti.size(), 0): ogni id di abilita' elencato da una
     Sequenza deve risolvere. Stato attuale dei dati: 32 riferite = 32
     caricate, 0 mancanti, 0 orfane (verificato). Il 32 magico e' sparito,
     resta assert_gt(..., 0).
  2. Ramo 'primitiva FUORI REGISTRO' di execute() prima non coperto: nuovo
     test_primitiva_fuori_registro_e_errore inietta un'abilita' con una
     primitiva inventata e verifica il warning "fuori registro" + abilita'
     che prosegue senza effetti. Aggiunto anche il gemello
     test_primitiva_nel_registro_ma_non_implementata_prosegue (illusion):
     i tre esiti distinti ora hanno un test ciascuno.
  3. Numeri magici -> derivati dai dati:
     * test_vocabolario_dei_tag: non piu' `== 82`. Legge tags.json e verifica
       che il loader abbia caricato TUTTE le voci. La guardia del vocabolario
       chiuso e' passata al validator Python: nuovo check anti-duplicato in
       validate_data.py (verificato: dup di 'acqua' -> exit 1).
     * test_hot_reload.test_reload_riporta_conteggi_e_errori: non piu'
       `files_loaded == 26` (si rompeva aggiungendo un JSON legittimo, gia'
       toccato 24->25->26). Ora: > 0, stabile fra due reload, errori == 0.
     * i conteggi 10/100 (pathway/sequenze) RESTANO fissi: hanno un commento
       esplicito "e' lo scope deciso, cambiarlo e' un bug" — sono guardie
       volute, non fragilita'.
  4. run_tests.gd: se _discover() torna vuoto -> "NESSUN file di test
     trovato" + quit(1) (prima: 0 test, falso verde). Anche total == 0 dopo
     l'esecuzione -> quit(1). Verificato puntando il runner su tests/fixtures
     (nessun test_*.gd): exit 1.
  5. Setup comune AbilityEngine: il runner ora chiama suite.prepara() prima
     di ogni test se esiste. test_ability_engine.prepara() fa clear_cooldowns
     + flush_effects. Rimosse le ~8 chiamate esplicite ridondanti sparse nei
     test.
- Come verificato: 108 test headless tutti verdi, validator exit 0.
  Il guard anti-0-test verificato a parte (exit 1).
- Prossima storia: US-020 (tell sonoro degli attacchi nemici).

---

[SETUP-9] FASE 2 aperta — prd.json convertito, ralph non lanciabile

- Cosa fatto: 006_PRD/prd-fase-2-pathway-core.md convertito in prd.json
  (29 story US-201..US-229, campo "fase": 2, tutte passes:false).
  branchName ralph/fase-2-pathway-core. Ordine di priorita' dal blocco A
  (progressione + primitive + capacita' motore) fino a J, con US-228 subito
  dopo US-202/203 (le primitive che le servono) e US-219 (verdetto
  sull'architettura) prima del blocco I. prd.json di fase 1 archiviato in
  archive/2026-09-03-fase-1-fondamenta/.
- RALPH NON LANCIABILE su questo PC: ralph.sh riga 108 usa
  `docker sandbox run claude`, ma Docker 29.6.2 ha RIMOSSO il plugin
  `docker sandbox` ("deprecated and has been removed, migrate to Docker
  Sandboxes"). In piu' Docker Desktop non era in esecuzione. Deciso con
  l'utente: le story di fase 2 le lavora Claude a mano in sessione
  interattiva, stesso flusso di ralph (una story per iterazione, test +
  regressioni, commit US-2NN, questo file aggiornato) ma senza container.
- Note ambiente: eseguibile Godot a
  ./Godot_v4.3-stable_win64.exe/Godot_v4.3-stable_win64_console.exe
  (cartella con lo stesso nome del file). Suite: 112 test a fase 1 chiusa.

[2026-09-03] US-201 — Stato di progressione del giocatore

- Cosa fatto:
  - scripts/progression.gd, autoload "Progression" (2o dopo GameData in
    project.godot). Tiene pathway_id + sequence (9->0) del giocatore.
    API di sola lettura: pathway(), sequence(), tier(), sequence_data(),
    stat_modifiers(). tier() deriva da sequence_data().tier, non e' calcolato
    qui. Mutazione: configura(pathway_id, seq) per load/creazione/debug/test
    (NON emette il segnale), avanza() decrementa di 1 e si ferma a 0 (emette
    sequence_changed(nuova, vecchia)).
  - FR-1 rispettato: nessun nome di Pathway nel codice. Il Pathway di
    partenza vive in data/balance.json, nuova sezione "progressione"
    {pathway_default, sequenza_iniziale}. _pathway_default() lo legge da
    GameData.get_balance.
  - stat_modifiers della Sequenza applicati allo StatsComponent del
    giocatore come modificatore per id "sequence:<n>" (US-007). avanza()
    rimuove "sequence:<vecchia>" e applica "sequence:<nuova>". Il giocatore
    entra in scena DOPO gli autoload, quindi player.gd _ready() chiama
    Progression.riapplica_al_giocatore() (una riga).
  - SAVE: SaveSystem VERSIONE_CORRENTE 2 -> 3. Campo "progressione"
    {pathway_id, sequence} in salva() e in _leggi_snapshot (riletto con
    _campo(TYPE_DICTIONARY, {}), non fidato). _migra_2_a_3 aggiunge
    {pathway_id:"", sequence:9} a un save di fase 1: la migrazione NON
    conosce nomi di Pathway, pathway_id vuoto -> Progression.da_salvataggio
    ricade sul default dei dati. da_salvataggio e' non fidata: typeof() +
    default, un raw non-oggetto non fa crash.
  - GameState.snapshot()/applica() agganciano Progression
    (per_salvataggio / da_salvataggio).
  - tools/validate_data.py: nuovo check "progressione" — pathway_default
    deve essere un Pathway attivo, sequenza_iniziale 0-9.
- Come verificato: 119 test headless (112 + 7 in test_progression.gd:
  default dai dati, avanza + segnale, stop a 0, stat_modifiers per id
  applicati/rimossi su un giocatore finto, round-trip del save, migrazione
  da schema_version 2, campi non fidati). validator exit 0 (8 warning
  preesistenti). Boot di main.tscn headless: GameData carica, nessuno
  script error, nessun errore Progression.
  Nessuna verifica a schermo: story di sistema senza UI, l'effetto sul
  giocatore e' coperto dal test con giocatore finto in scena.
- Note ambiente: gli "Unreferenced static string" al boot sono il rumore
  di Godot ucciso da `timeout` a meta' shutdown (gia' notato in US-015),
  non del codice.
- Prossima storia: US-202 (primitive shield, aura, dot, decay).

[2026-09-03] US-202 — Primitive del Twilight Giant, parte 1 (shield, aura, dot, decay)

- Cosa fatto:
  - ability_engine.gd: 4 handler nuovi nella tabella _handlers.
  - shield: la riserva vive in StatsComponent (_scudo + aggiungi_scudo,
    scudo(), azzera_scudo(), assorbi_danno(danno, tag)). hurtbox.gd instrada
    il danno da assorbi_danno PRIMA di toccare gli hp. _p_shield:
    assorbimento/durata/riflette/tag_bloccati/bersaglio tutti letti;
    riflette e tag_bloccati REGISTRATI ma non ancora enforced — riflette
    serve chi conosce il mittente, il filtro per tag scatta quando
    hurtbox.subisci portera' il tag_danno (oggi tag "" = danno non
    tipizzato -> lo scudo assorbe comunque). durata: _pending
    {kind:"scudo_expire"} -> azzera_scudo alla scadenza. bersaglio != self
    -> applied:false, come _p_heal.
  - dot: _pending {kind:"dot"} con accumulatore. danno_tick a ogni
    tick_rate per durata. Il numero di tick per chiamata e' cappato a
    min(delta, left): un tick_effects(100) su un dot da 6s non applica
    100 tick.
  - decay: _pending {kind:"decay"}. Il "danno" dichiarato e' il TOTALE,
    spalmato sulla durata (come _p_heal nel tempo, di segno opposto).
    raggio e colpisce_oggetti registrati; la query d'area sulle entita'
    arriva con l'integrazione combat delle abilita'.
  - aura: _pending {kind:"aura"} ancorata al CASTER (non allo stats).
    durata -1 = persistente: non scade mai nel tempo, sopravvive al
    combattimento; rimossa solo quando il caster non e' piu' valido.
    Il tick dell'effetto (taunt, decadimento, autorita_decadimento) e'
    registrato ma NON applicato: manca un sistema di status sulle entita'.
  - tick_effects riscritto: match sul kind, ancora = e["caster"] per le
    aure / e["stats"] per gli altri. flush_effects gestisce scudo_expire
    e salta le aure.
- Come verificato: 128 test headless (119 + 9 in test_primitive_tg1.gd:
  4 primitive nel registro, shield assorbe/scade/bersaglio-non-self,
  dot per tick, decay spalmato sulla durata, aura persistente muore col
  caster, aura a tempo scade, composizione shield+buff_stat via
  execute(tg_armatura_alba) senza warning). validator exit 0. Nessuna
  regressione: le 119 preesistenti restano verdi (hurtbox instradato da
  assorbi_danno resta identita' quando non c'e' scudo).
- Conteggio primitive implementate: 5 -> 9 su 28.
- Aperto: riflessione dello scudo e filtro per tag; il tick delle aure
  (serve un sistema di status); la query d'area di decay/aura.
- Prossima storia: US-203 (debuff_stat, light_purify, transform, terrain_modify).

[2026-09-03] US-203 spezzata in 2a/2b/2c; US-203 (2a) — debuff_stat, light_purify

- DECISIONE: US-203 come scritta era 4 primitive + un file dati nuovo
  (forms.json per transform) + un autoload di stato del mondo + una
  migrazione del save: troppo per una iterazione. Spezzata nel prd.json
  come US-021 a suo tempo:
  * US-203 (2a, QUESTA) = debuff_stat + light_purify. Zero dati nuovi,
    zero modifiche al save.
  * US-203B (2b) = transform + data/forms.json + schema + get_form.
  * US-203C (2c) = terrain_modify + WorldState + save schema_version su.
  Le priorita' delle story successive sono scalate di 2 (US-228 4->6 ecc.).
  Totale story di fase 2: 29 -> 31.

- Cosa fatto (2a):
  - _p_buff_stat e _p_debuff_stat ora chiamano _mod_stat(prim, stats,
    ability_id, tipo, debuff) condiviso. Il segno viene DAL DATO (valore
    gia' negativo nei JSON), non forzato nel codice. id del modificatore:
    "<abilita>:<stat>" per il buff, "<abilita>:debuff:<stat>" per il
    debuff, cosi' un'abilita' che fa entrambi sulla stessa stat non si
    sovrascrive. L'entry _pending del debuff porta "debuff": true.
  - _p_light_purify filtra _pending: toglie le entry kind dot/decay e le
    expire con "debuff": true sullo stesso stats, fino a "potenza"; per i
    debuff chiama remove_modifier cosi' la stat torna su. "per tag" del
    PRD non ha un parametro nel registro (params: raggio, potenza,
    riduce_sequenza) -> la purifica va per NUMERO. raggio e
    riduce_sequenza sono registrati nell'effect dict; la purifica d'area
    su piu' entita' arriva con l'integrazione combat.
- Come verificato: 135 test headless (128 + 7 in test_primitive_tg2.gd:
  registro, debuff moltiplicativo + scadenza, buff e debuff coesistono,
  light_purify toglie un dot / ripristina una stat / e' limitato dalla
  potenza, execute(tg_crepuscolo) compone decay+debuff senza warning).
  validator exit 0. Nessuna regressione.
- Primitive implementate: 9 -> 11 su 28.
- Prossima storia: US-203B (transform + data/forms.json).

[2026-09-03] US-203B (2b) — transform + data/forms.json

- Cosa fatto:
  - data/forms.json (schema_version 1) + data/schema/forms.schema.json.
    Le forme sono DATI: transform legge forma_id da qui, il motore non sa
    quali forme esistano. Due forme, quelle gia' referenziate da
    twilight_giant.json:
    * tg_forma_gigante: hp_max +120, difesa +0.25, velocita -35
    * tg_arma_luce: velocita +45, evasione +0.15, hp_max -40
    PLAUSIBILI non bilanciate. Ogni forma DEVE avere un trade-off
    negativo (il validator lo esige: "una forma non e' solo vantaggi").
  - game_data.gd: PATH_FORMS, _load_single(PATH_FORMS, "forms", ...),
    get_form(forma_id) -> {} se ignoto. 27 file caricati ora (era 26).
  - ability_engine._p_transform: applica gli stat_modifiers della forma
    come modificatore per id "transform:<ability_id>" (US-007), _pending
    {kind:"transform"}. REVERSIBILE in tre modi:
    1. durata > 0 -> scade (ramo transform in tick_effects e nel blocco
       di scadenza).
    2. annulla_transform(ability_id) pubblica -> toglie il modificatore
       e l'entry.
    3. costo_al_secondo > 0 -> drena spiritualita' a ogni tick; a 0 la
       forma cade. Con durata <= 0 l'entry e' "persistente" e vive
       finche' c'e' spiritualita'.
  - tick_effects: aggiunto span = delta per le entry persistenti (prima
    minf(delta, left) con left=0 dava span 0 e non drenava niente).
    flush_effects toglie anche i modificatori transform.
  - Forma ignota -> rec.applied = false + push_error (il validator la
    prende a monte). transform NON e' un debuff: light_purify non lo tocca.
  - validate_data.py: nuovo blocco forme. Ogni forma_id di una primitiva
    transform in data/abilities/ esiste in forms.json; stat_modifiers e'
    un oggetto non vuoto di stat note (hp_max, spiritualita_max, velocita,
    difesa, evasione, forza) con almeno un valore negativo.
- Come verificato: 142 test headless (135 + 7 in
  test_primitive_tg_transform.gd: registro + forme caricate, stat_modifiers
  applicati, reversibile su scadenza / su richiesta / a spiritualita'
  esaurita, forma ignota non applica nulla, execute(tg_crescita) compone
  transform + buff_stat senza warning). validator exit 0. Boot pulito
  (27 file). Nessuna regressione.
- Primitive implementate: 11 -> 12 su 28.
- Prossima storia: US-203C (terrain_modify + stato del mondo nel save).

[2026-09-03] US-203C (2c) — terrain_modify + stato del mondo persistente

- Cosa fatto:
  - Autoload WorldState (scripts/world_state.gd), dopo GameState in
    project.godot. _terrain_mods: Array di {tipo_modifica, posizione:[x,y],
    raggio}. registra_terreno() (le duplicate esatte non si accumulano),
    terreni(), pulisci(). per_salvataggio/da_salvataggio: rilettura NON
    FIDATA - ogni voce validata con typeof(), le malformate scartate una a
    una, raw non-oggetto -> lista vuota, mai crash. Stesso schema di
    Progression (US-201).
  - SAVE: SaveSystem VERSIONE_CORRENTE 3 -> 4. Campo "mondo" in salva() e
    in _leggi_snapshot (_campo TYPE_DICTIONARY). _migra_3_a_4 aggiunge
    {terrain_mods: []}. GameState.snapshot()/applica() agganciano
    WorldState come gia' fanno con Progression.
  - ability_engine._p_terrain_modify:
    * permanente:true -> WorldState.registra_terreno(tipo, posizione del
      caster, raggio). La modifica finisce nel save: un varco aperto resta
      aperto (design-world.md cap. 4).
    * permanente:false + durata > 0 -> _pending {kind:"terrain_temp"}
      ancorata al caster, scade senza effetto. La modifica vera al tilemap
      la applichera' il sistema del mondo di fase 6 leggendo WorldState.
  - tick_effects: l'ancora dell'entry ora e' e["caster"] se c'e', altrimenti
    e["stats"] (prima era gated solo su kind=="aura"; ora vale anche per
    terrain_temp). flush_effects salta le entry senza "stats".
  - validate_data.py: terrain_modify esige tipo_modifica non vuoto e
    permanente bool (permanente decide se la modifica va nel save).
- Come verificato: 149 test headless (142 + 7 in test_terrain_modify.gd:
  registro, permanente nello stato del mondo alla posizione del caster,
  round-trip del save, temporanea in coda che scade, da_salvataggio scarta
  le voci malformate, migrazione v3->v4, execute(tg_spezza_barriera)
  compone terrain_modify + light_purify senza warning). validator exit 0.
  Boot pulito. Nessuna regressione: test_save_system e test_progression
  verdi con la catena di migrazione 1->2->3->4.
- Primitive implementate: 12 -> 13 su 28. US-203 (2a/2b/2c) CHIUSA.
- Prossima storia: US-228 (riscrittura dati di darkness_1 senza probability_shift).

[2026-09-03] US-228 — darkness_1 (Knight of Misfortune) senza probability_shift

- CONTESTO: darkness_1 non aveva un'abilita' (sequenza stub, abilities:[]).
  Il concept "sfortuna cronica" puntava dritto a probability_shift, che e'
  DIFFERITA (serviva a Wheel of Fortune / gruppo key_of_light). US-228:
  scrivere l'abilita' con sole primitive attive.
- Cosa fatto:
  - data/abilities/darkness.json: nuova abilita' darkness_sfortuna_cronica
    (sequence_id darkness_1, costo 40, cd 30). Primitive tutte ATTIVE:
    * curse: effetto "sfortuna", condizione_rimozione "purificazione",
      durata 20s.
    * debuff_stat moltiplicativo su evasione (-0.35) e velocita (-0.2):
      il bersaglio schiva meno e si muove peggio. design-master diceva
      "evasione/precisione" ma precisione NON e' una stat del gioco ->
      velocita (annotato nelle note dell'abilita').
    * dot basso (3 danni ogni 2s per 20s) a tag follia: si logora.
  - data/pathways/darkness.json: darkness_sfortuna_cronica aggiunta a
    darkness_1.abilities. Lo stub resta (US-228 non chiede di
    completare la sequenza; darkness_2 fa gia' lo stesso con
    darkness_cancellazione).
  - curse implementata: _p_curse in ability_engine. Non c'e' un sistema
    di status, quindi la maledizione e' un MARCATORE: modificatore per id
    "curse:<ability_id>" senza delta, scade a tempo (kind expire,
    debuff:true) ed e' tolto da light_purify. effetto e
    condizione_rimozione registrati per un futuro cleanse-per-condizione.
    Il "danno" della sfortuna lo fanno debuff_stat e dot; curse e' il
    contenitore concettuale.
  - probability_shift resta deferred in primitives.json. Il check
    generico del validator (una primitiva differita in un'abilita' ->
    errore) copre gia' il caso: nessuna modifica al validator.
- Come verificato: 153 test headless (149 + 4 in test_darkness_1.gd:
  curse attiva nel registro / probability_shift assente dall'abilita',
  curse marca+scade+purificabile, curse scade da sola,
  execute(darkness_sfortuna_cronica) compone 4 primitive attive senza
  warning). validator exit 0, nessuna abilita' attiva usa una differita.
  Nessuna regressione.
- RISULTATO PERCEPITO: sfortuna cronica su un bersaglio - schiva meno
  (evasione giu'), inciampa (velocita giu'), si logora (dot follia),
  finche' qualcuno non lo purifica. Nessuna manipolazione vera della
  probabilita': quella resta a Wheel of Fortune (differito). Se in fase 5
  il feel non basta -> discussione per riattivare probability_shift col
  suo gruppo.
- Primitive implementate: 13 -> 14 su 28.
- Prossima storia: US-204 (AbilityEngine esegue un'abilita' non posseduta).

[2026-09-03] FIXUP US-228 (feedback utente) — precisione come stat

- L'utente ha chiesto se 'precisione' non fosse una stat da aggiungere
  (mira / colpi critici). Decisione: SI, come stat forward-looking
  speculare a 'evasione' (anche evasione oggi non e' letta da nessun
  sistema di combattimento). Aggiunta a:
  * stats_component.gd STAT_KEYS + _FALLBACK + configure_from_balance.
  * data/balance.json stats_base (precisione: 0) con _comment aggiornato:
    evasione/precisione sono forward-looking, le abilita' le modificano gia'
    ma il combat non le legge.
  * tools/validate_data.py known_stats (per il check delle forme).
  * darkness_sfortuna_cronica: debuff_stat torna su 'precisione' (era
    stato messo 'velocita' come ripiego), note aggiornata.
- Verificato: 159 test verdi (test_stats_component e test_darkness_1
  aggiornati), validator 0.

[2026-09-03] US-204 — AbilityEngine esegue un'abilita' non posseduta (prestito a tempo)

- CONTESTO: execute() prima non aveva NESSUN controllo di ownership -
  eseguiva qualunque abilita' esistente in GameData. Lo stress test
  chiedeva che Error (Sequenza 6) potesse usare abilita' altrui: ora c'e'
  un meccanismo esplicito di PRESTITO, e il gate di ownership per il
  giocatore.
- Cosa fatto (scripts/ability_engine.gd):
  - grant_temporary(ability_id, caster, durata) -> bool (false se l'abilita'
    non esiste). Registra in _granted ("<iid>:<ability_id>" -> istante di
    fine ms, stesso schema di _cooldowns) e accoda _pending
    {kind:"grant_expire", caster, key}.
  - is_granted(caster, id), granted_abilities(caster) -> Array,
    clear_granted().
  - owned_abilities(caster): le abilita' delle Sequenze da 9 alla corrente
    del Pathway (via Progression), ma SOLO se caster e' nel gruppo "player"
    con pathway attivo. Per ogni altro caster -> [] = "ownership
    sconosciuta".
  - _puo_lanciare(caster, id): posseduta OR prestata OR ownership
    sconosciuta. execute() lo chiama subito dopo il check dello stats:
    se false -> ERR_NON_POSSEDUTA (nuova costante), rifiuto gestito.
  - ZERO REGRESSIONI: i ~18 test che chiamano execute() usano caster
    generici (bare Node2D, non nel gruppo "player") -> owned_abilities []
    -> nessun limite, flusso identico a prima.
  - Scadenza: tick_effects ha il ramo grant_expire (-> _granted.erase);
    sweep_cooldowns() ora fa _sweep(index) su _cooldowns E _granted (voci
    scadute o caster liberato). flush_effects pulisce anche _granted.
- Come verificato: 159 test headless (153 + 6 in test_ability_grant.gd:
  registrazione + ispezione, abilita' inesistente -> false, il giocatore
  non lancia un'abilita' non posseduta, il prestito la sblocca / scade via
  tick_effects / torna rifiutata dopo la scadenza, un caster senza Pathway
  non ha restrizioni, il prestito muore col caster). validator exit 0,
  nessuna regressione.
- Prossima storia: US-205 (evocazioni persistenti serializzate).

[2026-09-03] US-205 — Evocazioni persistenti serializzate

- Cosa fatto:
  - Autoload SummonRegistry (scripts/summon_registry.gd), dopo WorldState.
    _summons: Array di {id "sum_N", tipo (= entita_id), comportamento,
    posizione:[x,y], hp}. evoca() restituisce l'id univoco; evocazioni(),
    conta(), rimuovi(id), pulisci(). per_salvataggio/da_salvataggio non
    fidato: un'evocazione senza 'tipo' valido (FONTE PERSA) non viene
    ricreata -> push_warning, nessun crash; raw non-lista -> [].
  - ability_engine._p_summon:
    * durata < 0 -> per "quantita" volte reg.evoca(entita_id, comportamento,
      posizione del caster, hp 10 placeholder). PERSISTENTI: stanno nel
      registro centrale, MAI figlie della scena di combattimento.
    * durata > 0 -> _pending {kind:"summon_temp"} ancorata al caster, che
      scade. entita_id vuoto -> applied:false.
  - Lo slot "evocazioni" del save esiste GIA' da US-015 (schema v2):
    GameState.snapshot()/applica() ora lo riempiono da SummonRegistry
    invece di []. Nessun bump di schema_version necessario.
  - Non ci sono ancora scene di entita' (fase 6): "ricreare" un'evocazione
    al load significa ripopolare il registro. Lo spawn del Node vero lo
    fara' il sistema del mondo/combat leggendo evocazioni().
  - validate_data.py: summon esige entita_id non vuoto e durata != 0
    (-1 persistente o > 0 temporanea).
- Come verificato: 165 test headless (159 + 6 in test_summon.gd: registro,
  persistente nel registro e NON in scena (caster senza figli extra),
  temporanea che scade, round-trip del save, fonte persa non ricreata,
  execute(death_legione) compone summon + aura -> 8 legionari persistenti).
  validator exit 0. Nessuna regressione.
- Prossima storia: US-206 (hook stored_ability_id nel motore).

[2026-09-03] US-206 — Hook stored_ability_id nel motore  -> BLOCCO A CHIUSO

- Cosa fatto (scripts/ability_engine.gd):
  - execute_stored(stored_ability_id, consumer): esegue l'abilita' portata
    da un oggetto. Come se il consumer la possedesse, UNA volta, SENZA
    costo di spiritualita' ne' cooldown, SENZA controllo di ownership
    (l'abilita' e' dell'oggetto, non del consumer).
  - Il "singolo uso" lo garantisce l'inventario di fase 3 consumando
    l'oggetto, NON il motore: una seconda chiamata a execute_stored
    riesce ancora (nessun cooldown, nessun tracking).
  - PERCORSO PARALLELO a execute(), non riusa grant_temporary. Il motore
    delle primitive e' estratto in _esegui_primitive(ability, caster,
    stats, ability_id, result), condiviso da execute() e execute_stored():
    stessa iterazione, stesso dispaccio, stessi TRE esiti (implementata /
    nel registro non scritta / fuori registro).
  - Errori gestiti: ERR_SCONOSCIUTA (abilita' inesistente), ERR_NO_STATS
    (consumer senza StatsComponent). Mai un crash.
  - Nessun sistema inventario in questa story: solo l'API.
- Come verificato: 170 test headless (165 + 5 in test_stored_ability.gd,
  con un'abilita' finta iniettata in GameData._abilities: esegue senza
  costo di spiritualita', non avvia cooldown, ignora l'ownership -
  execute() normale la rifiuterebbe con ERR_NON_POSSEDUTA - abilita'
  ignota / consumer senza stats -> errore gestito). La refattorizzazione
  di execute() NON rompe i 21 test di test_ability_engine. validator 0.
- BLOCCO A (US-201..206 + US-228) CHIUSO. Le tre capacita' del motore
  emerse dallo stress test ci sono tutte: abilita' non posseduta (US-204),
  evocazioni persistenti (US-205), stored_ability_id (US-206).
  Primitive implementate: 15 su 28 (shield, aura, dot, decay, debuff_stat,
  light_purify, transform, terrain_modify, curse, summon + le 5 di fase 1).
- Prossima storia: US-210 (EventTracker generico) - inizio blocco C.

[2026-09-03] US-210 spezzata; US-210 — EventTracker generico (il tracker)

- DECISIONE: US-210 (tracker + count + emettitori nei sistemi di fase 1 +
  save) toccava troppi file. Spezzata:
  * US-210 (QUESTA) = il tracker, i contatori con filtri, il save.
  * US-210B = gli emettitori in ability_engine/enemy/hurtbox.
  Priorita' di US-211+ scalate di 1. Story di fase 2: 31 -> 32.
- Cosa fatto:
  - scripts/event_tracker.gd, autoload dopo Progression. _log grezzo di
    {nome, dati}. emit_event(nome, dati): nome fuori dai 12 di
    tracked_events.json -> push_error, non registrato, nessun crash.
  - count(evento, filtri): filtra il log e aggrega alla lettura. Filtri
    per SUFFISSO: "_max" -> <=, "_min" -> >=, valore booleano true -> il
    campo deve valere true, altrimenti uguaglianza. La misura
    (conteggio/somma/secondi) viene dallo schema: conteggio = quante voci,
    somma/secondi = totale di dati.quantita. Filtro non ammesso per
    quell'evento -> push_error + 0.
  - game_data.gd: carica data/schema/tracked_events.json (28 file ora).
    get_tracked_events(), get_tracked_event(nome).
  - SAVE: SaveSystem VERSIONE_CORRENTE 4 -> 5. Campo "eventi" {log:[]} in
    salva()/_leggi_snapshot/_migra_4_a_5. GameState.snapshot()/applica()
    agganciano EventTracker.
- LIMITI DICHIARATI:
  * il _log cresce senza limite. Come le acting_actions, e' un punto da
    rivedere (US-023 era proprio su questo), ma l'Acting persistente ha
    bisogno della storia degli eventi. Da affrontare se diventa un
    problema di memoria reale.
  * il filtro "stesso_combattimento" (schema di enemy_defeated /
    perfect_parry) serve un concetto di sessione di combattimento che non
    esiste ancora: rimandato. count() oggi lo tratta come uguaglianza su
    dati.stesso_combattimento se presente.
- Come verificato: 178 test headless (170 + 8 in test_event_tracker.gd:
  emit + conteggio, evento fuori vocabolario, filtro di uguaglianza (con
  misura somma), filtro _max, filtro booleano, filtro non ammesso ->
  errore gestito, persistenza nel save, migrazione v4->v5). validator
  exit 0. Nessuna regressione (catena di migrazione 1->5 verde).
- Prossima storia: US-210B (emettitori nei sistemi di fase 1).

[2026-09-03] US-210B — EventTracker: emettitori nei sistemi di fase 1

- Cosa fatto (agganciati, non riscritti, ~1-4 righe a file):
  - player.gd: helper _traccia(evento, dati) che inoltra a EventTracker
    (via Engine.get_main_loop().root, non self, cosi' funziona anche su un
    istanza fuori dall'albero). _su_colpo_inflitto -> damage_dealt
    {quantita}; _su_danno_subito -> damage_taken {quantita};
    _su_parata_riuscita(perfetta=true) -> perfect_parry.
  - enemy.gd _su_morte -> enemy_defeated {} (in fase 1 il nemico e' sempre
    ucciso dal giocatore).
  - ability_engine.execute() su successo -> ability_used {ability_id}.
- LIMITE: i dati degli eventi sono MINIMI. I filtri ricchi delle
  acting_actions (senza_abilita, tipo_arma, sequenza_bersaglio,
  stesso_combattimento) non sono ancora popolati: servono contesto che il
  combat non ha (tipo d'arma, Sequenza del nemico, id della sessione di
  combattimento). US-211 testa l'ActingComponent con eventi costruiti a
  mano; US-219 (vertical slice) rivelera' cosa serve davvero arricchire.
- REGRESSIONE TROVATA DAI TEST: la prima versione del test istanziava
  player.tscn, che finisce nel gruppo "player" e ha fatto fallire
  test_i18n_hud e test_progression (trovavano il player sbagliato). Il
  test ora usa PlayerScript.new() e chiama i gestori (metodi puri)
  direttamente, senza montare la scena.
- Come verificato: 181 test headless (178 + 3 in test_event_emitters.gd:
  ability_used anche filtrato per id, enemy_defeated alla morte (enemy.tscn
  reale), damage_dealt/taken/perfect_parry dai gestori del player).
  validator exit 0. Boot pulito.
- Prossima storia: US-211 (Acting Method - barra acting_progress).

[2026-09-03] US-211 — Acting Method: la barra acting_progress

- Cosa fatto:
  - Autoload Acting (scripts/acting.gd), dopo SummonRegistry. Legge le
    acting_actions della Sequenza corrente da Progression.sequence_data().
  - Contributo di un'azione:
      clampf((count_ora - baseline) / target, 0, 1) * progresso_dichiarato
    baseline = conteggio EventTracker al momento dell'ingresso nella
    Sequenza. _riparti() rileva le baseline: l'Acting riparte da ZERO a
    ogni avanzamento (il log degli eventi NON si azzera, cambia la
    baseline).
  - acting_progress() = clampf(somma contributi - decadimento, 0, 1).
    ripetibile:false -> _latched: raggiunto il target, il contributo resta
    acquisito.
  - DECADIMENTO: Acting ascolta EventTracker.evento_emesso. Se
    nome=="ability_used" E la Sequenza ha un'acting_action con filtro
    senza_abilita:true (si recita "duello puro"), _decadimento +=
    balance.json.acting.decadimento_azione_incoerente (0.05).
  - e_completo(): >= 0.9999 (0.35+0.30+0.35 in virgola mobile non fa
    esattamente 1.0 - trovato dai test).
  - Segnale acting_progress_changed(valore). _riparti() su
    Progression.sequence_changed.
  - SAVE: SaveSystem VERSIONE_CORRENTE 5 -> 6. Campo "acting"
    {decadimento, baseline, latched}, _migra_5_a_6. GameState.applica()
    chiama Acting.da_salvataggio DOPO progressione ed eventi (Acting legge
    la Sequenza e i conteggi).
- LIMITE: gli emettitori di US-210B non popolano ancora i filtri ricchi
  (senza_abilita, tag_danno, tipo_arma). Il test usa eventi costruiti a
  mano; US-219 (vertical slice) chiudera' il cerchio evento->acting.
- Come verificato: 187 test headless (181 + 6 in test_acting.gd: accumulo
  da eventi, frazione parziale (1/3 * 0.35), cap a 1.0, decadimento per
  abilita' incoerente, reset su avanzamento (Seq 8 ha filtri diversi),
  persistenza via GameState.snapshot/applica). validator exit 0. Nessuna
  regressione (catena di migrazione 1->6 verde).
- Prossima storia: US-212 (blocco dell'avanzamento sotto acting_progress 1.0).

[2026-09-03] RIORDINO prd.json — dipendenze reali

- US-212 (blocco avanzamento) cita PotionSystem.concoct+bere (US-209) e
  follia += madness_on_force (US-213), entrambe piu' avanti nell'ordine.
  Anche US-209 (Concoction) dipende da US-207/208 (caratteristiche +
  formule) e US-213. Riordinato il prd.json (solo priority, nessun codice):
  13 US-207  14 US-208  15 US-213  16 US-209  17 US-212  18 US-214
  19 US-215  20 US-216  21 US-217  22 US-218  23 US-219  24 US-220 ...
  Rispetta l'"Ordine di lavoro consigliato" del PRD (C+B in parallelo,
  poi D follia). Blocchi A e C (fino a US-211) restano chiusi.
- Stato: 187 test headless verdi, validator 0. Primitive 15/28. Save
  schema_version arrivato a 6 (progressione, mondo, evocazioni gia'
  c'era, eventi, acting), catena di migrazione 1->6 completa e testata.
- Prossima storia: US-207 (Caratteristica Beyonder come oggetto).

[2026-09-03] ORDINE FINALE del prd.json + 2 story di sistema

- Su richiesta dell'utente: ordine delle story sistemato UNA volta,
  definitivo, non si tocca piu'. 34 story (era 32).
- Aggiunte due story per i sistemi che il PRD IMPLICA e che finora erano
  stati lasciati "registrati ma non applicati":
  * US-218B (priority 22): tag di danno lungo la pipeline hitbox->hurtbox
    ->assorbi_danno/eventi; tiro di schivata/precisione vero
    (evasione - precisione vs cap). Fa funzionare shield.tag_bloccati,
    i buff/debuff su evasione (fool_velo_illusorio, darkness_sfortuna),
    i filtri tag_danno delle acting_actions.
  * US-218C (priority 23): data/status_effects.json + schema + validator;
    StatsComponent.applica_status/rimuovi_status_per_tag; aura/curse
    applicano lo status 'effetto', light_purify rimuove per TAG (come
    dice il PRD); Area2D field per aura/decay che applica nel raggio.
- NUOVA REGOLA DI LAVORO (utente): quando una story richiede un sistema
  di cui il PRD parla, si COSTRUISCE dentro quella story. Niente piu'
  "documentato come non applicato". Gli stub gia' lasciati (aura tick,
  light_purify d'area, tag di danno) sono coperti da US-218B/C.
- Ordine da qui: 13 US-207, 14 US-208, 15 US-213, 16 US-209, 17 US-212,
  18 US-214, 19 US-215, 20 US-216, 21 US-217, 22 US-218B, 23 US-218C,
  24 US-218, 25 US-219, 26 US-220 ... 34 US-229.
- Prossima storia: US-207 (Caratteristica Beyonder come oggetto).

[2026-09-03] US-207 — Caratteristica Beyonder come oggetto

- Cosa fatto:
  - data/characteristics.json: 10 Caratteristiche del Twilight Giant, una
    per Sequenza 9..0 (char_twilight_giant_N). + schema.
  - game_data.gd: carica characteristics.json; get_characteristic(id),
    characteristic_for(pathway_id, sequence).
  - Autoload CharacteristicStore (scripts/characteristic_store.gd), dopo
    SummonRegistry. _held: lista di id CON ripetizioni. aggiungi, consuma
    (una occorrenza), possiede, conta(id?), posseduti, pulisci.
    per_salvataggio/da_salvataggio non fidato (solo stringhe non vuote).
  - SAVE: SaveSystem VERSIONE_CORRENTE 6 -> 7. Campo "caratteristiche"
    (lista di id), _migra_6_a_7. GameState.snapshot/applica agganciano.
  - DROP: balance.json.nemico_base.caratteristica {pathway_id, sequence,
    probabilita}. Il nemico da banco lascia char_twilight_giant_9
    GARANTITA (1.0). enemy.gd _lascia_caratteristica(): tiro vs
    probabilita, GameData.characteristic_for, spawn di
    scripts/characteristic_pickup.gd (Area2D a runtime come
    projectile/melee_arc, rombo chiaro segnaposto). Raccolta su
    body/area_entered del giocatore o via raccogli() pubblica ->
    CharacteristicStore.aggiungi + queue_free; niente doppia raccolta.
  - validate_data.py: id unici, pathway_id attivo, sequence 0-9,
    name_i18n; nemico_base.caratteristica risolve; ogni
    potion.characteristic_sequence non-stub del TG ha la sua Caratteristica.
- Come verificato: 193 test headless (187 + 6 in test_characteristic.gd).
  validator exit 0.
- VERIFICA A SCHERMO (finestra reale): piazzata una Caratteristica davanti
  al giocatore, spinto sopra -> store conta 0 -> 1,
  possiede('char_twilight_giant_9') = true, il pickup sparisce. La
  collisione Area2D funziona.
- Prossima storia: US-208 (formule delle pozioni e formule parziali).

[2026-09-03] US-208 — Formule delle pozioni e formule parziali

- Cosa fatto:
  - data/potions/formulas.json + data/schema/potion_formula.schema.json.
    Generato da TUTTE le Sequenze non-stub (script una tantum): 11 formule
    (twilight_giant 9..0 + fool_9), 30 ingredienti.
  - Struttura: 'ingredients' = vocabolario chiuso degli id ingrediente;
    'formulas' = { formula_id: {characteristic_sequence, ingredients[],
    soglia_parziale, penalita_parziale} }.
  - soglia_parziale = len(ingredients) - 1: con 3 ingredienti se ne
    possono usare 2 (pozione parziale), sotto no.
  - penalita_parziale ESPLICITA nei dati {follia_extra: 2.0,
    qualita: "instabile"}: mai un valore silenzioso. US-209 la applica.
  - game_data.gd: _load_single(formulas), get_formula(id),
    ingredient_ids(). 30 file caricati ora.
  - Nessuna logica runtime (concoct e' US-209).
  - validate_data.py: ogni potion.formula_id non-stub risolve; ogni
    ingrediente e' nel vocabolario; formula.characteristic_sequence ==
    numero della Sequenza; ingredienti inline == quelli della formula
    (la formula e' la fonte); soglia_parziale in [1, len];
    penalita_parziale non vuota.
- Come verificato: 198 test headless (193 + 5 in test_potion_formulas.gd:
  formule caricate, formula inesistente -> {}, vocabolario copre le
  formule, soglia parziale coerente, ogni Sequenza del TG ha la sua
  formula). validator exit 0. Nessuna regressione.
- BUG PRESO DAL VALIDATOR: la prima generazione copriva solo il
  twilight_giant; fool_9 (Sequenza non-stub da un vecchio audit) puntava
  a formula_fool_9 inesistente. Rigenerato scandendo tutti i pathway.
- Prossima storia: US-213 (Follia - statistica cumulativa con effetti a soglia).

[2026-09-03] US-213 — Follia: statistica cumulativa con effetti a soglia

- Cosa fatto:
  - Autoload Madness (scripts/madness.gd), dopo CharacteristicStore.
    _valore 0-100, _log delle sorgenti. add(quantita, sorgente) clampa
    0-100 e registra; riduci(q, sorgente) = add(-q) (il CAP frazionario
    delle Ancore, riduzione_max_ancore 0.60, lo applichera' AnchorSystem
    in US-216 passando la quantita' gia' cappata).
  - Soglie da balance.json.madness (fallback interni se manca):
    15 distorsioni, 40 abilita' autonome, 70 perdita input, 100 mostro.
    soglia_corrente() -> il VALORE della soglia raggiunta (0/15/40/70/100).
    Segnale madness_changed(valore, soglia_attraversata) - soglia_attraversata
    = -1 se non si e' cambiato banda.
  - Abilita' autonome: _process, sopra soglia 40, ogni ~1s un tiro
    tenta_abilita_autonoma() (pubblico per i test) con probabilita'
    probabilita_attivazione_autonoma_sotto_soglia (0.08); sul colpo emette
    abilita_autonoma(). Chi la casta e' compito di chi ascolta (US-215 o
    l'integrazione).
  - decadimento_naturale_al_minuto: rispettato in _process (oggi 0 -> no-op).
  - perdita_input_attiva() (>= 70) e stato_mostro() (>= 100) come query;
    l'effetto vero (input intermittente, game over) lo applica chi le legge.
  - SAVE: SaveSystem VERSIONE_CORRENTE 7 -> 8. Campo "follia"
    {valore, log}, _migra_7_a_8. GameState.snapshot/applica agganciano.
- Come verificato: 204 test headless (198 + 6 in test_madness.gd: add +
  log sorgente, clamp 0-100, soglie + segnale con la soglia attraversata,
  abilita' autonoma solo sopra 40 (0 tiri sotto, qualcuno su 2000 sopra),
  riduci non va sotto 0, persistenza + migrazione v7->v8). validator 0.
  Nessuna regressione.
- Prossima storia: US-209 (Concoction - preparare e bere la pozione).

[2026-09-03] US-209 — Concoction: preparare e bere la pozione

- Due autoload nuovi:
  - Foundation (scripts/foundation.gd, dopo Madness): _valore da
    balance.json.foundation.iniziale (50). applica(delta, sorgente) clamp
    [minimo, 100]. costante(nome). moltiplicatore_follia() =
    lerp(2.5, 1.0, valore/100): 1.0 a fondamenta piene, sale a 2.5 a zero.
    balance.json.foundation: nuovo malus_digestione_difficile -10.
  - PotionSystem (scripts/potion_system.gd, dopo Acting):
    * concoct(formula_id, characteristic_id, ingredienti[]): valida
      formula, coerenza Caratteristica (sequence == characteristic_sequence
      della formula) e possesso (CharacteristicStore); conta gli
      ingredienti RICHIESTI presenti fra quelli passati (distinti), sotto
      soglia_parziale -> ingredienti_insufficienti (Caratteristica NON
      consumata); parziale se < completo; consuma la Caratteristica e
      tiene _pozione {formula_id, da_sequenza, parziale, penalita}.
      Gli ingredienti sono un PARAMETRO, non un inventario: la raccolta
      ingredienti e' fase 3 (Mother), non-goal di fase 2.
    * bevi(forza=false): pozione per un'altra Sequenza -> rifiuto.
      Acting.e_completo() -> avanza (Progression.avanza -> stat_modifiers
      riapplicati, sequence_changed, Acting._riparti) + Foundation
      +bonus_recitazione_completa; parziale -> Madness.add(follia_extra *
      moltiplicatore_follia). Acting incompleto, forza=false -> DIGERITA
      MALE: Foundation -malus_digestione_difficile, Madness.add(
      madness_on_force * mult), nessun avanzamento, pozione sprecata.
      forza=true (gancio per US-212): avanza comunque + Foundation
      malus_avanzamento_forzato (-25) + follia pesante.
- SAVE: SaveSystem VERSIONE_CORRENTE 8 -> 9. Campo "fondamenta" {valore},
  _migra_8_a_9. GameState.snapshot/applica agganciano Foundation.
- Come verificato: 214 test headless (204 + 10 in test_concoction.gd:
  concoct completa/parziale/sotto-soglia/caratteristica incoerente/non
  posseduta; bevi con acting completo avanza + reset acting + bonus
  fondamenta; acting incompleto digerita male (fondamenta -10, follia su,
  resta a Seq 9); forzato (fondamenta -25); pozione parziale penalizza la
  follia; pozione per un'altra Sequenza rifiutata). validator 0.
- VERIFICA A SCHERMO rimandata: non c'e' UI di pozioni finche' non arriva
  la HUD di US-212 e la UI a libro di US-221. Il loop
  Caratteristica -> concoct -> acting -> bevi -> avanzamento e' coperto
  end-to-end dai test headless (e sara' il cuore di US-219).
- Prossima storia: US-212 (blocco dell'avanzamento sotto acting_progress 1.0).

[2026-09-03] US-212 — Blocco dell'avanzamento sotto acting_progress 1.0

- Il grosso della meccanica era gia' in US-209. US-212 aggiunge:
  - PotionSystem.avanzamento_disponibile() (pozione per la Sequenza
    corrente + Acting.e_completo) / avanzamento_forzabile() (pozione ma
    acting < 1.0).
  - HUD: hud.tscn riga "Recitazione" (barra 0-1) + label "Avanzamento".
    hud.gd si aggancia a Acting.acting_progress_changed e a
    PotionSystem.pozione_creata/bevuta; label nascosta senza pozione,
    "Avanzamento pronto" con acting completo, "Avanzamento solo forzando
    (fondamenta -25)" altrimenti.
  - i18n: 3 chiavi nuove in strings.csv, .translation rigenerati (--import).
- BUG TROVATO DALLA VERIFICA A SCHERMO: Acting emetteva
  acting_progress_changed solo su _riparti/decadimento, NON all'arrivo di
  un evento tracciato -> la HUD non si aggiornava mentre si recitava.
  Corretto: _su_evento riemette il segnale per ogni evento che una
  acting_action della Sequenza conta.
- Come verificato: 221 test headless (214 + 7 in test_advancement_gate.gd:
  stato senza pozione, solo-forzabile, disponibile, bere senza recitazione
  non avanza, floor fondamenta a 0 forzando 5 volte (9->4), moltiplicatore
  follia 1.0/1.75/2.5). validator 0.
- VERIFICA A SCHERMO (finestra reale): label nascosta / "solo forzando"
  con acting 0 / "Avanzamento pronto" + barra recitazione a 1.0 completa.
- Prossima storia: US-214 (Layer audio della follia).

[2026-09-03] US-214 — Layer audio della follia

- audio_manager.gd: carica audio.json.madness_layer, si aggancia a
  Madness.madness_changed -> aggiorna_follia(valore).
  - _whisper: AudioStreamPlayer sul bus whisper. _soglia_follia_attiva(v)
    = la soglia piu' alta con madness_min <= v. Il volume_db del bus
    whisper e' quello della soglia (GUIDATO dalla follia, non fisso), il
    detune diventa pitch_scale, il loop e' un wash sintetizzato per soglia
    (amb_whisper_faint/words/names/chorus). Sotto la prima soglia -> bus
    muto + whisper.stop().
  - SOGLIA 80: duck_music_db -20 abbassa il bus music (base memorizzato in
    _ready, ripristinato scendendo sotto 80).
  - disattiva_sussurri (imposta_accessibilita) -> set_bus_mute(whisper)
    SENZA toccare Madness; imposta_accessibilita riapplica al cambio.
  - ONE-SHOT: _process, sopra one_shot_casuali.madness_min (25), timer
    random in intervallo_secondi, poi tenta_one_shot() (pubblico) suona un
    sfx casuale da one_shot_casuali.sfx. disattiva_sussurri li sopprime.
  - SOGLIA 55: imposta_nomi_sussurro(nomi)/nomi_sussurro() - gancio per
    NPC incontrati e Ancore (AnchorSystem US-216), fallback generico se
    vuoto.
  - 9 sfx nuovi in SFX_SPEC (4 one-shot + 4 wash), tutti sintetizzati
    (US-019, niente file).
- Come verificato: 227 test headless (221 + 6 in test_madness_audio.gd:
  whisper muto sotto la prima soglia, volume guidato dalla follia (-24 a
  35, -10 a 85), duck musica a 80 + ripristino, disattiva_sussurri
  silenzia e resta muto, one-shot solo sopra 25 e soppressi da
  disattiva_sussurri, nomi con fallback). validator 0.
- Verifica all'orecchio non possibile (audio sintetizzato). Lo stato dei
  bus (mute, volume_db, duck) e' coperto dai test.
- Prossima storia: US-215 (Effetti visivi della follia).

[2026-09-03] US-214B — Pulizia di default del cadavere del nemico

- BUCO SEGNALATO DALL'UTENTE IN PLAYTEST: alla morte enemy.gd (US-011)
  disattiva collision/hitbox/hurtbox, riproduce "death" ed emette
  morto(self) ma NON si libera - e nessuno ascolta 'morto', quindi il
  cadavere restava a schermo congelato sul frame 5, identico a un nemico
  vivo.
- Cosa fatto: balance.json.nemico_base.cadavere {permanenza_s: 3.0,
  dissolvenza_s: 1.0}. enemy._avvia_dissolvenza_cadavere(): dopo
  morto.emit, se permanenza_s > 0 -> SceneTreeTimer(permanenza) -> tween
  di modulate:a a 0 su dissolvenza_s -> queue_free. permanenza_s 0 = il
  cadavere resta (lo gestira' uno spawner/encounter di fase 6). Il
  segnale 'morto' NON e' toccato: chi lo vuole lo riceve prima della
  dissolvenza.
- Come verificato: 230 test headless (227 + 3 in test_corpse.gd:
  permanenza 0 -> resta MORTO, permanenza breve -> liberato dopo
  permanenza+dissolvenza, segnale morto emesso comunque). validator 0.
- VERIFICA A SCHERMO (finestra reale): cadavere a alpha 1.0 a t=0,
  0.60 a t=3.4 (0.4s dentro la dissolvenza), sparito a t=4.4.
- NOTA per l'utente sui numeri/mappa: i numeri 0-5 sugli sprite sono
  l'indice del frame di animazione disegnato da generate_placeholders.py
  (aiuto di debug, spariscono con l'arte vera); i bordi colorati =
  hitbox/tell/iframe/parata. La mappa e' un banco di prova a placeholder
  (test_zone.gd, 40x30 tile 32x32): il mondo disegnato e' fase 6.
- Prossima storia: US-215 (Effetti visivi della follia).

[2026-09-03] US-215 — Effetti visivi della follia

- scenes/madness_overlay.tscn (CanvasLayer layer 90, FUORI dal libro) +
  scripts/madness_overlay.gd + shaders/madness_vignette.gdshader. In
  main.tscn e debug_scene.tscn. Si aggancia a Madness.madness_changed.
  Soglie da balance.json.madness (15/40/70); parametri dalla nuova sezione
  balance.json.madness_vfx (nessun numero nello script).
  - VIGNETTA (soglia 15): ColorRect a schermo intero con lo shader
    madness_vignette (scurisce i bordi). intensita cresce con la follia;
    respiro sinusoidale SE non riduci_flash.
  - DISTORSIONE (soglia 40): scatti brevi (ColorRect magenta) a intervalli
    random; disattivati da riduci_distorsione.
  - NERO DI SCENA (soglia 70): si attiva DA SOLO a intervalli random,
    alpha nero_alpha per nero_durata_s. Con riduci_flash -> STATICO a
    nero_alpha_statico 0.3, MAI assente (FR-8).
  - audio.json.accessibilita: aggiunti riduci_flash e riduci_distorsione
    (default false), letti via AudioManager.accessibilita.
  - Specchio visivo del layer audio (US-214), stesso valore di follia:
    nessuna info vive solo nel visivo o solo nell'audio (FR-7/8).
- Come verificato: 235 test headless (230 + 5 in test_madness_visual.gd:
  vignetta solo sopra 15, cresce con la follia, nero solo sopra 70,
  riduci_flash -> nero statico costante mai assente, distorsione visibile).
  validator 0.
- VERIFICA A SCHERMO (finestra reale): a follia 20 i bordi sono
  leggermente scuriti (intensita 0.22); a follia 80 col nero attivo lo
  schermo e' quasi tutto nero (0.85), i blob appena visibili.
- Prossima storia: US-216 (Ancore - riduzione della follia, distruttibilita').

[2026-09-03] US-216 — Ancore: registrazione, buffering della follia, distruttibilita'

- data/anchors.json (4 Ancore: mirco/sidon = NPC, dimora = luogo,
  promessa) + schema. Ogni Ancora: id, name_i18n, forza (punti di follia
  bufferizzabili), penalita (follia se distrutta). GameData: get_anchors()
  / get_anchor(id). 31 file caricati.
- Autoload AnchorSystem (scripts/anchor_system.gd), ULTIMO (dopo
  AudioManager). register(id) / destroy(id) / active() / forza_totale() /
  assorbi(quantita) = min(forza_totale, riduzione_max_ancore * quantita).
- BUFFERING: Madness.add(q, sorgente, tramite_ancore=true) ora consulta
  AnchorSystem.assorbi(q) e aggiunge solo q - assorbito. Il colpo da
  destroy() passa tramite_ancore=false -> NON bufferizzato ("la sua
  perdita e' un colpo"). riduzione_max_ancore (0.60) e' il CAP frazionario
  sul singolo colpo, non un reset.
- US-214 HOOK CHIUSO: AnchorSystem._aggiorna_nomi_sussurro() su
  register/destroy/load -> AudioManager.imposta_nomi_sussurro(name_i18n
  delle Ancore attive). I sussurri di soglia 55 pronunciano i nomi delle
  Ancore.
- SAVE: SaveSystem VERSIONE_CORRENTE 9 -> 10. Campo "ancore" (lista di id),
  _migra_9_a_10. GameState.snapshot/applica.
- validate_data.py: id unici, name_i18n, forza > 0, penalita > 0, almeno
  un'Ancora (il rituale di Seq 1 del TG sacrifica 'ancora_del_giocatore').
- Come verificato: 242 test headless (235 + 7 in test_anchors.gd: dati
  caricati, register/active, buffering (Ancora forza 10 assorbe 10 di un
  colpo da 20), cap frazionario (mai piu' del 60% di un colpo), destroy ->
  segnale + follia non bufferizzata, persistenza, nomi sussurri seguono le
  Ancore). validator 0. Nessuna regressione (test_madness verde: senza
  Ancore il buffering e' no-op).
- Prossima storia: US-217 (Motore del rituale di avanzamento).

[2026-09-03] US-217 — Motore del rituale di avanzamento

- Autoload RitualSystem (scripts/ritual_system.gd), ultimo. Legge
  advancement_ritual della Sequenza (formato gia' nei dati: location_tags,
  momento, fase_lunare, sacrifices, sigils).
  - Il mondo di fase 6 non c'e': imposta_luogo(tags) / imposta_tempo(
    momento, fase_lunare) simulano; aggiungi_sigillo(id) e
    fornisci_sacrificio(id) sono i ganci per i sistemi futuri (strutture
    fase 3, boss fase 5); 'ancora_del_giocatore' passa da AnchorSystem.
  - prerequisiti(rituale) -> {ok, mancanti[]}: elenca ogni tag/momento/
    fase/sigillo/sacrificio mancante col prefisso.
  - avvia(rituale): richiede prerequisiti E Acting.e_completo (come
    l'avanzamento per pozione). Build = audio.json.music.rituale.
    durata_build_secondi (45). _process conta alla rovescia -> _completa.
  - interrompi(motivo): rituale abortito. Madness.add(balance.json.rituale.
    penalita_follia_interruzione 15, NON bufferizzata) + Foundation
    malus_fondamenta_interruzione (-8) + AudioManager.silenzio_secco(1.2)
    (muta music/ambience/whisper per 1.2s, poi ripristina e ri-sincronizza
    il whisper col valore di follia).
  - _completa: consuma i sacrifici modellati (AnchorSystem.destroy sulla
    prima Ancora attiva per 'ancora_del_giocatore'), Progression.avanza(),
    Foundation +bonus_recitazione_completa.
- AudioManager: silenzio_secco(durata_s) / silenzio_secco_attivo() nuovi.
- SAVE: SaveSystem VERSIONE_CORRENTE 10 -> 11. Campo "rituale"
  {sigilli, sacrifici_forniti}, _migra_10_a_11. GameState.snapshot/applica.
- Come verificato: 248 test headless (242 + 6 in test_ritual.gd:
  prerequisiti dal rituale reale di Seq 4 (mancanti elencati, poi tutti
  soddisfatti), avvio richiede prereq + recitazione, completamento avanza
  di Sequenza, interruzione fallisce con penalita' follia+fondamenta,
  sacrificio dell'Ancora consumato, persistenza sigilli/sacrifici).
  validator 0. Catena di migrazione 1->11 verde.
- Verifica a schermo rimandata: nessuna UI di rituale finche' non c'e' la
  UI a libro / il mondo di fase 6. Il motore e' coperto dai test.
- Prossima storia: US-218B (integrazione combat: tag di danno, schivata/precisione).

[2026-09-03] US-218B — Integrazione combat: tag di danno, tiro di schivata e precisione

- TAG DI DANNO nella pipeline:
  - hitbox.attiva(danno, stagger, origine, tag_danno='') porta il tag;
    _physics_process passa {tag_danno} come 4o arg (dict) a
    hurtbox.subisci e ha_colpito(bersaglio, danno, tag) lo espone.
  - hurtbox.subisci(danno, stagger, da, extra={}) retrocompatibile:
    legge extra.tag_danno, lo passa a stats.assorbi_danno(danno, tag)
    (lo scudo con tag_bloccati ora blocca DAVVERO solo i tag elencati) e
    a colpito(danno, stagger, da, tag).
  - player.gd: balance.json.combattimento.attacco_leggero.tag_danno =
    'fisico'; _su_colpo_inflitto/_su_danno_subito ricevono il tag e lo
    mettono negli eventi damage_dealt/damage_taken -> le acting_actions
    con filtro tag_danno si riempiono da combattimento reale.
  - enemy.gd: attacco 'fisico'.
- TIRO DI SCHIVATA: prima del danno, prob = clamp(evasione_bersaglio -
  precisione_attaccante, 0, balance.json.combattimento.cap_schivata 0.85).
  Colpo mancato -> danno 0 + segnale schivato(da) + return (niente
  damage_taken). RNG per-hurtbox seedabile (set_rng) per replay/test
  deterministici. evasione e precisione (stat forward-looking di
  US-024/228) ora contano davvero.
- Segnali colpito e ha_colpito estesi (+tag): aggiornati i callable in
  dummy.gd e test_combat.gd. Le lambda a meno argomenti restano valide
  in Godot 4.
- Come verificato: 252 test headless (248 + 4 in test_combat_integration.gd:
  scudo blocca solo i tag elencati (fisico si', luce no), alta evasione
  (0.6) -> molti colpi mancati su 200, precisione 0.9 vs evasione 0.5 ->
  prob 0 e nessuna schivata, damage_dealt emesso col tag e filtrabile).
  validator 0.
- TRAPPOLA RITROVATA (gia' in US-009): le lambda GDScript catturano le
  locali PER VALORE. Il test raccoglieva le schivate in un int -> restava
  0. Corretto con un Dictionary (tipo riferimento).
- Prossima storia: US-218C (sistema di status + campi d'area per aura/decay).

[2026-09-03] US-218C — Sistema di status sulle entita' + campi d'area

- data/status_effects.json (5 status: sfortuna, taunt, decadimento,
  autorita_decadimento, comando_non_morti) + schema. Ogni status: tag (dal
  vocabolario), durata_default (>0 o -1), negativo (bool), stat_modifiers
  opzionali. GameData: get_status_effect(id). 32 file.
- StatsComponent: _statuses {id: {tag, left}}. applica_status(id, durata)
  (durata < 0 = durata_default; stat_modifiers -> modificatore 'status:<id>'
  via US-007), rimuovi_status, rimuovi_status_per_tag(tag), ha_status,
  status_attivi. _process scade da solo gli status a tempo.
- ability_engine:
  - _p_curse: la maledizione e' ora uno STATUS nominato, non un marcatore
    vuoto (prima era un modificatore per id senza delta + _pending). Lo
    scade StatsComponent, lo toglie light_purify per tag.
  - _p_aura: applica lo status 'effetto' + con raggio > 0 e caster in scena
    genera un field.
  - _p_decay: con raggio > 0 e caster in scena genera un field d'area;
    altrimenti fallback sulla coda _pending (danno spalmato sul caster).
  - _p_light_purify: oltre a dot/decay/debuff pending, rimuove gli status
    negativo:true PER TAG (torna 'per tag' come dice il PRD), entro potenza.
- scripts/field.gd: Area2D figlia del caster, collision_mask 4 (le hurtbox
  sono sul layer 3). A ogni tick_rate trova le hurtbox nel raggio e applica
  lo status (aura) o il danno (decay). Si autodistrugge a durata; durata -1
  finche' il caster e' valido.
- validate_data.py: ogni status ben formato (tag noto, durata, stat note);
  ogni effetto di aura/curse in data/abilities esiste in status_effects.json.
- Come verificato: 259 test headless (252 + 7 in test_status_effects.gd:
  status caricati, applica/rimuovi, stat_modifiers dello status, scadenza
  via _process, rimuovi_per_tag, light_purify toglie i negativi per tag
  lasciando i non-negativi, campo aura applica lo status a un bersaglio nel
  raggio e NON a uno fuori - overlap Area2D verificato headless).
  test_darkness_1 e test_primitive_tg1 aggiornati (curse -> ha_status;
  decay con raggio 0 -> coda). validator 0.
- CHIUSI i debiti dichiarati in US-202: il tick delle aure, la query d'area
  di decay/aura, "light_purify per tag". Restano non wired: aura 'taunt'
  che fa targetare i nemici (serve AI), 'comando_non_morti' (serve summon
  controllabili).
- Prossima storia: US-218 (Percezione per Sequenza).

[2026-09-03] US-218 — Percezione per Sequenza

- Autoload PerceptionSystem (scripts/perception_system.gd), ultimo. Legge
  audio.json.sequence_perception (voci a 9/7/5/3/1).
  _voce_per_sequenza(seq) = la voce con la chiave numerica piu' PICCOLA
  >= seq (a Seq 6 vale quella di 7: hai gia' superato la 7); la piu'
  bassa se seq < 1. Nessun numero di Sequenza nel codice. Si aggancia a
  Progression.sequence_changed. layers_attivi/reveal_attivi/reveal_attivo.
  Cumulativa: scendendo di Sequenza i reveal aumentano e sono un superset.
- AUDIO: AudioManager.imposta_layers_ambientali(layers) - un
  AudioStreamPlayer per layer sul bus ambience, acceso/spento in base
  alla lista corrente. 4 amb_* sintetizzati nuovi.
- VISIVO (FR-7 - ogni reveal ha audio E visivo):
  scenes/perception_overlay.tscn (CanvasLayer layer 80) +
  perception_overlay.gd. spiriti_vicini -> un Control che disegna
  cerchio+anello sulle posizioni SCHERMO dei nodi del gruppo 'nemici'
  (anche oltre i muri: e' un overlay). densita_mistica -> ColorRect grana
  tenue. In main.tscn e debug_scene.tscn.
- Come verificato: 264 test headless (259 + 5 in test_perception.gd:
  Seq 9 niente, Seq 7 primo reveal + layer, Seq intermedia usa la voce
  verso l'alto (6->7, 4->5), cumulativita' 7<5<3 e superset, i layer
  audio si accendono/spengono). validator 0.
- VERIFICA A SCHERMO (finestra reale): a Seq 5 anello blu sul nemico
  (spiriti_vicini) + velo di grana (densita_mistica); a Seq 9 overlay
  spento.
- Prossima storia: US-219 (Twilight Giant eseguito end-to-end - IL VERDETTO
  SULL'ARCHITETTURA).

[2026-09-03] US-219 — Twilight Giant end-to-end.  VERDETTO: L'ARCHITETTURA REGGE.

- Il Twilight Giant va dalla Sequenza 9 alla 5 con ZERO codice dedicato.
  Nessun `if` per il Pathway, nessun nome di Sequenza in scripts/.
- tests/test_vertical_slice.gd, 3 test (267 totali):
  1. test_twilight_giant_dalla_sequenza_9_alla_5 - per ogni salto
     9->8->7->6->5:
     * CharacteristicStore.aggiungi("char_twilight_giant_N")
     * PotionSystem.concoct("formula_twilight_giant_N", cid, ingredienti
       dalla formula)
     * RECITAZIONE riempita LEGGENDO le acting_actions della Sequenza
       corrente da Progression.sequence_data(). L'helper
       _recita_sequenza_corrente() per ogni azione emette 'target' eventi
       (misura conteggio) oppure un evento con quantita=target (somma/
       secondi), coi FILTRI presi COME DATI. Nessun numero del TG nel test.
     * Acting.e_completo() -> PotionSystem.bevi(false) -> Progression avanza
     * i stat_modifiers di ogni Sequenza (hp_max 20/35/50/80/130, forza,
       difesa, danno_arma...) applicati al giocatore finto e verificati
       contro get_base + delta
     * niente follia da forzatura: la recitazione e' completa, quindi
       l'avanzamento e' NORMALE (e["forzato"] == false)
  2. test_ogni_abilita_del_twilight_giant_9_5_si_esegue_senza_warning:
     le 11 abilita' delle Sequenze 9-5 (primitive usate: melee_arc,
     buff_stat, dash, projectile, shield, transform, debuff_stat, aura)
     eseguono TUTTE senza warning "non implementata" ne' "fuori registro".
  3. test_nessun_codice_nomina_il_twilight_giant: legge ogni scripts/*.gd,
     toglie i commenti, verifica ZERO occorrenze di "twilight_giant",
     "Warrior"/"Pugilist" (nomi di Sequenza), "\"tg_". La CI gia' esegue
     run_tests.gd -> il check e' automatico.
- Il commento di ritual_system.gd che citava "twilight_giant.json" e'
  stato genericizzato in "data/pathways/".
- CONCLUSIONE: niente ha richiesto codice dedicato al Twilight Giant. La
  spina dorsale data-driven regge. Le fasi 3-7 sono in gran parte lavoro
  di dati. Il checkpoint di fase 5 (aggiungere i 9 Pathway rimanenti senza
  codice) e' realistico. Blocco H CHIUSO.
- Debiti residui NON bloccanti per il criterio di uscita (documentati
  nelle rispettive story): aura 'taunt' che fa targetare i nemici (serve
  AI, fase 5), 'comando_non_morti' (serve summon controllabili), i filtri
  acting che dipendono dal mondo (area_cleared reale, npc_influenced,
  time_in_state 'trasformato', tipo_arma) - emessi a mano nel test, si
  popoleranno da soli quando quei sistemi esisteranno.
- Prossima storia: US-220 (i18n reale) - inizio blocco I.

[2026-09-03] US-220 — i18n reale (R-12): cataloghi delle stringhe dei dati

- DUE sistemi i18n, ora distinti e documentati (CLAUDE.md nuova sez. "i18n"):
  1. UI chrome (HUD): assets/i18n/strings.csv -> .translation, tr("HUD_..").
     Invariato.
  2. Stringhe DEI DATI: data/i18n/it.json + en.json, GameData.tr_data(key).
- CONVENZIONE UNICA delle chiavi dei dati (prima ne convivevano tre):
  <categoria>[.<pathway_id>].<local>
  - il segmento pathway_id c'e' per sequence/ability/acting/characteristic/
    form; assente per anchor/synergy e per 'pathway' stesso
  - local = id verbatim (anchor/synergy meno il prefisso di tipo), numero per
    sequence/characteristic
  Le uniche divergenti erano acting.* (mancava il pathway: acting.tg_9_x ->
  acting.twilight_giant.tg_9_x, 31 chiavi) e 3 abilita' del Fool che avevano
  perso il prefisso id (ability.fool.divinazione_pendolo ->
  ability.fool.fool_divinazione_pendolo). Riscritte dal tool nei file di dati.
- tools/generate_i18n_stubs.py (nuovo): scandisce data/ (esclusi schema/,
  i18n/, pathways_deferred/), riscrive le chiavi non canoniche sul posto,
  rigenera i due cataloghi. Chiavi nuove: seed dal campo gemello in chiaro
  (name/descrizione) se c'e', altrimenti stub "TODO <chiave>". Preserva le
  traduzioni autoriali. --check per la CI/idempotenza (exit 1 se serve un run).
  Risultato: it.json 195 chiavi, 141 reali (tutte le acting_actions + nomi di
  Pathway/Sequenza dai campi in chiaro), 54 stub (abilita'/caratteristiche/
  ancore/forme: non hanno testo in chiaro nei dati, traduzione da scrivere).
  en.json: {} - incompleto per scelta, tr_data ricade su it.
- GameData: _load_flat() per i cataloghi piatti {chiave: stringa};
  tr_data(key, locale="") con fallback en->it->chiave (mai vuoto, mai crash);
  has_translation(key) (false sugli stub "TODO "); i18n_keys().
- validate_data.py: nuovo blocco R-12. Cammina data/ (stesso skip del tool),
  raccoglie ogni *_i18n dei dati attivi, ERRORE se una chiave non ha voce in
  it.json (col comando da lanciare). WARN su chiavi orfane in it.json e sul
  conteggio stub (non e' un errore: e' lavoro di traduzione).
- Come verificato: 272 test headless (267 + 5 in test_i18n_data.gd: catalogo
  caricato, chiave presente traduce ed e' testo vero, chiave mancante ->
  se stessa, fallback en->it, ogni *_i18n dei Pathway attivi risolve - lo
  stesso invariante del validator dentro il motore). validate_data.py exit 0
  (9 warning: gli 8 preesistenti + il conteggio stub). generate_i18n_stubs.py
  --check exit 0 (idempotente). Nessuna verifica a schermo: tr_data non ha
  ancora un consumatore visivo (la UI a libro e' US-221+); il HUD usa l'altro
  sistema (tr()), test_i18n_hud verde.
- Resta aperto: 54 stringhe da tradurre davvero (abilita'/caratteristiche/
  ancore/forme). en.json e' vuoto: la traduzione inglese e' tutta da fare.
  Entrambe sono lavoro di contenuto, non di codice - il validator le conta
  come warning, non le pretende.
- Prossima storia: US-221 (shell del libro - parte 1: controller e dati).

[2026-09-03] US-221 — Shell del libro, parte 1: controller e dati

- DATI:
  * data/schema/page_types.json: vocabolario CHIUSO di 8 tipi di pagina
    (menu_principale, creazione_personaggio, mappa, diagramma_pathway,
    inventario, registro_sinergie, journal_quest, impostazioni). Un tipo
    nuovo e' codice, va discusso.
  * data/ui/book.json (+ data/schema/book.schema.json): il blocco 'libro'
    (voltata_ms 350, dissolvenza_ridotta_ms 80, sfx, macchie_follia), 8
    'pages' (id, tipo, name_i18n, ordine, sbloccata_da.fase, flag), 3
    'segnalibri'. Copiato 1:1 da design-ui-libro.md cap. 4.
  * data/balance.json nuova sezione 'gioco' { fase: 2 }: la fase di sviluppo
    raggiunta. Le pagine con sbloccata_da.fase > gioco.fase esistono ma sono
    bianche (in fase 2: inventario/sinergie/mappa/journal).
- GameData: get_ui_book(), page_types(). _load_flat gia' c'era (US-220);
  book.json e page_types.json caricati con _load_single (38 file ora).
- Autoload Book (scripts/book.gd), ULTIMO. PROCESS_MODE_ALWAYS: input e
  controller restano vivi col mondo in pausa.
  * apri()/chiudi()/alterna(): stesso ingresso apre e chiude. apri() ->
    get_tree().paused = true + segnale libro_aperto(pagina). chiudi() ->
    paused = false + libro_chiuso + memorizza l'ultima pagina.
  * si riapre SEMPRE sull'ultima pagina consultata (_ultima, default = prima
    pagina per 'ordine').
  * vai_a(id): cambia pagina mentre e' aperto, segnale pagina_cambiata;
    pagina inesistente o gia' corrente -> no-op.
  * pagine() (ordinate, + chiave 'sbloccata' bool), pagina(id),
    pagina_sbloccata(id) (false = bianca), segnalibri(), config(key),
    e_aperto(), pagina_corrente().
  * _fase_gioco() da balance.json gioco.fase, fallback 999 (dati illeggibili
    -> non nascondere pagine).
  * _unhandled_input: azione "libro" -> alterna().
- INPUT: project.godot nuova azione "libro" = Tab (physical_keycode 4194306)
  o I (73). Autoload Book aggiunto in coda alla lista.
- i18n: 8 chiavi ui.page.* tradotte a mano in it.json (Scaffale, Frontespizio,
  Mappa, Diagramma dei Pathway, Inventario, Registro delle sinergie, Diario,
  Colophon). it.json: 203 chiavi.
- validate_data.py: nuovo blocco libro - 8 tipi nel vocabolario; tipo di ogni
  pagina nel vocabolario; id e ordine unici; segnalibri -> pagine esistenti;
  name_i18n presente; sbloccata_da ben formato; voltata_ms int > 0;
  balance.json gioco.fase intero presente.
- run_tests.gd: 'paused = false' prima di ogni test - un test che apre il
  libro non deve trascinare la pausa dell'albero nei test successivi.
- Come verificato: 279 test headless (272 + 7 in test_book.gd: autoload+dati,
  tipi nel vocabolario, pagine bianche vs sbloccate in fase 2, apri ferma il
  mondo / chiudi lo riavvia, alterna stesso ingresso, riapre sull'ultima
  pagina, segnalibri validi). validate_data.py exit 0 (9 warning preesistenti
  + stub i18n). VERIFICA A SCHERMO (main.tscn reale, script usa e getta):
  Tab -> libro aperto su 'copertina' + get_tree().paused true + il player non
  si muove premendo move_right; Tab di nuovo -> chiuso + paused false.
- Resta aperto: le PAGINE vere (rendering) sono una per story; l'animazione
  di voltata + navigazione Q/E + sfx sono US-222; il libro non si apre ancora
  su nessuna schermata disegnata. La persistenza dell'ultima pagina nel save
  non e' in US-221 (in memoria, si resetta al riavvio) - agganciarla quando
  serve.
- Prossima storia: US-222 (shell del libro, parte 2: voltata e navigazione).

[2026-09-03] US-222 — Shell del libro, parte 2: voltata e navigazione

- scenes/book_overlay.tscn + scripts/book_overlay.gd (CanvasLayer layer 85,
  PROCESS_MODE_ALWAYS: vive col mondo in pausa). In main.tscn e
  debug_scene.tscn. E' la PRESENTAZIONE; il controller resta l'autoload Book.
  * Disegna: sfondo scuro semitrasparente, la pagina (ColorRect pergamena
    600x340 centrato), la piega centrale, il Titolo (dalla chiave i18n della
    pagina via GameData.tr_data), il Corpo (placeholder "[ tipo ]" se
    sbloccata, "· appunto a matita · / questa pagina si aprira' piu' avanti"
    se bianca - MAI assente), i 3 segnalibri come Button sul bordo destro.
  * VOLTATA: contatore in _process (come madness_overlay, NON un Tween: i
    Tween si fermano con l'albero in pausa). Durata = libro.voltata_ms/1000
    (350 ms). Una fascia d'ombra (ColorRect "Voltata") attraversa la piega;
    a meta' (t>=0.5) il testo della pagina cambia. A fine voltata l'ombra
    sparisce.
  * riduci_animazioni (nuova opzione in data/audio.json.accessibilita,
    default false): la voltata diventa una dissolvenza di
    dissolvenza_ridotta_ms (80 ms). FR-8: il Titolo/Corpo sono sempre testo,
    nessuna informazione vive solo nell'animazione.
  * Si aggancia ai segnali di Book: libro_aperto -> _apri, libro_chiuso ->
    _chiudi, pagina_cambiata -> _volta.
- book.gd: avanti()/indietro() (scorrono _pagine per 'ordine', si fermano ai
  bordi, un libro non cicla); azzera() (torna chiuso + prima pagina: ritorno
  al menu / isolamento dei test); libro_config().
- INPUT: project.godot azioni pagina_indietro = Q, pagina_avanti = E.
  Gestite in book_overlay._unhandled_input solo a libro aperto.
- AUDIO: audio_manager.gd - _ui (AudioStreamPlayer sul bus ui) + suona_ui(sfx);
  SFX_SPEC + sfx_ui_book_open [520,0.22,0.15] e sfx_ui_page [700,0.09,0.25]
  (sintetizzati, US-019). _apri suona sfx_apri, _volta suona sfx_volta (id
  presi da libro.sfx_apri/sfx_volta nei dati).
- run_tests.gd: gia' resetta paused=false fra i test (US-221). azzera() nei
  prepara() di test_book e test_book_overlay: Book e' un autoload, lo stato
  (_ultima pagina) trapassava fra le suite.
- Come verificato: 284 test headless (279 + 5 in test_book_overlay.gd: apri
  mostra il titolo dalla chiave i18n, navigazione avanti/indietro cambia
  pagina e titolo, pagina bianca mostra l'appunto, la voltata ha durata
  finita, riduci_animazioni la chiude in un tick tenendo il titolo).
  validate_data.py exit 0. generate_i18n_stubs.py --check exit 0.
  VERIFICA A SCHERMO (main.tscn, finestra reale): Tab apre il libro su
  'Scaffale'; E sfoglia frontespizio -> mappa -> diagramma -> inventario, il
  titolo e il corpo seguono, la fascia d'ombra della voltata attraversa la
  pagina, le pagine bianche (mappa, inventario) mostrano l'appunto a matita;
  Q torna indietro; Tab chiude e il mondo riparte. Screenshot in user://us222.
- Resta aperto: i segnalibri (Button) si sovrappongono un po' alla pagina (la
  pagina e' 600 su viewport 640) - cosmetico, si sistema con l'arte vera. Le
  PAGINE vere (contenuto) sono una per story: US-223 copertina/frontespizio,
  US-224 diagramma, US-225 colophon. La voltata e' un placeholder a fascia
  d'ombra: i 3-4 frame di curvatura disegnati arrivano con l'arte.
- Prossima storia: US-223 (pagina scaffale + frontespizio / creazione personaggio).

[2026-09-03] US-223 — Pagina scaffale (menu principale) e frontespizio

- PAGINE VERE: il codice implementa un tipo di pagina per ogni tipo del
  vocabolario. book_overlay.gd ora ha PAGINE = { tipo: PackedScene }; _rendi()
  istanzia la scena nel MarginContainer 'Contenuto' e chiama aggiorna(); i
  tipi senza scena ricadono sul placeholder '[ tipo ]', le pagine bianche
  sull'appunto a matita. Corpo (Label) -> Contenuto (MarginContainer).
- scenes/pages/page_menu_principale.{tscn,gd} (VBoxContainer): un Button per
  slot (data/ui/book.json libro.slot = 4). SaveSystem.anteprima(slot) (NUOVO:
  stato+nome+tempo senza applicare ne' migrare, campi non fidati):
  VUOTO -> "Tomo nuovo" -> GameState.scegli_slot + Book.vai_a('frontespizio');
  VALIDO -> "<nome> - <tempo>" -> GameState.carica_slot -> Book.chiudi;
  CORROTTO -> "Tomo bruciato" disabled (US-015: mai crash, mai cancellato).
  + "Torna alla partita" se partita_in_corso.
- scenes/pages/page_creazione_personaggio.{tscn,gd}: partita NON attiva ->
  LineEdit con "Enel" (GameState.nome_default(), design-lore) + "Comincia";
  conferma() -> GameState.nuova_partita(nome, slot scelto) -> Book.chiudi.
  intercetta_avanti(): la voltata in avanti (E) conferma ("si conferma con la
  prima voltata"). Partita attiva -> mostra nome + Pathway (tr_data del
  name_i18n) + Sequenza + tier, sola lettura.
- GameState: salva_slot/carica_slot(slot) (generalizzano salva/carica_rapido),
  nuova_partita(nome, slot), partita_in_corso(), slot_corrente(),
  scegli_slot()/slot_scelto(), nome_default(), segnale partita_iniziata.
- i18n: le stringhe di CHROME del libro (bottoni, inviti) vanno in
  assets/i18n/strings.csv + tr() come l'HUD (BOOK_SCAFFALE_*, BOOK_FRONTESPIZIO_*),
  NON in tr_data: quello resta per le identita' dei dati (titolo pagina,
  nome Pathway). .translation rigenerati con --import.
- validate_data.py: libro.slot intero in [1,12].
- Come verificato: 289 test headless (284 + 5 in test_page_scaffale.gd: slot
  vuoti = tomo nuovo, tomo corrotto = bruciato+disabled e il file non e'
  toccato, tomo valido mostra il nome e carica, frontespizio parte da 'Enel'
  e conferma scrive nome+save, a partita in corso mostra chi sei senza campo
  nome). validator 0, i18n --check 0. VERIFICA A SCHERMO (main.tscn finestra
  reale): lo scaffale mostra 4 tomi (nuovo / "Sidon - 0m" / bruciato grigio /
  nuovo); il frontespizio mostra il campo "Enel" selezionato + "Comincia".
  Screenshot user://us223.
- Resta aperto: il gioco boota ancora dritto in main.tscn con un player - non
  c'e' un vero gating "prima apri il libro" (fase 6, flusso di avvio). La
  pagina identita' e' minimale (nome/pathway/sequenza/tier).
- Prossima storia: US-224 (pagina diagramma dei Pathway con fog of war).

[2026-09-03] US-224 — Pagina diagramma dei Pathway con fog of war

- Autoload KnowledgeStore (scripts/knowledge_store.gd), prima di Book: set di
  flag booleani namespaced (design-master cap. 5). impara(flag)/conosce(flag)/
  tutti()/dimentica_tutto(); per_salvataggio (lista) / da_salvataggio (non
  fidato). SAVE: VERSIONE_CORRENTE 11->12, campo "conoscenza" (lista),
  _migra_11_a_12; GameState.snapshot/applica agganciano lo store.
- scenes/pages/page_diagramma_pathway.{tscn,gd}: GridContainer generato dai
  dati - 10 colonne (GameData.pathway_ids ordinati) x 10 righe (Seq 9->0).
  Header colonna = tr_data(pathway.name_i18n). Nessun nome hardcoded.
  * _stato(pid, n): "corrente" (mia colonna, mia Sequenza), "noto" (mia
    colonna e n >= mia_seq: Sequenze vissute; OPPURE KnowledgeStore conosce
    "pathway:<pid>" o "sequenza:<pid>:<n>"), "ignoto" (offuscato) altrimenti.
  * cella = ColorRect colorato per stato (corrente ciano, noto pergamena,
    ignoto scuro).
  * sotto: "Sei qui: <pathway> S<n> <tier>" + le abilita' della Sequenza
    corrente (tr_data di ability.name_i18n).
- book_overlay.gd: PAGINE += diagramma_pathway. FIX: _rendi() ora fa
  remove_child PRIMA di queue_free - queue_free e' differito, get_child(0)
  restava la pagina vecchia per un frame (rompeva navigazione e input).
  Segnalibri: da Button larghi con testo a nastri sottili (12px) col nome
  nel tooltip - non coprono piu' la pagina.
- i18n: BOOK_DIAGRAMMA_SEI_QUI in strings.csv.
- Come verificato: 294 test headless (289 + 5 in test_page_diagramma.gd:
  griglia 10x10 dai dati con 100 celle tutte con stato, la propria colonna
  nota fino alla Sequenza corrente + corrente evidenziata + non raggiunte
  offuscate, fog of war sugli altri Pathway (impara sequenza -> solo quella;
  impara pathway -> colonna), abilita' della Sequenza corrente elencate,
  conoscenza nel save round-trip). validator 0, i18n --check 0. VERIFICA A
  SCHERMO (main.tscn): a Seq 6 la colonna Twilight Giant e' nota da S9 a S6
  (S6 ciano), le altre offuscate; impara("pathway:fool") accende la colonna
  Fool, impara("sequenza:death:5") accende solo quella cella; sotto, le 2
  abilita' di Seq 6. Screenshot user://us224.
- Resta aperto: le abilita' compaiono come stub "TODO ..." (traduzione, non
  codice). Il sistema conoscenza pieno (fonti: dialoghi, libri, sinergie
  lore) e' fase 6: qui c'e' solo il magazzino + il consumo nel diagramma.
- Prossima storia: US-225 (colophon / impostazioni + user://settings.json).

[2026-09-03] US-225 — Colophon (impostazioni) e user://settings.json

- Autoload SettingsStore (scripts/settings_store.gd), dopo KnowledgeStore.
  user://settings.json con lo stesso rigore del save: scrittura ATOMICA
  (tmp + DirAccess.rename), JSON.parse_string + typeof() al load, file
  corrotto -> push_warning e si riparte dai default dei DATI (mai fatale).
  * get_val(sez, chiave, default): se il tipo nel file diverge dal default
    (settings manomesso) torna il default; int<->float tollerati.
  * set_val(sez, chiave, val): _applica() + _salva() + segnale cambiata.
  * _applica_tutto() all'avvio (dopo un frame): lingua, volumi dei 6 bus,
    toggle accessibilita' -> AudioManager.imposta_accessibilita, scala
    finestra / fullscreen via DisplayServer, rimappature InputMap.
  * Sezioni: audio (volume_<bus> dB + sottotitoli/indicatori/disattiva_sussurri),
    video (scala_finestra 1/2/3, fullscreen, disattiva_shake, riduci_hitstop/
    distorsione/flash/animazioni, macchie_follia), input (azione -> physical_
    keycode, sostituisce il 1o evento tastiera), lingua (it/en).
  * AudioManager e i VFX NON leggono i default: leggono cio' che SettingsStore
    spinge (i toggle passano tutti da imposta_accessibilita, che madness_
    overlay gia' consulta).
- scenes/pages/page_impostazioni.{tscn,gd} (ScrollContainer): sezioni
  costruite da liste, ogni controllo -> SettingsStore.set_val, effetto
  immediato. Rebind: click sul bottone -> "Premi un tasto..." -> il prossimo
  InputEventKey diventa il bind. book_overlay.PAGINE += impostazioni.
- i18n: ~24 chiavi COLOPHON_* in strings.csv (chrome del libro, come l'HUD).
- Come verificato: 302 test headless (294 + 8 in test_settings.gd: scrittura
  atomica + round-trip, file corrotto non fatale, tipo incoerente scartato,
  volume applicato a runtime al bus, lingua applicata a runtime, toggle ->
  AudioManager, rimappatura tasti in InputMap, il colophon costruisce le
  sezioni). validator 0, i18n --check 0. VERIFICA A SCHERMO (main.tscn): il
  colophon mostra i 6 slider di volume (Sussurri gia' basso a -30) e i
  toggle; set scala_finestra=3 -> finestra 1920x1080 senza riavvio;
  volume_music=-20 -> il bus scende subito; disattiva_sussurri -> arriva ad
  AudioManager. settings.json scritto pulito. Screenshot user://us225.
- Resta aperto: nessuna sezione controller dedicata (Godot mappa i joypad
  sulle stesse azioni; il rebind cattura solo tastiera per ora). La scala
  finestra non e' persistita fra sessioni come preset di progetto ma sì in
  settings.json.
- Prossima storia: US-226 (data/vfx.json - palette visive e schema).

[2026-09-03] US-226 — data/vfx.json: palette visive e schema

- data/vfx.json + data/schema/vfx.schema.json (design-vfx.md cap. 4). Gemello
  visivo di audio.json.pathway_palette: un renderer per PRIMITIVA + una
  palette per PATHWAY. NESSUN campo VFX sulle abilita' - ability.schema.json
  non si tocca.
  * tratto_vocabolario: 10 tratti chiusi (pennellata_carica/secca, assenza,
    tratteggio, scheggia, cerchio, glifo, meccanico, crescita, fluido), uno
    per Pathway - il tratto E' la firma.
  * pathway_palette_visiva: 10 voci (inchiostro/primario/accento #rrggbb,
    tratto, scia, distorsione 0..1). Colori PLAUSIBILI, l'arte li rivede.
  * primitive_vfx: 15 voci, una per primitiva IMPLEMENTATA (sprite, frames,
    impact_frame, scia).
  * impact_frames (stile inversione, durata_frames 2, si tara con hitstop_ms),
    nero_di_scena (trigger + durata_ms), accessibilita (riduci_distorsione/
    flash, default da SettingsStore).
- data/schema/primitives.json: aggiunto "implemented": true alle 15 primitive
  col handler nel motore (proprieta' del REGISTRO, come "deferred"). Il
  validator ne ha bisogno: e' Python, non legge _handlers.
- GameData: get_vfx_palette(pathway_id), get_primitive_vfx(tipo), get_vfx(sez).
  vfx.json caricato con _load_single (39 file).
- ability_engine: tipi_primitiva_implementati() -> _handlers.keys().
- validate_data.py: blocco VFX - tratto_vocabolario = 10; palette per ogni
  Pathway attivo (come il check audio); inchiostro/primario/accento #rrggbb;
  tratto nel vocabolario e UNICO per Pathway; ogni primitiva attiva e
  implementata ha una voce primitive_vfx ben formata; ERRORE se un'abilita'
  ha un campo che inizia per "vfx".
- Come verificato: 307 test headless (302 + 5 in test_vfx.gd: palette per
  ogni Pathway attivo coi colori #rrggbb, tratto unico per Pathway, lookup
  primitive_vfx (+ ignoto -> {}), ogni primitiva di tipi_primitiva_
  implementati ha un VFX, sezioni impact_frames/nero_di_scena presenti).
  validator 0, i18n --check 0. Nessuna verifica a schermo: story di soli
  dati, il renderer e' US-227.
- Prossima storia: US-227 (VFX delle primitive di fase 2 con impact frame).

[2026-09-03] US-227 — VFX delle primitive di fase 2 con impact frame

- Autoload Vfx (scripts/vfx.gd), dopo SettingsStore. Si aggancia a
  AbilityEngine.ability_executed: per ogni primitiva dell'abilita' legge
  data/vfx.json (get_primitive_vfx[tipo] + get_vfx_palette[pathway del
  caster]) e riproduce lo sprite placeholder tinto col 'primario' della
  palette, che sfuma su 'frames' frame. Primitiva senza voce primitive_vfx
  -> saltata (le altre man mano).
  * AGGANCIO AL TIMING: oggi l'esecuzione e' istantanea, il trigger e'
    ability_executed; gioca_primitiva() e' pubblico cosi' quando ci sara'
    l'animazione di lancio il player la chiamera' sull'evento
    ability_release dei frame senza toccare vfx.gd.
  * pathway del caster: player -> Progression.pathway(); altri -> palette
    neutra (fallback nel colore).
- scenes/vfx_overlay.tscn + scripts/vfx_overlay.gd (CanvasLayer layer 88,
  sotto la follia). In main.tscn e debug_scene.tscn.
  * IMPACT FRAME: sui colpi con impact_frame:true, un lampo bianco a schermo
    per durata_frames/60 s (data/vfx.json.impact_frames, si tara con
    hitstop_ms). Con riduci_flash -> un bordo bianco spesso 18px invece del
    lampo: stessa informazione (colpo importante), niente lampeggio. FR-8.
  * NERO DI SCENA: alpha 0.85 per nero_di_scena.durata_ms (250) sui trigger
    dichiarati - abilita' di Sequenza <= 4 (parse di sequence_id in vfx.gd),
    parry_perfect (player._su_parata_riuscita), posture_break
    (enemy._su_postura_rotta), via Vfx.evento_combat(nome).
  * Contatori in _process (come madness_overlay), non Tween.
- Come verificato: 313 test headless (307 + 6 in test_vfx_render.gd: impact
  frame durata finita, nero di scena durata finita, abilita' offensiva ->
  impact frame, abilita' Seq 4 -> nero di scena, lo sprite placeholder
  compare accanto al caster, riduci_flash non toglie l'informazione).
  validator 0, i18n --check 0. VERIFICA A SCHERMO (main.tscn, finestra
  reale): eseguendo tg_fendente_pesante sul player lo schermo lampeggia
  bianco (impact frame) e compare un blob dorato (palette twilight_giant);
  eseguendo tg_caccia_spirituale a Seq 4 lo sfondo si mangia di nero;
  con riduci_flash il lampo diventa un bordo bianco. Screenshot user://us227.
- Resta aperto: il "lampo bianco" e' l'approssimazione placeholder
  dell'inversione bianco/nero vera (serve uno shader/impact sprite disegnato,
  fase arte). primitive_vfx copre le 15 implementate; le altre 13 attive
  arrivano coi loro handler in fase 5.
- Prossima storia: US-229 (macchie di follia sulle pagine del libro).

[2026-09-03] US-229 — Macchie di follia sulle pagine del libro (P2)

- book_overlay.gd si aggancia a Madness.madness_changed. Soglie da
  data/balance.json.madness (soglia_distorsioni 15, soglia_abilita_autonome 40).
  * scenes/book_overlay.tscn: nodo Macchie (ColorRect + lo shader
    madness_vignette.gdshader gia' esistente, US-215) sopra la pagina, e
    NoteMargine (Label, grafia rossa piccola in basso a sinistra).
  * soglia 15: i bordi della pagina si scuriscono (intensita cresce con la
    follia), con un respiro sinusoidale in _process.
  * soglia 40: compare una nota a margine - i sussurri in forma SCRITTA
    (FR-8, equivalente visivo del layer audio di US-214). Il testo usa i
    nomi di AudioManager.nomi_sussurro() (NPC/Ancore, fallback generico) e 3
    frasi in strings.csv (BOOK_MACCHIA_1/2/3).
  * CONGELAMENTO: se disattiva_sussurri O riduci_animazioni (AudioManager.
    accessibilita) O il toggle 'macchie di follia' e' spento - interruttore
    a DUE livelli: data/ui/book.json libro.macchie_follia AND
    SettingsStore video.macchie_follia (US-225) - le macchie si fissano a uno
    stato NEUTRO (intensita 0.22, niente respiro); la nota a margine resta
    (e' testo, FR-8). La follia continua a salire e a fare danno: la
    meccanica non cambia.
- Come verificato: 318 test headless (313 + 5 in test_book_macchie.gd:
  niente macchie a follia 0, bordi scuri sopra 15, nota a margine sopra 40,
  il toggle dei dati congela (pagine pulite), disattiva_sussurri congela a
  neutro ma NON azzera la follia). validator 0, i18n --check 0. VERIFICA A
  SCHERMO (main.tscn): a follia 75 i bordi della pagina del diagramma sono
  scuriti (intensita ~0.70, pulsante) + nota rossa "questa non e' la tua
  grafia"; con disattiva_sussurri l'intensita si fissa a 0.22 e la follia
  resta 75. Screenshot user://us229.
- FASE 2 CHIUSA: 35/35 story. Il criterio di uscita (Twilight Giant dalla
  Sequenza 9 alla 5 con zero codice dedicato) era gia' verificato in US-219;
  il blocco UI-libro / VFX / i18n e' completo.
- Prossima: generare il PRD di fase 3 con /prd (roadmap.md - sistemi di
  supporto: inventario, alchimia, forgiatura, pet, base building, talenti).

[2026-09-03] RALPH-3 — Conversione PRD fase 3 -> prd.json

- 006_PRD/prd-fase-3-sistemi-di-supporto.md convertito in prd.json (formato
  del progetto: US-3NN + campo "fase": 3). 35 story, priority 1-35.
- Ordine per dipendenze: Blocco A inventario (US-301..307, prio 1-7), poi
  Blocco C sigilli/forgiatura (US-315/316/317/318, prio 8-11), poi B alchimia
  (US-308/310..314, prio 12-17), D strutture (US-319/320, 18-19), E pet
  (US-321..325, 20-24), F base building (US-326..329, 25-28), G talenti
  (US-330..333, 29-32), H integrazione (US-334..336, 33-35).
- US-309 (incisione sigilli, blocco B del PRD) FUSA in US-317 (blocco C):
  erano la stessa story. Il PRD lo prevedeva.
- prd.json di fase 2 archiviato in archive/2026-09-03-fase-2-pathway-core/.
- branchName: ralph/fase-3-sistemi-di-supporto.
- Prossima storia: US-301 (vocabolario e schema degli item).

[2026-09-03] US-301 — Vocabolario e schema degli item

- data/schema/item_categories.json: vocabolario CHIUSO di 7 categorie
  (ingrediente, equip, sigillo, pergamena, materiale, valuta, consumabile).
- data/schema/equip_slots.json: 4 slot (arma, armatura, accessorio_1,
  accessorio_2) + 3 tipi (arma, armatura, accessorio - il valore del campo
  'slot' su un item; accessorio -> primo accessorio_N libero in US-303).
- data/schema/item.schema.json: forma di data/items/*.json.
- data/items/ (7 file, 20 item): ingredienti (2 coltivabili), equip (incl. 1
  Sigillato con effetto_collaterale), materiali, pergamene (stored_ability_id
  = tg_stretta_ferrea / tg_armatura_alba), sigilli (sigillo_ref -> data/sigils/
  di US-315), valuta (2 tagli), consumabili (effetto = primitiva; 1 nutre_pet).
- GameData: DIR_ITEMS, _load_items (come _load_abilities), get_item(id),
  item_categories(), items_per_categoria(cat), item_ids(), equip_slots(),
  equip_slot_tipi(). 41 file caricati.
- validate_data.py: blocco item - id unici; categoria nel vocabolario; tag
  nel vocabolario dei tag; valore int >= 0; impilabile bool; equip -> slot nei
  tipi + stat_modifiers su stat note + sigillato:true richiede
  effetto_collaterale (US-318); stored_ability_id risolve; effetto.tipo e' una
  primitiva del registro (non differita); >= 12 item; warn se una categoria
  non ha esempi.
- i18n: 40 chiavi item.* tradotte a mano in it.json (243 chiavi totali).
- Come verificato: 325 test headless (318 + 7 in test_items.gd: vocabolari
  caricati, get_item + ignoto -> {}, ogni categoria ha un item, >= 12 item,
  equip con slot e stat valide, pergamena con stored_ability che risolve,
  ogni name_i18n/descrizione_i18n degli item risolve). validator 0,
  generate_i18n_stubs.py --check 0. Nessuna verifica a schermo: story di
  dati + loader.
- Prossima storia: US-302 (autoload Inventory).

[2026-09-03] US-302 — Autoload Inventory

- Autoload Inventory (scripts/inventory.gd), dopo CharacteristicStore.
  * impilabile:true -> contatore in _stack; impilabile:false -> istanze
    distinte in _istanze con instance_id univoco ("inv_N", _next_iid).
  * aggiungi/rimuovi/conta/possiede/per_categoria(cat)/tutto();
    rimuovi_istanza(instance_id) e restituisci_istanza() per l'equip (US-303);
    ricchezza() (US-305, spostato qui). item ignoto -> warn, non aggiunto.
  * Segnali item_aggiunto(item_id, quantita), item_rimosso(...).
  * Nessun cap spaziale ne' peso.
- SAVE: VERSIONE_CORRENTE 12 -> 13. Campo "inventario" {stack, istanze,
  next_iid}, _migra_12_a_13 (personaggio pregresso a mani vuote).
  GameState.snapshot/applica agganciano Inventory. da_salvataggio non fidato:
  stack con item_id che non risolve o quantita' <= 0 scartati, istanze senza
  instance_id/item_id valido scartate, raw non-oggetto -> vuoto.
- Come verificato: 332 test headless (325 + 7 in test_inventory.gd: stack
  aggiungi/conta/rimuovi + rimozione oltre il posseduto -> false, non-stack
  = istanze distinte con instance_id + rimuovi_istanza, item ignoto non
  esplode, segnali, ricchezza (30*1 + 2*50 = 130), round-trip via GameState,
  da_salvataggio non fidato). validator 0, catena migrazione 1->13 verde.
- Prossima storia: US-303 (equipaggiamento a slot).

[2026-09-03] US-303 — Equipaggiamento a slot

- Autoload Equipment (scripts/equipment.gd), dopo Inventory. _slot: mount ->
  {instance_id, item_id}. Mount da data/schema/equip_slots.json (arma,
  armatura, accessorio_1, accessorio_2); il campo 'slot' dell'item e' un TIPO
  (arma/armatura/accessorio), _mount_per() mappa: accessorio -> primo
  accessorio_N libero.
  * equipaggia(instance_id): solo categoria:equip; smonta il mount se pieno;
    Inventory.rimuovi_istanza (esce dallo zaino); applica stat_modifiers come
    modificatore per id 'equip:<mount>' (US-007) sullo StatsComponent del
    giocatore.
  * rimuovi_slot(mount): remove_modifier + Inventory.restituisci_istanza.
  * equipaggiato(mount), slot_pieni(), tag_attivi() (gancio US-306),
    riapplica_al_giocatore() (chiamata da player.gd _ready, come Progression).
- SAVE: VERSIONE_CORRENTE 13 -> 14. Campo "equipaggiamento" {mount ->
  {instance_id, item_id}}, _migra_13_a_14. GameState.applica chiama
  Equipment.da_salvataggio DOPO Inventory (le istanze equipaggiate non sono
  nello zaino). da_salvataggio non fidato: mount non valido o item non-equip
  scartati; poi riapplica_al_giocatore().
- player.gd _ready: chiama anche Equipment.riapplica_al_giocatore().
- Come verificato: 338 test headless (332 + 6 in test_equipment.gd: equipaggia
  applica i modificatori + esce dallo zaino, rimuovi_slot li toglie e
  restituisce, due accessori su slot diversi, instance_id inesistente ->
  false, round-trip via GameState riapplica il modificatore, da_salvataggio
  non fidato). validator 0, catena migrazione 1->14 verde. Player finto
  liberato a fine test (come test_progression) - un Node bare in gruppo
  'player' rompe test_i18n_hud/progression/vertical_slice che tipano Node2D.
- Prossima storia: US-304 (stored_ability_id - consumo dall'inventario, P0).

[2026-09-03] US-304 — stored_ability_id: consumo dall'inventario (P0)

- Inventory.usa(instance_id, bersaglio := null) -> {ok, reason, risultato}:
  se l'item ha stored_ability_id, chiama AbilityEngine.execute_stored(sab,
  giocatore) (US-206, gia' pronto: nessun costo/cooldown/ownership) e SOLO su
  risultato.ok consuma l'istanza (rimuovi_istanza). Item senza
  stored_ability_id -> {ok:false, reason:"nessuna_abilita"}, non consumato,
  nessun crash. Istanza assente / nessun giocatore / motore assente -> esiti
  gestiti.
- data/items/pergamene.json: nuova pergamena_hermit (stored_ability_id
  hermit_pergamena). La note di stress test in data/abilities/hermit.json e'
  RISOLTA: la pergamena e' un item, non serve una primitiva nuova. reveal_info
  resta attiva-non-implementata: l'item funziona, il VFX arriva col Pathway
  Hermit in fase 5.
- i18n: 2 chiavi item.pergamena_hermit* tradotte.
- Come verificato: 342 test headless (338 + 4 in test_stored_item.gd: usa
  pergamena_vigore -> abilita' eseguita (risultato.ok) + consumata, seconda
  usa sulla stessa istanza -> istanza_assente gestito, item equip senza
  stored_ability_id -> nessuna_abilita gestito e non consumato, pergamena_hermit
  -> ok (l'item funziona anche con la primitiva non ancora resa)). validator
  0, i18n --check 0. VERIFICA A SCHERMO (main.tscn): pergamena_vigore in
  zaino -> usa() ok=true -> conta 1->0 (consumata) -> execute_stored esegue
  tg_stretta_ferrea (buff_stat difesa moltiplicativo: invisibile a difesa
  base 0 - quirk di buff_stat/balance, non del contratto). Il VFX placeholder
  della primitiva (US-227) compare sul giocatore. Screenshot user://us304.
  CONTRATTO P0 CHIUSO.
- Prossima storia: US-305 (valuta come oggetto).

[2026-09-03] US-305 — Valuta come oggetto

- Gran parte era gia' fatta in US-301 (data/items/valuta.json: moneta_comune
  impilabile:true valore:1, + sigillo_reale valore:50) e US-302
  (Inventory.ricchezza() = somma conta * valore su categoria:valuta).
  US-305 aggiunge solo la verifica esplicita: la valuta NON ha metodi
  dedicati in Inventory, e' conta('moneta_comune') come ogni stack.
- Come verificato: 345 test headless (342 + 3 in test_valuta.gd: la valuta e'
  categoria:valuta impilabile valore 1; si aggiunge/rimuove/conta e compare
  in per_categoria come ogni item; ricchezza() = 0 a mani vuote, 30*1+4*50=230
  con due tagli). validator 0.
- Prossima storia: US-306 (tag di inventario ed equip esposti per le sinergie).

[2026-09-03] US-306 — Tag di inventario ed equip esposti per le sinergie

- Inventory.tag_attivi() -> {tag: conteggio}: delega a Equipment.tag_attivi()
  (i tag degli item INDOSSATI; i sigilli incastonati si aggiungeranno in
  US-317). Gli item nello zaino NON contano - una sinergia e' cio' che
  indossi. API di SOLA LETTURA: non conosce le sinergie, e' fase 4
  (SynergySources, US-334) a comporla con pet/talenti/stanze e risolvere.
- design-master.md cap. 5: nuovo bullet "Fonti di richiede_tag" - le 4
  sorgenti (Inventory, PetSystem, TalentSystem, BaseSystem), ognuna con
  tag_attivi() di sola lettura, sommate da SynergySources.
- data/items/equip.json: aggiunto il tag 'guerra' a spada_ferrea e
  corazza_cuoio (kit di ferro del guerriero) - serviva una coppia con tag
  condiviso su slot diversi per il test.
- Come verificato: 347 test headless (345 + 2 in test_inventory_tags.gd: un
  equip nello zaino non produce tag / equipaggiato si', due equip con tag
  'guerra' condiviso su slot diversi -> conteggio 2, smontarne uno -> 1).
  validator 0.
- Prossima storia: US-307 (pagina inventario del libro).

[2026-09-03] US-307 — Pagina inventario del libro. BLOCCO A (inventario) CHIUSO.

- data/balance.json gioco.fase 2 -> 3: la "fase di sviluppo corrente". La
  pagina 'inventario' (sbloccata_da fase 3) passa da bianca a scritta.
  Aggiornati test_book (test_pagine_bianche_vs_sbloccate) e test_book_overlay
  (usa 'mappa' come esempio di pagina bianca, sbloccata_da fase 6).
- scenes/pages/page_inventario.{tscn,gd} (VBoxContainer), in book_overlay.PAGINE.
  5 sezioni con bottoni (toggle): ZAINO e INDOSSO scritte, RICETTARIO/TALENTI/
  BASE segnaposto ("si scrive piu' avanti").
  * Zaino: item raggruppati per categoria (label i18n BOOK_CAT_*), nome da
    GameData.tr_data + "xN" per gli stack. equip -> bottone "Indossa"
    (Equipment.equipaggia); pergamena -> "Usa" (Inventory.usa).
  * Indosso: i 4 slot di equip_slots (label BOOK_SLOT_*), item equipaggiato o
    "vuoto", bottone "Togli" (Equipment.rimuovi_slot). Riga segnaposto per i
    sigilli incastonati (US-317).
- i18n: ~22 chiavi BOOK_INV_* / BOOK_CAT_* / BOOK_SLOT_* in strings.csv,
  .translation rigenerati (--import).
- Come verificato: 351 test headless (347 + 4 in test_page_inventario.gd:
  pagina istanziata (non bianca), lo zaino elenca gli item, equipaggia dal
  bottone della pagina -> slot arma pieno, la sezione Indosso mostra i 4
  slot). validator 0, i18n --check 0. VERIFICA A SCHERMO (main.tscn): con
  ingredienti/equip/pergamene/materiali/valuta in zaino, la pagina mostra le
  categorie coi bottoni Indossa/Usa; la sezione Indosso mostra Arma/Armatura/
  Accessorio I/II tutti vuoti + il segnaposto sigilli. Screenshot user://us307.
- BLOCCO A completo: US-301..307. Inventario, equip, stored_ability_id (P0),
  valuta, tag per le sinergie, pagina del libro. Prossimo blocco: C
  (sigilli e forgiatura, US-315..318).
- Prossima storia: US-315 (schema dei sigilli).

[2026-09-03] US-308 — Ricettario, tier e qualita' delle pozioni

- BLOCCO B (alchimia). Percorso SEPARATO dalle formule di AVANZAMENTO di
  fase 2 (data/potions/formulas.json resta invariata): qui si preparano
  pozioni CONSUMABILI da ingredienti di data/items/.
- data/schema/potion_quality.json: vocabolario CHIUSO {scarsa, instabile,
  pura, eccelsa} (crescente). formulas.json.penalita_parziale gia' usa
  "instabile": scala condivisa, una pozione parziale e' al piu' instabile.
- data/schema/potion_recipe.schema.json + data/potions/recipes.json: 6 ricette
  (3 base note_da_subito, 2 avanzata, 1 leggendaria). Ognuna: id, name_i18n,
  tier, nota_da_subito, output {item_id | effetto}, ingredienti {item_id: qta},
  qualita_base.
- data/items/: 5 ingredienti nuovi (acqua_sorgiva, petalo_solare,
  sangue_di_lupo, lacrima_di_luna + fungo gia' c'era) e 3 consumabili nuovi
  (pozione_cura_maggiore, elisir_ombra, ambrosia) come output delle ricette.
- GameData: get_recipe(id), recipe_ids(), recipes_per_tier(tier),
  potion_quality(). recipes.json + potion_quality.json caricati (43 file).
- validate_data.py: blocco ricette - tier in {base,avanzata,leggendaria};
  nota_da_subito bool == (tier base); qualita_base nel vocabolario; ogni
  ingrediente e' un item categoria:ingrediente con qta int > 0; output.item_id
  risolve o output.effetto.tipo e' una primitiva implementabile. + item_cat
  (id -> categoria) raccolto nel blocco item per riuso.
- i18n: 18 chiavi item.* / recipe.* tradotte (265 chiavi totali).
- Come verificato: 357 test headless (351 + 6 in test_recipes.gd: vocabolario
  qualita' 4 crescente, get_recipe + tier + id ignoto -> {}, recipes_per_tier
  per i 3 tier, solo le base sono note_da_subito, ogni ingrediente/output
  risolve, la qualita' parziale di fase 2 e' nel vocabolario). validator 0,
  i18n --check 0.
- Prossima storia: US-310 (concoction v2 - qualita', laboratorio, output).

[2026-09-04] US-310 — Concoction v2: qualita', laboratorio, output nell'inventario

- PotionSystem: sezione nuova per le pozioni CONSUMABILI (percorso separato
  dalla concoction di avanzamento di fase 2, che resta invariata).
  * ricetta_nota(recipe_id): tier 'base' (nota_da_subito) OPPURE flag
    KnowledgeStore 'ricetta:<id>' (avanzata scoperta / leggendaria appresa).
  * prepara(recipe_id) -> {ok, reason, item_id, qualita}: verifica ricetta
    nota + ingredienti in Inventory, consuma gli ingredienti (Inventory.rimuovi),
    aggiunge l'output (Inventory.aggiungi), emette pozione_preparata. Reason:
    sistema_assente / ricetta_inesistente / ricetta_ignota / ingredienti_mancanti
    (quest'ultimo NON consuma nulla).
  * _qualita_finale: indice di qualita_base sulla scala potion_quality +
    _bonus_stanza('laboratorio','qualita_pozione') (US-327, 0 finche' non
    c'e' BaseSystem) + _bonus_talento('alchimia_qualita') (US-329, 0 finche'
    non c'e' TalentSystem), clampato. Per ora qualita = qualita_base.
- La qualita' e' calcolata e restituita ma NON ancora persistita per-istanza
  (i consumabili sono stack): quando la qualita' contera' meccanicamente
  (bevi con effetti di qualita') si estende l'inventario. Per ora US-314
  mostra la qualita_base della ricetta.
- Come verificato: 362 test headless (357 + 5 in test_concoction_v2.gd:
  prepara base nota -> item in inventario + qualita 'pura' + ingrediente
  consumato, ingredienti mancanti -> non consuma, ricetta inesistente/ignota,
  avanzata preparabile solo col flag KnowledgeStore, prepara una consumabile
  NON crea una pozione di avanzamento). validator 0. Nessuna regressione
  (i 17 test di test_concoction / test_advancement_gate verdi).
- Prossima storia: US-311 (sperimentazione e scoperta delle ricette).

[2026-09-04] US-311 — Sperimentazione e scoperta delle ricette

- data/potions/experiment_outcomes.json: 5 esiti CHIUSI di un esperimento
  senza ricetta - fumo (niente), scarto (produce scarto_alchemico), ustione
  (danno 20), contaminazione (follia 8), aberrazione (evoca, gancio US-312).
  I tre mostruosi (mostruoso:true) hanno il peso modulato dalla follia.
- data/items/materiali.json: scarto_alchemico (valore 0).
- GameData: experiment_outcomes(). PATH_EXPERIMENT_OUTCOMES (44 file).
- PotionSystem.sperimenta(ingredienti: Array) -> {ok, esito, ...}:
  * verifica di possedere gli ingredienti passati (multiset); mancanti ->
    ingredienti_mancanti, NON consuma. Presenti -> consuma SEMPRE.
  * se il multiset == quello di una ricetta 'avanzata' non ancora nota ->
    SCOPERTA: KnowledgeStore.impara('ricetta:<id>') + produce la pozione.
  * altrimenti FALLIMENTO pesato: peso_base per esito, i mostruosi *
    Foundation.moltiplicatore_follia() * (1 - riduzione rischio del
    laboratorio/talenti, 0 finche' non esistono). Tiro con RNG seedabile
    (imposta_seed). _applica_esito: produce lo scarto / add follia / -hp al
    giocatore / no-op; evoca -> _evoca_aberrazione (no-op documentato, US-312).
- validate_data.py: blocco esiti - vocabolario chiuso {fumo, scarto, ustione,
  contaminazione, aberrazione}; peso int >= 0; mostruoso bool; produce risolve.
- i18n: 2 chiavi scarto_alchemico.
- Come verificato: 366 test headless (362 + 4 in test_experiment.gd: la combo
  di una avanzata la scopre (+flag+pozione+consumo), combo ignota ->
  fallimento deterministico (RNG seedabile) e ingredienti consumati anche sul
  fallimento, ingredienti mancanti -> non consuma, follia alta -> esiti
  mostruosi piu' frequenti su 300 tiri). validator 0, i18n --check 0.
- Prossima storia: US-312 (fallimenti mostruosi - l'aberrazione).

[2026-09-04] US-312 — Fallimenti mostruosi: l'aberrazione

- SummonRegistry: nuovo canale TEMPORANEO. evoca_temporanea(tipo,
  comportamento, durata, hp) -> id ("tmp_N"); _temporanee: Array processata
  in _process (scade a tempo, emette evocazione_rimossa). conta_temporanee(),
  temporanee(). NON serializzata: per_salvataggio/da_salvataggio invariati.
  pulisci() svuota anche le temporanee.
- data/balance.json nuova sezione 'alchimia': aberrazione_hp 30,
  aberrazione_durata_s 25, follia_aberrazione 12, malus_fondamenta_aberrazione -15.
- PotionSystem._evoca_aberrazione(entita_id) (era il no-op gancio di US-311):
  SummonRegistry.evoca_temporanea('aberrazione_alchemica','ostile',durata,hp)
  + Madness.add(follia) + Foundation.applica(malus). Numeri da balance.json.
  + forza_esito(nome): applica un esito di esperimento a comando (debug/test).
- Come verificato: 369 test headless (366 + 3 in test_aberrazione.gd:
  forza_esito('aberrazione') -> 1 evocazione temporanea 'ostile' + follia su
  + fondamenta giu'; NON e' fra le persistenti / non entra nel save;
  evoca_temporanea con durata 1s scade dopo 1.2s di _process). validator 0,
  nessuna regressione (test_summon verde).
- Prossima storia: US-313 (ricette leggendarie via conoscenza).

[2026-09-04] US-313 — Ricette leggendarie via conoscenza

- PotionSystem.ricetta_nota() gia' gestiva le leggendarie: non nota_da_subito
  -> serve il flag KnowledgeStore 'ricetta:<id>'.
- Inventory.usa() esteso: se l'item ha 'insegna_ricetta', chiama
  KnowledgeStore.impara('ricetta:<id>') e consuma. Controllato PRIMA di
  stored_ability_id. Restituisce {ok:true, risultato:{ricetta_appresa:<id>}}.
- data/items/pergamene.json: pergamena_ambrosia (insegna_ricetta ric_ambrosia).
- validate_data.py: ogni insegna_ricetta risolve a una ricetta di tier
  'leggendaria' (le base/avanzata non si insegnano con una pergamena).
- test_items aggiornato: una pergamena ha stored_ability_id O insegna_ricetta.
- i18n: 2 chiavi pergamena_ambrosia.
- Come verificato: 372 test headless (369 + 3 in test_ricetta_leggendaria.gd:
  leggendaria ignota non si prepara nemmeno con gli ingredienti, la pergamena
  la insegna (flag + consumo), dopo averla appresa si prepara e produce
  l'ambrosia). validator 0, i18n --check 0.
- Prossima storia: US-314 (il ricettario nel libro).

[2026-09-04] US-314 — Il ricettario nel libro. BLOCCO B (alchimia) CHIUSO.

- page_inventario.gd: la sezione 'ricettario' (era il segnaposto "arriva piu'
  avanti") diventa reale. Ricette ordinate per tier (base, avanzata,
  leggendaria) poi per id.
  * ricetta NOTA (PotionSystem.ricetta_nota): nome (tr_data) + "(tier)" +
    bottone "Prepara" (disabilitato se mancano ingredienti -> Inventory.conta;
    premuto -> PotionSystem.prepara + refresh).
  * ricetta NON nota: riga OFFUSCATA (alpha 0.55) "Ricetta sconosciuta - N
    ingredienti" - sai che esiste e quanti ingredienti, non quale (stesso
    principio del fog of war del diagramma).
- i18n: BOOK_RIC_PREPARA, BOOK_RIC_SCONOSCIUTA (con %d) in strings.csv.
- Come verificato: 375 test headless (372 + 3 in test_page_ricettario.gd: le
  base sempre visibili + le altre offuscate, un'avanzata scoperta compare
  col nome, Prepara dal ricettario produce la pozione). validator 0,
  i18n --check 0. VERIFICA A SCHERMO (main.tscn): il ricettario mostra 3 base
  + 1 avanzata appresa (Prepara attivo/disattivo secondo gli ingredienti) +
  2 righe "Ricetta sconosciuta - N ingredienti" offuscate. Screenshot
  user://us314.
- BLOCCO B completo: US-308, 310, 311, 312, 313, 314. Ricettario a 3 tier,
  qualita', concoction v2, sperimentazione + scoperta, fallimenti mostruosi
  (aberrazione), ricette leggendarie, ricettario nel libro.
- Prossima storia: US-319 (StructureRegistry - blocco D).

[2026-09-05] CONTROLLO + US-315 — Schema dei sigilli (blocco C, riallineamento)

- Controllo richiesto dall'utente prima di proseguire: git log/status puliti,
  python tools/validate_data.py -> 0 errori (9 warning noti), Godot 4.3
  headless -> 375/375 test passati, letto scripts/potion_system.gd (blocco B)
  per qualita' del codice: coerente con lo stile del progetto, nessun if per
  contenuto specifico, forward-compatible con BaseSystem/TalentSystem non
  ancora esistenti (bonus 0 finche' assenti). Nessuna regressione trovata.
- ERRORE DI SEQUENZA TROVATO: la nota RALPH-3 (2026-09-03) fissa l'ordine
  Blocco A -> Blocco C (sigilli/forgiatura, prio 8-11) -> Blocco B (alchimia,
  prio 12-17) -> D. La sessione precedente ha chiuso il Blocco B e scritto
  "Prossima storia: US-319" saltando interamente il Blocco C. prd.json aveva
  gia' le priority corrette (8-11 per C), solo la nota finale era sbagliata.
  Corretto riprendendo da US-315 come da richiesta esplicita dell'utente
  ("continua con il blocco C").
- Cosa fatto (US-315):
  - data/schema/sigil.schema.json (doc) + data/schema/sigil_effect_types.json:
    vocabolario CHIUSO {effetti: stat_modifier|stored_ability_id|tag_grant,
    effetti_collaterali: stat_modifier|tag_grant (sottoinsieme - un collaterale
    non e' mai "esegui un'abilita'", e' sempre una stat che peggiora o un tag
    scomodo)}.
  - data/sigils/core.json: 8 sigilli (sig_forza_1, sig_difesa_1, sig_velocita_1,
    sig_precisione_1, sig_custode, sig_eco_lama, sig_ombra_fame, sig_sangue_avido).
    2 con effetto_collaterale: sig_ombra_fame (evasione +12% / spiritualita_max
    -15), sig_sangue_avido (forza +18% / hp_max -20). sig_forza_1 e
    sig_ombra_fame erano GIA' referenziati da data/items/sigilli.json (stub di
    US-301): id scelti per farli risolvere senza toccare quell'item.
  - data/items/sigilli.json completato da 2 a 8 voci (una per sigillo).
  - GameData: get_sigil(id)/sigil_ids()/sigil_effect_types(collaterale) (46 file).
  - validate_data.py: name_i18n presente, tag nel vocabolario, effetto/
    effetto_collaterale.tipo nel vocabolario chiuso (ammessi diversi per i due
    campi), stat_modifier.stat nota, stored_ability_id risolve, tag_grant.tag
    noto, effetto_collaterale.valore < 0 se stat_modifier (REGOLA: un
    collaterale e' sempre uno svantaggio, US-315), >= 8 sigilli, >= 2 con
    collaterale, ogni item categoria:sigillo il cui sigillo_ref risolve a un
    sigillo esistente.
  - i18n: 20 chiavi nuove tradotte a mano (item.sigillo_* + sigillo.sig_*,
    289 totali).
- Come verificato: 381 test headless (375 + 6 in test_sigils.gd: >= 8 sigilli,
  get_sigil + id ignoto -> {}, >= 2 con collaterale, collaterale sempre
  svantaggio, stored_ability_id del sigillo risolve, ogni item sigillo
  referenzia un sigillo esistente). validate_data.py -> 0 errori, 9 warning
  (stessi di sempre). Nessuna verifica a schermo: story di dati + loader,
  nessuna UI tocca ancora i sigilli (arriva con US-317).
- Prossima storia: US-316 (forgiatura dell'equipaggiamento).

[2026-09-05] US-316 — Forgiatura dell'equipaggiamento

- Cosa fatto:
  - data/schema/forge_blueprint.schema.json (doc) + data/forge/blueprints.json:
    3 blueprint (bp_spada_ferrea: 3 lingotto_ferro -> spada_ferrea,
    bp_corazza_cuoio: 4 cuoio_conciato -> corazza_cuoio, bp_amuleto_lunare:
    2 cristallo_grezzo + 1 lingotto_ferro -> amuleto_lunare). Tutti gli output
    sono equip GIA' esistenti (stub di US-301): niente item duplicati.
    Tutti nota_da_subito:true: nessuna story di fase 3 introduce una scoperta
    dei blueprint (a differenza delle ricette avanzata/leggendaria di US-311/313).
  - Autoload Forge (scripts/forge.gd, dopo PotionSystem in project.godot):
    forgia(blueprint_id) -> {ok, reason, item_id, instance_id, qualita}.
    Verifica blueprint noto + tutti i materiali presenti PRIMA di consumare
    (stesso pattern di PotionSystem.prepara): sul rifiuto zero consumo.
    _qualita_finale ricalca alla lettera PotionSystem._qualita_finale (stessa
    scala potion_quality.json, stesso _bonus_stanza/_bonus_talento a 0 finche'
    BaseSystem/TalentSystem non esistono): il wiring vero del bonus e' US-327/331,
    non questa story, come gia' deciso per l'alchimia in US-310.
  - Inventory.aggiungi() ora ritorna Array (gli instance_id creati; [] per gli
    item impilabile:true, che non ne hanno). Serve a Forge per sapere QUALE
    istanza ha appena prodotto. Tutti i chiamanti esistenti (potion_system.gd,
    tutti i test) ignoravano il valore di ritorno: cambio retrocompatibile,
    verificato a grep prima di farlo.
  - GameData: get_blueprint(id), blueprint_ids() (47 file).
  - validate_data.py: name_i18n presente, nota_da_subito bool, qualita_base
    nel vocabolario condiviso, output.item_id risolve ed e' categoria:equip
    (non un materiale o altro), ogni materiale e' categoria:materiale con
    quantita intera > 0.
  - i18n: 3 chiavi blueprint.* tradotte a mano (292 totali).
- Come verificato: 387 test headless (381 + 6 in test_forge.gd: forgia con
  materiali -> equip in inventario + materiali consumati; materiali mancanti
  -> rifiuto, zero consumo; blueprint inesistente gestito; l'equip forgiato ha
  i tag del blueprint di provenienza; qualita' = qualita_base finche' non c'e'
  stanza ne' talento; ogni blueprint dei dati e' ben formato). validate_data.py
  -> 0 errori, 9 warning (gli stessi di sempre). Le 4 "ERROR: Parse JSON
  failed" nel log sono PRE-ESISTENTI (test_save_system/test_settings che
  alimentano di proposito un JSON malformato per verificare la gestione
  dell'errore): confermato leggendo il contesto del log, non hanno a che fare
  con Forge. Nessuna verifica a schermo: story di sistema, nessuna UI la
  espone ancora (arriva con US-329, sezione Base del libro - la forgiatura
  in se' non ha una sua pagina nel PRD di fase 3).
- Prossima storia: US-317 (incisione dei sigilli sull'equip, fusione con
  US-309 gia' decisa in RALPH-3).

[2026-09-05] US-317 — Incisione dei sigilli sull'equip (fusione US-309)

- Cosa fatto:
  - Equipment (scripts/equipment.gd): incastona(mount, sigillo_instance_id)
    fallisce se il mount e' vuoto o se non ci sono slot_sigilli liberi;
    rimuovi_sigillo(mount, sigillo_instance_id) restituisce il sigillo allo
    zaino; sigilli_incastonati(mount) per l'ispezione.
  - effetto/effetto_collaterale del sigillo si applicano come modificatore
    "sigillo:<instance_id>[:collaterale]" sullo StatsComponent. Solo
    stat_modifier produce un modificatore reale (tag_grant vive gia' in
    tag_attivi(), stored_ability_id non ha ancora un consumatore attivo:
    lo schema di US-315 lo copre, l'USO e' materia di una story futura -
    nessun crash, solo nessun effetto per ora, come "non ancora implementata"
    altrove nel motore). moltiplicativo converte in delta sulla stat BASE,
    ricalcando alla lettera ability_engine.gd._mod_stat.
  - rimuovi_slot(mount) ora estrae PRIMA ogni sigillo incastonato e lo
    restituisce allo zaino: smontare un equip non fa piu' perdere i sigilli
    incisi (nessuna perdita silenziosa di oggetti, principio gia' del save).
  - Inventory.tag_attivi() (US-306) ora include anche i tag dei sigilli
    incastonati, non solo quelli dell'equip: Equipment.tag_attivi() somma
    entrambe le fonti.
  - SAVE: schema_version 14 -> 15. Il campo 'equipaggiamento' cambia forma da
    {mount: {...}} a {slot: {mount: {...}}, sigilli: {mount: [{instance_id,
    item_id, sigillo_ref}]}}. _migra_14_a_15 sposta il vecchio dict piatto
    sotto 'slot' invece di perderlo; 'sigilli' parte vuoto.
  - BUG PRE-ESISTENTE TROVATO E CORRETTO (non introdotto da questa sessione):
    save_system.gd._migra() aveva un `match v:` che copriva solo v1..v10;
    le funzioni _migra_11_a_12 (US-224), _migra_12_a_13 (US-302),
    _migra_13_a_14 (US-303) erano scritte e corrette ma MAI invocate — dead
    code. Il bug era mascherato: _leggi_snapshot() applica comunque un
    default sano (TYPE_DICTIONARY/ARRAY -> {}/[]) a ogni campo mancante,
    quindi il comportamento osservabile restava corretto anche senza le
    migrazioni esplicite. Aggiunti i case 11/12/13/14 mancanti al match,
    prima di aggiungere il proprio case 14 (necessario perche' la mia
    migrazione 14->15 girasse davvero). Trovato controllando il log dei test
    ("nessuna migrazione da v11/v12/v13: salto") durante il controllo di
    integrita' a inizio sessione, prima ancora di toccare US-317.
  - CORREZIONE DATI DI US-315: sig_forza_1 e sig_sangue_avido dichiaravano
    moltiplicativo:true sulla stat 'forza', ma StatsComponent.configure_from_balance
    non assegna MAI una base a 'forza' (e' un accumulatore solo-bonus, come
    gli stat_modifiers piatti di ogni equip esistente: spada_ferrea,
    lama_del_pentimento). moltiplicativo:true su una base 0 e' un effetto
    SEMPRE zero: un bug silenzioso. Portati a moltiplicativo:false (flat),
    coerente con l'unica convenzione che 'forza' abbia mai avuto nel
    progetto. Trovato scrivendo il test di round-trip di questa story, che
    lo avrebbe reso invisibile altrimenti (il sigillo veniva incastonato
    "con successo" ma non cambiava nulla). Aggiunto un avviso nel _comment
    di data/sigils/core.json cosi' non si ripete su un sigillo futuro.
  - TRAPPOLA DI TEST (non un bug del motore): il primo tentativo del test di
    round-trip falliva confrontando la forza PRIMA e DOPO snapshot/applica,
    perche' GameState.applica() sincronizza anche Progression (che applica
    IL SUO modificatore "sequence:<n>", comprensivo di un contributo a
    forza): la prima misura non l'aveva mai visto (Progression non era mai
    stata sincronizzata), la seconda si'. Non e' un bug dei sigilli: e' un
    side effect legittimo di un altro sistema toccato dallo stesso round
    trip. Il test ora sincronizza Progression una volta PRIMA di misurare
    "prima", isolando cosi' il solo contributo del sigillo. Lezione: un test
    di round-trip via GameState.snapshot()/applica() confronta stati del
    MONDO INTERO, non del solo sistema che si vuole testare — se un altro
    sistema autoload non era mai stato sincronizzato prima, il confronto
    prima/dopo e' falsato dal SUO primo sync, non da un bug del sistema sotto
    test.
- Come verificato: 393 test headless (387 + 6 in tests/test_incastonamento.gd:
  incastona applica l'effetto e consuma il sigillo dallo zaino; il limite di
  slot_sigilli e' rispettato; effetto_collaterale applicato e rimosso
  insieme all'effetto; i sigilli incastonati contano in tag_attivi; smontare
  l'equip restituisce sia l'equip sia i sigilli, zero perdita; il round-trip
  del save preserva l'incastonamento e il modificatore). validate_data.py ->
  0 errori, 9 warning (gli stessi di sempre). Le "ERROR: Parse JSON failed"
  nel log restano quelle preesistenti di test_save_system/test_settings
  (JSON malformato di proposito), confermato di nuovo dal contesto del log.
  Nessuna regressione sui test di Equipment esistenti (test_da_salvataggio_non_fidato
  aggiornato al nuovo formato annidato, stesso comportamento testato).
  Nessuna verifica a schermo: nessuna story del PRD di fase 3 espone
  l'incastonamento nella pagina inventario (US-307/US-329 non lo coprono).
- Prossima storia: US-318 (oggetti Sigillati).

[2026-09-05] US-318 — Oggetti Sigillati (blocco C CHIUSO)

- Cosa fatto:
  - data/schema/sigillato_effect_types.json: vocabolario CHIUSO diverso da
    quello dei sigilli incastonabili (US-315/data/schema/sigil_effect_types.json):
    qui l'effetto_collaterale e' SEMPRE attivo mentre l'equip e' indosso, non
    un modificatore statico. Due famiglie: "tick" (follia_al_secondo,
    spiritualita_drain_al_secondo - un valore/secondo) e "tag" (tag_negativo -
    un tag extra sommato in tag_attivi(), es. "attira i nemici").
  - 2 nuovi equip Sigillati oltre a lama_del_pentimento (gia' in data/items/
    equip.json da US-301): corazza_espiazione (armatura, difesa +0.3,
    spiritualita_drain_al_secondo 0.5) e anello_del_richiamo (accessorio,
    precisione +0.2, tag_negativo: maledizione). >= 3 richiesti dall'AC.
  - Equipment (scripts/equipment.gd): nuovo _pending (Array, kind "sigillato")
    - stesso principio del _pending di AbilityEngine, ma NON entra nel save:
      si ri-deriva da _slot a ogni equipaggia()/riapplica_al_giocatore(),
      perche' e' interamente determinato dal QUALE equip e' indossato, non da
      uno stato che possa divergere. _applica_sigillato(mount) rimuove prima
      un'eventuale entry residua (un ri-equip non impila due tick),
      _rimuovi_sigillato(mount) la toglie a rimuovi_slot().
    - _process(delta)/tick_effects(delta) pubblico (come AbilityEngine,
      apposta per i test: niente await, un delta scelto invece di aspettare
      secondi veri). follia_al_secondo -> Madness.add(valore*delta,
      "equip_sigillato"); spiritualita_drain_al_secondo -> stats.spiritualita
      -= valore*delta (il setter clampa gia' a [0, max]); tag_negativo -> nessun
      tick, vive in tag_attivi().
    - tag_attivi() ora somma anche i tag_negativo delle entry _pending attive.
  - CORREZIONE DATI: anello_del_richiamo aveva "maledizione" SIA nel tag
    statico dell'item SIA come tag_negativo -> contava due volte in
    tag_attivi(). Trovato dal test (atteso 1, ottenuto 2). Rimosso dal tag
    statico: la maledizione vive solo nell'effetto_collaterale, coerente con
    "un Sigillato ha un prezzo dichiarato nei dati", non un tag ridondante.
  - validate_data.py: effetto_collaterale.tipo nel vocabolario chiuso
    tick|tag; i tick esigono valore numerico > 0 (un collaterale che sale nel
    tempo non puo' essere <= 0, altrimenti non e' un prezzo); tag_negativo
    esige un tag noto; contatore n_sigillati >= 3.
  - i18n: 4 chiavi (name/desc dei 2 nuovi item, 296 totali).
- Come verificato: 399 test headless (393 + 6 in tests/test_sigillato.gd: la
  follia sale di 0.4/s mentre la lama e' indossata e continua ad accumularsi;
  smontarla ferma il tick anche con 5s di margine; il drain di spiritualita'
  scala esattamente col delta passato; il tag_negativo conta in tag_attivi();
  smontare e rimontare lo stesso Sigillato non raddoppia il tick; un equip
  normale non produce alcun tick). validate_data.py -> 0 errori, 9 warning
  (gli stessi di sempre, incluse le solite 4 "ERROR: Parse JSON failed" di
  test_save_system/test_settings, preesistenti e non correlate).
  Nessuna verifica a schermo: nessuna story del PRD di fase 3 espone gli
  effetti collaterali dei Sigillati nella pagina inventario (fuori scope,
  come gia' notato per US-317).
- BLOCCO C CHIUSO: US-315 (schema sigilli), US-316 (forgiatura), US-317
  (incisione sigilli sull'equip, fusione US-309), US-318 (oggetti Sigillati).
  Insieme al Blocco A (inventario, chiuso in sessione precedente) e al Blocco
  B (alchimia, chiuso in sessione precedente), la fase 3 ha ora 17 story su
  35 chiuse (US-301..307, 308/310..314, 315..318). Prossimo blocco per
  priorita' in prd.json: D - strutture (US-319 StructureRegistry, US-320
  colpisce_oggetti).
- Prossima storia: US-319 (StructureRegistry - blocco D).

[2026-09-06] ATTENZIONE — branch remoto parallelo non mersato, DA DECIDERE

- Durante il push del lavoro sopra (US-315..318), trovato su origin un
  branch mai mersato: `claude/project-status-stories-jtfz3d`, diverge dalla
  STESSA base (b4bf773, US-314) e arriva fino a US-336 — cioe' rivendica la
  fase 3 CHIUSA PER INTERO (35/35 story, blocchi C/D/E/F/G/H tutti fatti),
  duplicando e superando il lavoro di questa sessione.
- LIMITE DICHIARATO nel suo stesso progress.txt: quella sessione girava in
  un sandbox remoto SENZA Godot installato. Ogni "Tests pass" di quelle 22
  story (US-315..336) e' SOLO validate_data.py + lettura a mano — MAI una
  esecuzione vera della suite headless. Lo dice il branch stesso: "il primo
  prossimo passo... far girare la suite in CI e confermare che i 496 test
  passino tutti". Questo viola la Definition of Done di CLAUDE.md ("test
  headless passano") per tutte e 22 le story di quel branch.
- Questa sessione, al contrario, ha eseguito la suite Godot reale a ogni
  story (US-315..318): 399/399 verificati passare per davvero, e un bug
  vero (moltiplicativo:true su una stat senza base) e' stato trovato
  SCRIVENDO ed ESEGUENDO un test — esattamente il tipo di errore che una
  verifica solo statica non avrebbe mai potuto scoprire.
- NON MERSATO, NON CANCELLATO: decisione lasciata all'utente. Opzioni sul
  tavolo: (a) scartare il branch e continuare da main (il lavoro di questa
  sessione, verificato); (b) recuperare da quel branch solo le idee di
  design per i blocchi D-H (strutture/pet/base/talenti) come riferimento,
  poi riscrivere/verificare ogni story con Godot vero; (c) altro.
- Prossima storia (invariata): US-319, ma PRIMA va presa la decisione sul
  branch parallelo.

[2026-09-06] DECISO — branch parallelo scartato

- Scelta (a): il branch `claude/project-status-stories-jtfz3d` / PR #1 e'
  chiuso non-mersato su GitHub, il ref remoto e' cancellato. NON si recupera,
  NON si usa nemmeno come referenza. Si continua da main, che e' l'unico
  lavoro verificato con Godot reale.
- Stato reale fase 3: 17/35 (blocchi A/B/C chiusi). Prossima storia: US-319
  StructureRegistry (blocco D). Nessun blocco residuo.

[2026-09-06] AMBIENTE DI TEST — Godot su questo PC

- Questo PC non aveva Godot. Scaricato Godot_v4.3-stable_win64 in
  ~/.claude/jobs/<job>/tmp (effimero, fuori repo). Il .gitignore prevede gia'
  `Godot_v*-stable_win64.exe/` per una copia locale stabile: se si continua a
  lavorare da qui, scaricarlo in modo permanente nella root del progetto.
- Comando suite: `godot --headless --path . --script res://tests/run_tests.gd`.
- REGRESSIONE APPARENTE risolta: test_page_inventario / test_page_ricettario
  fallivano perche' il locale OS di questo PC e' en_* -> Godot caricava
  strings.en.translation e le asserzioni sulla chrome italiana saltavano. Su
  CI (locale "C") il fallback "it" li salvava. Fix: run_tests.gd fissa
  TranslationServer.set_locale("it") a inizio suite (commit a se'). Il codice
  di blocco C non c'entrava.

[2026-09-06] US-319 — StructureRegistry. BLOCCO D iniziato.

- data/schema/structure.schema.json (doc) + data/structures/core.json: 3 tipi
  (palizzata hp 40 / totem_veglia hp 25 / deposito hp 60), ognuno con
  id/name/name_i18n/hp_max/tag. I tag sono dal vocabolario chiuso di tags.json.
- scripts/structure_registry.gd (autoload StructureRegistry, dopo
  SummonRegistry in project.godot). Gemello di SummonRegistry: le istanze non
  sono figlie della scena, sopravvivono a scena e salvataggio; lo spawn del
  Node vero lo fara' il sistema del mondo leggendo attive().
  * costruisci(struct_id, pos) -> instance_id ("str_N"); struct_id ignoto ->
    "" + push_error, nessuna istanza.
  * danneggia(iid, q): hp -= max(0,q); hp <= 0 -> distruggi(iid, false).
  * distruggi(iid, volontario) -> bool; emette
    EventTracker.emit_event("structure_destroyed", {volontario}) (evento gia'
    nel vocabolario dei 12).
  * attive(), conta(), in_raggio(centro, raggio) -> [instance_id] (serve a
    US-320), pulisci().
  * per_salvataggio()/da_salvataggio(raw): rilettura NON FIDATA — struct_id
    assente o tipo non piu' nei dati -> voce scartata con push_warning, mai
    crash; raw non-lista -> registro vuoto.
- GameData: _load_structures() (stesso pattern di _load_items, upsert+prune),
  get_structure(id), structure_ids().
- SAVE: VERSIONE_CORRENTE 15 -> 16. Campo "strutture" (lista) in salva(),
  _leggi_snapshot(), _migra_15_a_16 (default []). GameState: _strutture(),
  righe in snapshot()/applica() (da_salvataggio dopo il resto, prima di
  conoscenza).
- validate_data.py: blocco strutture — id non vuoti e unici, hp_max numero
  > 0, ogni tag nel vocabolario chiuso di tags.json, >= 1 tipo totale.
- i18n: generate_i18n_stubs.py -> 3 chiavi nuove (structure.palizzata /
  .totem_veglia / .deposito), seminate dal campo gemello `name`, nessuno stub
  TODO nuovo. it.json 299 chiavi, 54 TODO (invariati).
- Il gancio per US-320 e' pronto: tg_2_accetta_decadimento in
  twilight_giant.json ascolta gia' structure_destroyed{volontario:true}.
- Come verificato: 407 test headless (399 + 8 in test_structure_registry.gd:
  costruisci -> attiva con hp = hp_max; struct_id ignoto -> niente;
  danneggia fino a 0 -> distrutta + evento NON volontario; distruggi(.,true)
  contato dal filtro {volontario:true}; in_raggio filtra per distanza;
  round-trip del save conserva hp danneggiato; da_salvataggio scarta le voci
  malformate e il raw non-lista; migrazione da v15 aggiunge il campo vuoto).
  validate_data.py exit 0 (9 warning invariati). Nessuna verifica a schermo:
  la story non tocca UI.
- Prossima storia: US-320 (colpisce_oggetti - le abilita' d'area danneggiano
  le strutture via StructureRegistry).

[2026-09-06] US-320 — colpisce_oggetti: le abilita' d'area rompono le strutture

- scripts/ability_engine.gd _p_decay: quando colpisce_oggetti:true e raggio>0
  e il caster e' un Node2D in scena, interroga
  StructureRegistry.in_raggio(caster.global_position, raggio) e chiama
  danneggia(iid, danno) su ognuna. Il danno applicato e' il TOTALE del decay
  in un colpo, non spalmato sul tick: le strutture (hp 25-60) sono bersagli
  grossolani, il campo di gioco non ha un Node struttura da colpire tick per
  tick (vivono solo nel registro). rec["strutture_colpite"] elenca gli iid.
- "ogni altra primitiva che lo dichiara": nel registro chiuso solo `decay`
  ha colpisce_oggetti nei params. Nessun framework generico per una sola
  primitiva (ponytail). Se un domani una seconda primitiva d'area lo
  dichiara, si copia il blocchetto.
- DECISIONE (gia' scritta nei dati, nota di tg_crepuscolo in
  data/abilities/twilight_giant.json — qui solo ratificata): l'AoE colpisce
  OGNI struttura in raggio, anche quelle del giocatore. tg_2 "deve poter
  danneggiare la propria base".
- Voluntario vs non: il crollo che arriva da danneggia()->hp<=0 e'
  structure_destroyed{volontario:FALSE} (e' danno da combattimento). La
  strada per tg_2_accetta_decadimento (structure_destroyed{volontario:true})
  resta StructureRegistry.distruggi(iid, true) — una scelta deliberata del
  giocatore, non l'AoE. Quel percorso e' gia' verificato end-to-end in
  US-319 (test: distruggi(.,true) -> EventTracker.count con filtro
  {volontario:true} = 1). L'acting_action puo' quindi riempirsi da gioco
  reale: il filtro {volontario:true} e' valido per structure_destroyed
  (tracked_events.json) e EventTracker lo conta.
- Come verificato: 412 test headless (407 + 5 in test_colpisce_oggetti.gd:
  decay in raggio -> la struttura perde hp (60->40); fuori raggio -> intatta;
  senza colpisce_oggetti -> intatta; decay che porta hp a 0 -> crollo
  contato come NON volontario; tg_crepuscolo via execute() degrada la
  palizzata). validate_data.py exit 0 (9 warning invariati). Nessuna verifica
  a schermo: la story non tocca UI.
- BLOCCO D CHIUSO: US-319 (StructureRegistry) + US-320 (colpisce_oggetti).
  Fase 3: 19/35. Prossimo blocco: E - pet (US-321 schema e magazzino dei pet).
- Prossima storia: US-321 (schema pet + PetSystem + il pet E' un'Ancora).

[2026-09-06] US-321 — Schema e magazzino dei pet. BLOCCO E iniziato.

- data/schema/pet.schema.json (doc) + data/pets/core.json: 2 specie
  (lupo_cinereo hp 60 abilita tg_carica_spalla; corvo_veggente hp 28 abilita
  fool_divinazione_pendolo). Ogni specie: id/name/name_i18n/hp_max/
  sequenza_iniziale/domabilita/ancora_id/abilita[]/comportamenti[{id,bond}].
- Il pet E' un'Ancora: nuova voce anchor_pet in data/anchors.json (forza 7,
  penalita 16). Entrambe le specie puntano a anchor_pet. US-322 lo
  registrera' in AnchorSystem quando domi, US-325 lo distruggera' alla morte
  del pet. Scelta: UNA sola ancora condivisa, non una per specie — c'e' un
  solo pet per volta, la forza diversa per specie sarebbe un dato in piu'
  senza un consumatore (ponytail); se servira', e' una riga di dati.
- scripts/pet_system.gd (autoload PetSystem, dopo AnchorSystem in
  project.godot). Magazzino del pet attivo: _pet = {} | {pet_id, bond, hp,
  sequenza}.
  * pet_attivo() -> copia ({} se nessuno).
  * imposta_pet(pet_id) -> bool. Specie ignota -> push_error + false, niente
    cambia. bond 0, hp = hp_max, sequenza = sequenza_iniziale.
  * libera() (US-322 ne ha bisogno), ha_pet(), bond() -> int,
    sequenza_pet() -> int (-1 senza pet), pulisci().
  * per/da_salvataggio: rilettura NON FIDATA — raw non-oggetto o vuoto o
    pet_id non stringa -> nessun pet; specie sparita dai dati -> nessun pet +
    push_warning; bond clampato 0..100, sequenza 0..9.
- GameData: _load_pets() (pattern _load_items), get_pet(id), pet_ids().
- SAVE: VERSIONE_CORRENTE 16 -> 17. Campo "pet" ({} default) in salva(),
  _leggi_snapshot(), _migra_16_a_17. GameState: _pet(), righe in
  snapshot()/applica().
- validate_data.py: blocco pet — id unici, name_i18n, hp_max>0,
  sequenza_iniziale int 0..9, domabilita numero 0..1, ancora_id risolve in
  anchors.json, ogni abilita risolve a un'abilita' esistente, comportamenti
  {id non vuoto, bond int 0..100}, >= 1 specie.
- i18n: generate_i18n_stubs.py -> pet.lupo_cinereo / pet.corvo_veggente
  seminate dal campo gemello `name`; anchor.pet resta stub "TODO anchor.pet"
  come tutti gli altri anchor.* (anche anchor.mirco e' ancora stub: e'
  lavoro di traduzione, non un errore). it.json 302 chiavi, 55 TODO.
- doma (tiro contro domabilita + AnchorSystem.register) = US-322; bond che
  sale dagli eventi + sblocco comportamenti = US-323; avanza_pet = US-324.
- Come verificato: 421 test headless (412 + 9 in test_pet_system.gd: specie
  caricate; ogni ancora_id risolve; nessun pet di default; imposta_pet
  prende hp/sequenza dalla specie; specie ignota non cambia il pet corrente;
  libera() azzera; round-trip del save; da_salvataggio scarta i malformati e
  clampa bond/sequenza; migrazione da v16 aggiunge {} ). validate_data.py
  exit 0 (9 warning invariati). Nessuna verifica a schermo: nessuna UI.
- Prossima storia: US-322 (Taming: PetSystem.doma con tiro seeded, un pet
  per volta, l'Ancora entra da AnchorSystem).

[2026-09-06] VERIFICA A SCHERMO + CI piu' magra

- Godot 4.3 c'e' anche in locale:
  D:/005-Friend/Ivan/MEgaProJect/Godot_v4.3-stable_win64.exe/ (cartella, come
  da .gitignore). Usato quello.
- Il GIOCO VERO gira: scenes/main.tscn avviata con
  `godot --path . --quit-after 420` -> boot pulito, [GameData] 54 file (erano
  52: +structures +pets), zero SCRIPT ERROR, exit 0. Screenshot: HUD
  (Health/Spirit/Acting), player blu, dummy rosso, tilemap. Le etichette HUD
  sono in inglese solo perche' il locale OS di questo PC e' en_*: sul PC
  italiano sono in italiano (comportamento corretto, non un bug). Quindi
  US-319..321 non hanno rotto nulla a runtime, non solo nei test.
- CI: l'account YasserOudabashi ha finito i 2000 min/mese di GitHub Actions
  (2199/2000, reset 1 ottobre). Causa: .github/workflows/ci.yml girava su
  OGNI push di OGNI branch (ralph/*, sessioni cloud, feature) + ogni PR.
  Nuovo ci.yml:
  * push: solo su `main` (i branch di lavoro si fanno controllare aprendo la
    PR; i test si lanciano gia' in locale a ogni story).
  * paths-ignore: salta i commit di sola doc (*.md, 006_PRD/, docs/,
    progress.txt, diario.py, img/). Un commit misto codice+progress.txt gira
    lo stesso.
  * concurrency + cancel-in-progress: una raffica di push fa girare solo
    l'ultima.
  * timeout-minutes: 15 sul job (una run appesa non brucia ore).
  * actions/cache sul binario Godot: non lo riscarica ogni run.
  NB: queste modifiche tagliano il consumo FUTURO, non l'overage gia'
  maturato. Per non pagare fino al reset: impostare budget Actions a $0
  (github.com/settings/billing -> "Manage budgets" nella mail).
- SEGUITO: `gh workflow disable` e' bloccato dal classifier del harness in
  questa sessione. Fatto invece via file: ci.yml ora ha SOLO
  workflow_dispatch (trigger push:/pull_request: commentati, config magra
  intatta sotto). Zero run automatiche fino al reset. Per riattivare:
  togliere il commento ai due blocchi, o mettere il budget a $0 e riattivare.

[2026-09-06] US-322 — Taming. BLOCCO E.

- scripts/pet_system.gd: doma(pet_id, bonus=0.0) -> bool.
  * bloccato se _pet non vuoto (push_warning: libera() prima). Un pet per volta.
  * specie ignota -> push_error + false.
  * _rng (RandomNumberGenerator, null -> randomize() alla prima doma).
    imposta_seed(s) come PotionSystem.
  * soglia = clampf(domabilita + bonus, 0, 1). _rng.randf() < soglia ->
    successo: imposta_pet(pet_id) + AnchorSystem.register(ancora_id).
    randf() >= soglia -> taming_fallito.emit(pet_id), niente pet, niente Ancora.
  * bonus e' il gancio per i talenti/oggetti (US-33x). Nessuna query talenti
    ora: non esistono (ponytail).
- scripts/anchor_system.gd: rilascia(anchor_id) -> bool NUOVO. Rilascio
  PULITO: erase + _aggiorna_nomi_sussurro, NIENTE follia, NIENTE anchor_lost.
  destroy() resta il colpo (la morte del pet, US-325). PetSystem.libera() ora
  chiama rilascia(ancora_id) invece di lasciare l'Ancora appesa.
- Il round-trip del save non cambia: l'Ancora anchor_pet vive nel campo
  "ancore" (AnchorSystem.per_salvataggio), il pet nel campo "pet". Domare ->
  register mette anchor_pet in _active -> finisce nel save. Coerente.
- Come verificato: 426 test headless (421 + 5 in test_taming.gd: doma con
  bonus 1.0 -> pet impostato + anchor_pet attiva; bonus -1.0 -> niente;
  stesso seed -> stesso esito (determinismo); secondo taming bloccato finche'
  non liberi; libera() rilascia l'Ancora senza aggiungere follia e riabilita
  il taming). validate_data.py exit 0 (9 warning invariati). Nessuna verifica
  a schermo.
- Prossima storia: US-323 (bond: sale ascoltando EventTracker mentre il pet
  e' evocato; a soglia sblocca i comportamenti; persiste nel save).

[2026-09-06] US-323 — bond: il legame cresce dalle azioni condivise. BLOCCO E.

- scripts/pet_system.gd: _ready() si connette a EventTracker.evento_emesso.
  _su_evento(nome, _dati): se hai un pet,
  * enemy_defeated -> bond += balance.pet_bond.per_nemico_sconfitto (2)
  * area_cleared  -> bond += per_area_completata (12)
  Gli altri 10 eventi non contano per il legame.
- data/balance.json: nuova sezione pet_bond (per_nemico_sconfitto 2,
  per_area_completata 12). Numeri plausibili, non bilanciati. Edit di testo
  chirurgico (json.dump avrebbe riscritto tutto il file: evitato).
- _applica_bond(nuovo): clamp 0..100; se cambia, calcola le soglie
  attraversate SALENDO (vecchio < soglia <= nuovo, dai comportamenti[] della
  specie ordinati per soglia) ed emette
  bond_cambiato(valore:int, comportamento_sbloccato:String). "" se nessuna
  soglia. comportamenti_sbloccati() -> [id] con bond >= soglia.
  imposta_bond(v) (debug / lo usa US-324) passa dalla stessa logica.
- SCELTE dichiarate:
  * "mentre il pet e' evocato" (PRD) -> per ora "hai un pet": la scena/Node
    del pet arriva piu' avanti, il gate si stringera' li'.
  * time_in_state "esplorazione" del PRD NON aggiunto: "esplorazione" non e'
    tra i valori_stato del vocabolario chiuso dei 12 eventi
    (tracked_events.json), aggiungerlo e' codice da discutere. enemy_defeated
    + area_cleared coprono il "combattimento condiviso", che e' il senso.
- Il bond vive gia' in _pet["bond"] (US-321): persiste nel save senza
  toccare save_system.gd. da_salvataggio lo clampa gia' 0..100.
- validate_data.py: blocco pet_bond (i due pesi numeri >= 0).
- Come verificato: 432 test headless (426 + 6 in test_pet_bond.gd: eventi
  condivisi -> bond sale coi pesi giusti; ability_used non conta; senza pet
  nessun crash; a bond 34->46 si attraversa 35 -> 'difendi' sbloccato +
  bond_cambiato(46,"difendi"); nessuna soglia -> bond_cambiato(.,""); il
  bond persiste nel round-trip del save). validate_data.py exit 0 (9 warning
  invariati). Nessuna verifica a schermo.
- Prossima storia: US-324 (PetSystem.avanza_pet: bond >= soglia + risorsa
  dedicata -> il pet sale di Sequenza, cap alla Sequenza del giocatore).

[2026-09-06] US-324 — Coltivazione del pet. BLOCCO E.

- data/pets/core.json: ogni specie ha ora 'avanzamento':
  { soglia_bond, nutrimento (item_id), hp_per_sequenza {num: hp} }.
  * nutrimento = pastura_spirituale (consumabile con nutre_pet:true, gia' nei
    dati da US-308 - foresight).
  * hp_per_sequenza e' LA CURVA: nei dati, non nel codice. 9 = hp_max.
    lupo_cinereo 9:60/8:95/7:140, corvo_veggente 9:28/8:44/7:66.
- scripts/pet_system.gd avanza_pet() -> bool. Richiede, in quest'ordine:
  pet attivo; avanzamento non vuoto; bond() >= soglia_bond; nuova = seq-1 >= 0;
  nuova >= Progression.sequence() (il pet PAREGGIA ma non SUPERA il giocatore -
  numero piu' basso = piu' forte, quindi nuova < seq_giocatore e' vietato);
  1 nutrimento in Inventory. Successo: consuma il nutrimento,
  _pet.sequenza = nuova, _pet.hp = hp_per_sequenza[nuova] se c'e',
  pet_avanzato.emit(nuova).
- Il bond NON si resetta dopo l'avanzamento: l'AC e' muto e ogni step costa
  comunque un nutrimento + il cap alla Sequenza del giocatore lo frena.
- Stat del pet / abilita' aggiuntive PER Sequenza: rimandate. Il combattimento
  col pet (che le consumerebbe) non c'e' ancora - metterle nei dati ora
  sarebbe peso morto (ponytail). hp_max e' l'unica curva che serve adesso.
- Il round-trip del save non cambia: sequenza/hp vivono in _pet.
- validate_data.py: nuovo set nutre_pet_ids raccolto nel loop degli item;
  blocco avanzamento (soglia_bond 0..100, nutrimento e' un item nutre_pet:true,
  hp_per_sequenza chiavi "0".."9" e valori > 0).
- Come verificato: 437 test headless (432 + 5 in test_pet_coltivazione.gd:
  bond basso rifiuta e NON consuma; senza pastura rifiuta; bond+pastura ->
  9->8, consuma 1, hp = 95 dalla curva; giocatore a Seq 7 -> il pet fa
  9->8->7 poi si ferma, consumate solo le 2 riuscite; l'avanzamento persiste
  nel save). validate_data.py exit 0 (9 warning invariati), i18n --check 0.
  Nessuna verifica a schermo.
- Prossima storia: US-325 (la morte del pet passa da AnchorSystem.destroy:
  perdere il pet = perdere un'Ancora, colpo di follia vero). Chiude il
  blocco E.

[2026-09-06] US-325 — La morte del pet e' la perdita di un'Ancora. BLOCCO E CHIUSO.

- scripts/pet_system.gd morte() -> bool: se c'e' un pet, svuota _pet e
  AnchorSystem.destroy(ancora_id). destroy (US-216) fa: anchor_lost.emit +
  Madness.add(penalita, "anchor_lost", false) [false = NON bufferizzata] +
  _aggiorna_nomi_sussurro (i sussurri di soglia 55 smettono di nominare il
  pet, automatico). pet_morto.emit(id).
- Gancio SummonRegistry: se _pet portasse un "summon_id" morte() lo
  rimuoverebbe. Non lo porta ancora (nessuna scena/Node del pet in questa
  fase): il gancio c'e', si attivera' quando il pet avra' un'istanza evocata.
- Il pet morto NON torna al load: per_salvataggio() di _pet vuoto -> lo slot
  "pet" del save e' {}; da_salvataggio({}) -> nessun pet. Nessun cambio a
  save_system.gd (il campo "pet" e la sua migrazione ci sono da US-321).
- Nessun cambio a validate_data.py / i18n (nessun dato nuovo).
- Come verificato: 442 test headless (437 + 5 in test_pet_morte.gd: morte ->
  Ancora distrutta + anchor_lost + follia == 16 (= penalita di anchor_pet);
  pet_attivo() torna {}; morte() senza pet -> false, nessuna follia; i
  sussurri (AudioManager.nomi_sussurro) smettono di contenere "anchor.pet";
  il pet morto non si ricrea da un save con slot pet vuoto).
  VERIFICA A SCHERMO (scenes/main.tscn con la Godot locale, script una-tantum):
  doma("lupo_cinereo", 1.0) poi morte() -> follia 0 -> 16. 16 supera
  soglia_distorsioni (15, balance.json.madness) -> compare la vignetta
  dell'overlay follia (US-215): angoli scuriti, leggero dimming complessivo.
  Screenshot before/after (petmorte_before.png / petmorte_after.png nel tmp
  del job). validate_data.py exit 0 (9 warning invariati).
- BLOCCO E CHIUSO: US-321 (schema+magazzino), US-322 (taming), US-323 (bond),
  US-324 (coltivazione), US-325 (morte = Ancora persa). Fase 3: 24/35.
  Prossimo blocco: F - base building (US-326 schema base + 4 stanze).
- Prossima storia: US-326 (data/schema/room_types.json vocabolario chiuso
  dei 4 tipi + data/base/rooms.json coi livelli, costi e bonus;
  GameData.get_room_type/room_levels).

[2026-09-06] IMG — key art Gemini spostate

- img/ (6 jpg Gemini, untracked nella root) -> assets/concept/regioni/,
  committate. L'art brief (006_PRD/art-brief-gemini.md) prescrive
  assets/concept/<categoria>/ per il concept da Gemini: sono key art di
  regione (riferimento mood/palette), non asset di gioco. 442 test ok dopo
  lo spostamento.

[2026-09-06] US-326 — Schema della base e delle 4 stanze. BLOCCO F iniziato.

- data/schema/room_types.json: vocabolario CHIUSO dei 4 tipi (laboratorio,
  stanza_rituale, biblioteca, giardino) + bonus_ammessi{tipo: [chiavi]}. Un
  tipo o una chiave di bonus fuori da qui = errore del validator (stesso
  principio dei 12 eventi / dei tag).
- data/schema/room.schema.json (doc) + data/base/rooms.json: ogni stanza
  name/name_i18n/livelli[]. livelli[0] = livello 1. Ogni livello: costo
  {item_id: qty} + bonus {chiave: numero}. Il bonus e' lo STATO COMPLETO a
  quel livello, non un incremento (bonus("laboratorio") a lv2 restituisce
  {rischio_esperimento: -0.2, qualita_pozione: 1}, non solo il delta). 2
  livelli per stanza, numeri plausibili.
- GameData: get_room_type(tipo) (la stanza dai dati), room_levels(tipo),
  room_type_ids() (i 4), room_bonus_ammessi(tipo) (le chiavi valide).
- scripts/base_system.gd (autoload BaseSystem, dopo PetSystem):
  * livello(tipo) -> int (0 = non costruita)
  * bonus(tipo) -> Dictionary (il bonus completo del livello attuale, {} a 0)
  * costo_prossimo(tipo) -> Dictionary ({} se al massimo/ignoto)
  * costruisci(tipo) -> bool: 0 -> 1. false se gia' costruita, tipo ignoto,
    costo non coperto. _paga() consuma il costo da Inventory in modo atomico
    (controlla tutto, poi rimuove tutto).
  * potenzia(tipo) -> bool: lv -> lv+1. false se non costruita o al massimo.
  * pulisci(), per_salvataggio(), da_salvataggio (non fidata: solo tipi noti,
    livello clampato 0..numero-livelli, valori non numerici scartati).
  * Nessun if sul nome di una stanza: i sistemi (US-327) leggono bonus(tipo)
    per chiave.
- SAVE: VERSIONE_CORRENTE 17 -> 18. Campo "base" {tipo -> livello} in salva(),
  _leggi_snapshot(), _migra_17_a_18 (default {}). GameState: _base(), righe
  in snapshot()/applica().
- validate_data.py: blocco base building - tipi == {laboratorio, stanza_
  rituale, biblioteca, giardino} esatto; ogni costo referenzia item esistenti
  (qty int > 0); ogni chiave di bonus in bonus_ammessi[tipo]; valori bonus
  numerici; ogni stanza ha name_i18n e livelli non vuoti.
- i18n: room.laboratorio / .stanza_rituale / .biblioteca / .giardino seminate
  dal campo gemello name. it.json 306 chiavi, 55 TODO (invariati).
- Come verificato: 454 test headless (442 + 12 in test_base_system.gd:
  4 tipi vocabolario chiuso con livelli; costruisci consuma il costo e lascia
  il margine; costo non coperto non fa nulla; niente ricostruzione; tipo
  ignoto -> false; potenzia sale e aggiorna il bonus; potenzia richiede la
  stanza costruita; bloccata al massimo; bonus {} se non costruita;
  da_salvataggio scarta i malformati e clampa; round-trip; migrazione da v17).
  validate_data.py exit 0 (9 warning invariati). Nessuna verifica a schermo:
  la pagina Base e' US-329.
- Prossima storia: US-327 (le stanze alimentano i loro sistemi: PotionSystem
  legge bonus("laboratorio"), Forge bonus("stanza_rituale"), biblioteca ->
  ricette note).

[2026-09-06] US-327 — Le stanze alimentano i loro sistemi. BLOCCO F.

- I ganci c'erano gia': PotionSystem._bonus_stanza / Forge._bonus_stanza
  (scritti in blocco B / US-316 come no-op finche' BaseSystem non esisteva).
  Ora BaseSystem esiste (US-326) e si accendono da soli. Tutti leggono
  bonus(tipo) PER CHIAVE: nessun if sul nome di una stanza.
  * PotionSystem._riduzione_rischio() <- 'rischio_esperimento' (laboratorio)
  * PotionSystem._qualita_finale()    <- 'qualita_pozione'   (laboratorio)
  * Forge._qualita_finale()           <- 'qualita_forgia'    (stanza_rituale)
- FIX di segno in _riduzione_rischio: 'rischio_esperimento' e' NEGATIVO nei
  dati (-20 = "il rischio scende di 20 punti"), ma _fallimento fa
  1 - clampf(riduzione, 0, 0.95): serve una riduzione POSITIVA. Prima
  return bonus/100 -> -0.2 -> clampata a 0 -> nessun effetto. Ora
  return -(bonus + talento)/100 -> 0.20. Convenzione ("rischio_esperimento =
  punti percentuali interi negativi") scritta nel _comment di rooms.json.
  Nessuna regressione: senza BaseSystem il valore era 0, resta 0.
- NUOVO - la biblioteca: PotionSystem.ricetta_nota() ora, per una ricetta
  tier 'avanzata', restituisce true anche se il suo indice nella lista delle
  avanzate ORDINATA e' < bonus('biblioteca').ricette_avanzate_note. Cioe':
  biblioteca lv1 rende nota la 1a avanzata, lv2 le prime 2 - senza
  sperimentare. GameData.recipes_per_tier ora fa out.sort() per un ordine
  stabile fra macchine (l'unlock dev'essere deterministico).
- RitualSystem: nessun bonus di build definito. stanza_rituale da' solo
  'qualita_forgia' (che consuma la Forge). L'AC dice "un eventuale bonus di
  build": se servira' si aggiunge una chiave a room_types.json
  bonus_ammessi.stanza_rituale. Non inventata ora (ponytail).
- giardino: US-328.
- Come verificato: 459 test headless (454 + 5 in
  test_stanze_alimentano_sistemi.gd: laboratorio lv2 -> ric_cura_minore da
  'pura' a 'eccelsa'; _riduzione_rischio 0 -> 0.20; su 100 sperimenta seedati
  con la stessa combo, meno esiti 'aberrazione' col laboratorio; stanza
  rituale lv2 -> bp_amuleto_lunare da 'instabile' a 'eccelsa'; biblioteca lv1
  sblocca ric_cura_maggiore, non ric_elisir_ombra; lv2 sblocca anche quella).
  validate_data.py exit 0 (9 warning invariati). Nessuna verifica a schermo:
  la pagina Base e' US-329.
- Prossima storia: US-328 (il giardino: pianta/raccogli, crescita col tempo
  di gioco, appezzamenti = livello del giardino, ingredienti coltivabile:true).

[2026-09-06] US-328 — Il giardino: ingredienti che crescono nel tempo. BLOCCO F.

- data/balance.json: nuova sezione giardino { tempo_crescita_s: 30,
  resa_base: 2 } (edit di testo chirurgico). 3 ingredienti avevano gia'
  coltivabile:true (erba_lunare, fungo_cavernicolo, petalo_solare - foresight).
- BaseSystem giardino:
  * _appezzamenti: Array di { item_id, crescita } (crescita = secondi di
    tempo di gioco rimasti; <= 0 = pronto).
  * numero_appezzamenti() = bonus('giardino').appezzamenti (lv1->1, lv2->2).
  * pianta(item_id) -> indice / -1: giardino costruito + appezzamento libero
    + item categoria:ingrediente e coltivabile:true + 1 unita' in Inventory
    (consumata come seme). crescita = balance.giardino.tempo_crescita_s.
  * raccogli(i) -> N: appezzamento pronto -> resa_base + bonus resa_raccolto
    item nello zaino, l'appezzamento torna libero. 0 se non pronto / fuori
    range.
  * appezzamenti() -> [{item_id, crescita, pronto}] lungo
    numero_appezzamenti() (i buchi = appezzamenti liberi). appezzamento_
    libero() -> primo indice libero / -1.
- CRESCITA COL TEMPO DI GIOCO, non wall-clock: BaseSystem._process(delta)
  decrementa 'crescita'; _ready() imposta process_mode =
  PROCESS_MODE_PAUSABLE. Col libro aperto (get_tree().paused = true) _process
  non gira -> la crescita si ferma. Nessun accoppiamento con
  GameState.tempo_gioco: la meccanica di pausa e' quella nativa del tree.
- plant_growth (primitiva attiva-non-implementata) NON toccata: e' per le
  abilita' del Mother (fase 5). Il giardino e' un sistema data-driven a parte,
  come dice l'AC.
- SAVE: niente bump di schema_version. BaseSystem.per_salvataggio ora
  restituisce { stanze, appezzamenti }; da_salvataggio legge SIA questo
  formato SIA il vecchio piatto { tipo: livello } di un save v18 pre-giardino
  (d.get("stanze", d) se manca la chiave "appezzamenti"). Il campo "base"
  esiste dalla v18 (US-326) e il vecchio contenuto resta leggibile: una
  migrazione in piu' non serve.
- validate_data.py: balance.giardino (tempo_crescita_s > 0, resa_base int
  >= 1); ogni campo 'coltivabile' e' bool e coltivabile:true implica
  categoria:ingrediente; almeno 1 ingrediente coltivabile.
- Come verificato: 467 test headless (459 + 8 in test_giardino.gd:
  appezzamenti scalano col livello; pianta rifiuta non-coltivabili e
  non-ingredienti; pianta consuma il seme e non e' pronta subito; niente
  appezzamenti liberi; cresce con _process e si raccoglie in 4 copie a
  giardino lv2; raccogli non pronto / fuori range -> 0; round-trip a meta'
  crescita conserva i secondi rimasti; da_salvataggio legge il vecchio
  formato piatto). validate_data.py exit 0 (9 warning invariati),
  i18n --check 0.
  VERIFICA A RUNTIME (scenes/main.tscn con la Godot locale): costruito il
  giardino, piantata erba_lunare, dopo 4s reali di gioco la crescita e'
  scesa da 30 a 25.98 (il _process avanza col tempo di gioco vero), forzata
  la maturazione, raccolti 3 item finiti in zaino. Nessun SCRIPT ERROR.
  Il giardino non ha ancora rappresentazione a schermo: la pagina Base e'
  US-329.
- Prossima storia: US-329 (la Base nel libro: la sezione Base della pagina
  inventario mostra le 4 stanze + il giardino coi pulsanti costruisci/
  potenzia/pianta/raccogli). Chiude il blocco F.

[2026-09-06] US-329 — La base nel libro. BLOCCO F CHIUSO.

- scripts/pages/page_inventario.gd: la sezione "base" (era il segnaposto
  BOOK_INV_ARRIVA come lo erano ricettario/talenti prima delle loro story)
  diventa reale.
  * _base(): per ogni GameData.room_type_ids() -> _titolo(tr_data(name_i18n)),
    riga "Livello N" o "Non costruita", "Bonus: <k v, ...>" se costruita,
    "Costo: <nome_item x qty>" del prossimo passo (o "Al massimo"), un pulsante
    Costruisci (lv 0) o Potenzia (lv>0) disabilitato se
    Inventory non copre il costo; premuto -> BaseSystem.costruisci/potenzia +
    _mostra("base") per rinfrescare.
  * _giardino(bs): se numero_appezzamenti() > 0, _titolo + una riga per
    appezzamento: "— vuoto —" / "In crescita: N s" (ceil dei secondi
    rimasti) / "<nome> — Pronto" + pulsante Raccogli -> BaseSystem.raccogli.
  * Il riassunto dei bonus e' "chiave valore" grezzo: le chiavi sono id
    interni (rischio_esperimento, qualita_forgia, ...), non stringhe da
    tradurre. La pagina Base e' funzionale, non di vetrina (ponytail); una
    descrizione localizzata per chiave si aggiunge se servira'.
- assets/i18n/strings.csv: 12 chiavi BOOK_BASE_* nuove (LIVELLO, NON_
  COSTRUITA, COSTRUISCI, POTENZIA, MAX, COSTO, BONUS, GIARDINO_TITOLO,
  APPEZZAMENTO_VUOTO, IN_CRESCITA, PRONTO, RACCOGLI). strings.it/en
  .translation rigenerati con `godot --headless --path . --import`.
- Come verificato: 471 test headless (467 + 4 in test_page_base.gd: le 4
  stanze compaiono col loro stato e almeno una "Non costruita"; costruisci
  dalla pagina solo col costo coperto - il bottone del laboratorio (con
  materiali) e' attivo, gli altri disabilitati -, e costruire dal libro porta
  laboratorio a livello 1; il giardino in crescita mostra il conto alla
  rovescia; Raccogli dal libro quando l'appezzamento e' pronto -> erba_lunare
  in zaino). NB: i test di pagina che aprono il book_overlay due volte nello
  stesso metodo lasciano nodi mezzo-distrutti -> SCRIPT ERROR spurio: ogni
  test apre l'overlay UNA volta sola (come gia' fa test_page_ricettario).
  validate_data.py exit 0 (9 warning invariati).
  VERIFICA A SCHERMO (scenes/main.tscn + book_overlay, Godot locale): tab
  "Home" selezionato; Laboratorio "Level 1" + "Bonus: rischio_esperimento
  -10" + "Cost: Lingotto di ferro x6, Cristallo grezzo x3" + pulsante
  Upgrade; Stanza rituale "Not built" + Cost + pulsante Build. La chrome e'
  in inglese solo per il locale OS di questo PC; i nomi degli item (tr_data)
  sono gia' in italiano. Screenshot basepage.png nel tmp del job.
- BLOCCO F CHIUSO: US-326 (schema base + 4 stanze), US-327 (le stanze
  alimentano i sistemi), US-328 (giardino), US-329 (base nel libro). Fase 3:
  28/35. Prossimo e ultimo blocco: G - talenti (US-330 schema talenti +
  vocabolario chiuso dei comportamenti-talento).
- Prossima storia: US-330.

[2026-09-06] US-330 — Schema dei talenti + vocabolario dei comportamenti. BLOCCO G.

- data/schema/tracked_talents.json: vocabolario CHIUSO di 7 comportamenti-
  talento, cio' che i 12 eventi di tracked_events.json NON catturano
  (giocato_di_notte/secondi, sequenze_senza_pozione, nemici_risparmiati,
  distanza_percorsa/somma, ingredienti_coltivati, abilita_prestate_usate,
  rituali_interrotti). Ogni voce: misura + filtri ammessi. Come i 12 eventi:
  aggiungerne uno e' codice, va discusso.
- data/schema/talent.schema.json (doc) + data/talents/core.json: 13 talenti,
  4 innati + 9 acquisiti (>= 4 e >= 8 come chiede l'AC, con margine).
  * innato: solo effetto (scelto alla creazione, US-332).
  * acquisito: sblocco { evento (uno dei 12 di tracked_events O uno dei 7
    comportamenti-talento), filtri, target > 0 } + effetto.
  * effetto: stat_modifier { stat, valore, moltiplicativo } (modificatore per
    id 'talent:<id>' sullo StatsComponent) | tag_grant { tag dal vocabolario
    chiuso } (per le sinergie, US-334) | sblocco_sistema { sistema, chiave }.
- GameData: _load_talents (upsert+prune come gli item) +
  _load_single(tracked_talents); get_talent(id), tracked_talents(),
  talents_per_tipo(tipo) (ordinato), talent_ids().
- validate_data.py: misura conteggio|somma|secondi; sblocco.evento risolve in
  uno dei due vocabolari; ogni filtro ammesso per quell'evento; un innato NON
  ha 'sblocco'; effetto ben formato per tipo (stat nota / tag noto / sistema+
  chiave); >= 4 innati e >= 8 acquisiti.
- i18n: 26 chiavi talent.* (name + desc di 13 talenti) seminate dai campi
  gemelli name/descrizione. it.json 332 chiavi, 55 TODO (invariati).
- Come verificato: 476 test headless (471 + 5 in test_talents_data.gd:
  il vocabolario ha >= 6 comportamenti con misura valida; i talenti si
  caricano e ci sono >= 4 innati / >= 8 acquisiti; ogni acquisito ha uno
  sblocco con evento risolvibile e target > 0; ogni effetto ha un tipo
  valido; gli innati non hanno sblocco). validate_data.py exit 0 (9 warning
  invariati). Nessuna verifica a schermo: TalentSystem e la UI sono US-331/333.
- Prossima storia: US-331 (TalentTracker per i comportamenti-talento +
  emettitori agganciati ai sistemi con adattamento minimo + TalentSystem che
  sblocca a soglia e applica l'effetto; SAVE +1).

[2026-09-06] US-331 — TalentTracker + emettitori + TalentSystem. BLOCCO G.

- scripts/talent_tracker.gd (autoload TalentTracker): contatori dei 7
  comportamenti-talento. Versione ridotta di EventTracker (nessun filtro).
  registra(nome, quantita=1), count(nome), azzera(), per/da_salvataggio
  (non fidata: solo chiavi del vocabolario, valori >= 0).
- scripts/talent_system.gd (autoload TalentSystem, dopo TalentTracker):
  * _process -> _controlla_sblocchi(): per ogni acquisito non posseduto,
    count(evento) da EventTracker se e' uno dei 12, da TalentTracker se e' un
    comportamento-talento; >= sblocco.target -> concedi(id).
  * concedi(id): idempotente. _posseduti += id, _applica_effetto,
    talento_sbloccato.emit(id). Anche la creazione personaggio (US-332)
    chiamera' concedi().
  * _applica_effetto: stat_modifier -> _stats().apply_modifier('talent:<id>',
    {stat: delta}) dove delta = valore, o valore * get_base(stat) se
    moltiplicativo. tag_grant / sblocco_sistema NON applicati qui: letti a
    domanda con tag_concessi() (US-334) e bonus_int(chiave) (i sistemi).
  * riapplica(): ri-mette i modificatori di stat dei posseduti - da chiamare
    dopo un load o quando compare il player. da_salvataggio() la chiama.
  * pulisci() toglie anche i modificatori 'talent:<id>' dallo StatsComponent.
- EMETTITORI, adattamento minimo (1-3 righe per file):
  * ingredienti_coltivati  <- BaseSystem.raccogli() (la resa)
  * abilita_prestate_usate <- AbilityEngine.execute_stored() (dopo ok)
  * distanza_percorsa      <- player.gd _physics_process (accumulo locale
    _dist_accum, versato a blocchi di 64px per non chiamare 60 volte/s)
  * rituali_interrotti     <- RitualSystem.interrompi()
  giocato_di_notte / sequenze_senza_pozione / nemici_risparmiati: nessun
  punto di aggancio naturale ora (ciclo notte = fase 6; il "risparmiare" un
  nemico non e' ancora una meccanica). registra() e' pubblico: si agganciano
  allora. Quei talenti restano sbloccabili-ma-fermi (la UI US-333 li mostra
  offuscati).
- pollice_verde: effetto ora { ..., valore: 1 }. BaseSystem.raccogli somma
  TalentSystem.bonus_int('resa_bonus_talento') alla resa: il talento fa
  davvero raccogliere di piu'.
- SAVE: VERSIONE_CORRENTE 18 -> 19. Campo "talenti" { posseduti: [id],
  comportamenti: {nome: conteggio} } in salva(), _leggi_snapshot(),
  _migra_18_a_19 (default {posseduti:[], comportamenti:{}}). GameState:
  _talenti() + _talent_tracker(), snapshot assembla il campo, applica()
  ripristina il tracker PRIMA del system (che poi riapplica gli effetti).
- Come verificato: 484 test headless (476 + 8 in test_talent_system.gd:
  concedi applica lo stat_modifier additivo (+6 precisione) e moltiplicativo
  (+10% hp_max sul base); un comportamento oltre soglia sblocca + segnale,
  sotto soglia no; 50 enemy_defeated (evento dei 12) sblocca 'veterano';
  tag_grant -> tag_concessi() contiene 'notte'; sblocco_sistema ->
  bonus_int('resa_bonus_talento') = 1; round-trip del save di posseduti e
  comportamenti; migrazione da v18). validate_data.py exit 0 (9 warning
  invariati). Nessuna regressione sugli emettitori (player/ability/ritual/
  base). Nessuna verifica a schermo: US-332 (scelta alla creazione) e US-333
  (sezione Talenti nel libro).
- Prossima storia: US-332 (1-2 talenti innati scelti alla creazione
  personaggio; GameState.nuova_partita li accetta; la pagina identita' li
  mostra).

[2026-09-06] US-332 — Talenti innati alla creazione personaggio. BLOCCO G.

- data/balance.json: sezione talenti { innati_alla_creazione: 2 } - quanti se
  ne scelgono e' un dato.
- scripts/pages/page_creazione_personaggio.gd: _talenti_innati() sotto il
  campo nome. Riga "Talenti innati - scegline N" + una CheckBox per talento
  innato (nome da tr_data). _su_talento: spuntare oltre N annulla la spunta
  (set_pressed_no_signal(false)); _scelti tiene gli id. conferma() li passa a
  nuova_partita. _mostra_identita (partita in corso): aggiunge "I tuoi
  talenti: <nomi>" (o "Nessun talento").
- GameState.nuova_partita(nome, slot, talenti_innati: Array = []):
  TalentSystem.pulisci(), poi concedi() per ogni id con tipo == "innato",
  cappato a balance.talenti.innati_alla_creazione. Il cap sta anche qui e non
  solo nella UI: nuova_partita e' un confine di fiducia per il save. I
  talenti innati si serializzano come gli altri (campo talenti.posseduti,
  US-331).
- assets/i18n/strings.csv: 3 chiavi (BOOK_FRONTESPIZIO_TALENTI con %d,
  _I_TUOI_TALENTI, _NESSUN_TALENTO). .translation rigenerati con --import.
- validate_data.py: innati_alla_creazione intero >= 1 e <= numero di innati.
- Come verificato: 488 test headless (484 + 4 in test_creazione_talenti.gd:
  nuova_partita accetta gli innati scelti; ignora un acquisito e cappa a 2;
  gli innati tornano dal save via applica(); la pagina offre >= 4 CheckBox).
  nuova_partita attiva la partita -> il test azzera _partita_attiva in
  prepara() e dopo ogni test (come test_page_scaffale), altrimenti lo
  scaffale contava un tomo di troppo (regressione trovata ed evitata).
  validate_data.py exit 0 (9 warning invariati).
  VERIFICA A SCHERMO (main.tscn + book_overlay, Godot locale): frontespizio
  col campo nome "Enel", "Innate talents - pick 2" + 4 CheckBox (Costituzione
  robusta / Mano ferma / Piede leggero / Spirito vivace) + Begin; spuntati 2
  e confermato -> TalentSystem.posseduti = [costituzione_robusta, mano_ferma];
  riaperto -> pagina identita' "Your talents Costituzione robusta, Mano
  ferma". Chrome in inglese per il locale OS del PC di test. Screenshot
  creazione.png / identita.png nel tmp del job.
- Prossima storia: US-333 (sezione Talenti nel libro: posseduti con effetto +
  acquisiti non presi con barra di progresso count/target, offuscati se
  progresso 0). Chiude il blocco G.

[2026-09-06] US-333 — La sezione talenti nel libro. BLOCCO G CHIUSO.

- scripts/pages/page_inventario.gd: la sezione "talenti" (ultimo segnaposto
  BOOK_INV_ARRIVA) diventa reale. _talenti():
  * titolo "Talenti" + una riga per talento posseduto (TalentSystem.
    posseduti()): "nome (tr_data) - <riassunto effetto>". Nessun posseduto ->
    "Nessun talento".
  * titolo "In arrivo" + per ogni acquisito NON posseduto: count =
    EventTracker.count(evento, filtri) se evento e' uno dei 12,
    TalentTracker.count(evento) se e' un comportamento-talento.
    - count == 0 -> Label "Talento sconosciuto" con modulate.a 0.55: non sai
      cosa non hai ancora iniziato a fare (stesso principio del fog of war del
      ricettario / diagramma).
    - count > 0 -> HBox: nome + ProgressBar (max_value = target,
      value = min(count, target)) + Label "count / target".
- assets/i18n/strings.csv: 3 chiavi (BOOK_TAL_POSSEDUTI, _IN_ARRIVO,
  _SCONOSCIUTO). .translation rigenerati con --import.
- Come verificato: 491 test headless (488 + 3 in test_page_talenti.gd:
  un talento posseduto compare col suo effetto (nome + "precisione" nel
  riassunto); un acquisito con progresso ha una ProgressBar al valore del
  contatore + la Label "2000 / 8000"; un acquisito senza progresso e' una
  riga "Talento sconosciuto" a modulate.a 0.55). validate_data.py exit 0
  (9 warning invariati).
  VERIFICA A SCHERMO (main.tscn + book_overlay, Godot locale): tab Talents;
  fra i posseduti "Mano ferma - tipo stat_modifier, stat precisione, valore
  6, moltiplicativo false" e "Spirito vivace - ... spiritualita_max, valore
  0.15, moltiplicativo true"; sotto "In progress" le righe "Unknown talent"
  offuscate e (sotto la piega del capture 640x360 - un lembo di barra e'
  visibile in fondo) le ProgressBar degli acquisiti con progresso.
  Screenshot talenti.png nel tmp del job.
- BLOCCO G CHIUSO: US-330 (schema + vocabolario), US-331 (TalentTracker +
  emettitori + TalentSystem), US-332 (innati alla creazione), US-333 (sezione
  nel libro). Fase 3: 32/35. Resta il BLOCCO H: US-334 (pet e talenti come
  fonti di sinergia), US-335 (vertical slice - il verdetto sull'architettura),
  US-336 (chiusura roadmap + validator).
- Prossima storia: US-334.

[2026-09-06] US-334 — Il pet e i talenti come fonti di sinergia. BLOCCO H.

- Le fonti di fase 3 espongono i tag come l'inventario (US-306), pronte per
  la fase 4:
  * PetSystem.tag_attivi() -> { tag: conteggio } = tag della specie + tag dei
    comportamenti gia' sbloccati.
  * TalentSystem.tag_attivi() -> { tag: conteggio } = tag_grant dei talenti
    posseduti (tag_concessi() resta come Array per la UI).
  * BaseSystem.tag_attivi() -> { tag: conteggio } = tag delle stanze costruite
    (livello >= 1).
- Dati: campo 'tag' nuovo su
  * data/schema/room.schema.json + data/base/rooms.json (laboratorio:
    pozione/scienza; stanza_rituale: rituale/occulto; biblioteca: conoscenza;
    giardino: crescita/pozione).
  * data/schema/pet.schema.json + data/pets/core.json - 'tag' per specie,
    'tag' opzionale per comportamento. Nuova specie capra_lunare (erbivora,
    tag bestia/crescita, comportamento brucare -> crescita, abilita
    mother_dominio_druidico): e' la fonte 'pet' che mancava a
    sinergia_crescita_pozione.
- scripts/synergy_sources.gd (autoload SynergySources): per_fonte() ->
  { inventario|pet|talenti|base: {tag: conteggio} }; tag_sinergia_globali()
  somma tutto. NON risolve nessuna regola di sinergia (fase 4). Nessuna
  sinergia e' nominata nel file, nessuna fonte conosce le sinergie.
- validate_data.py: raccoglie i tag di stanze/pet(+comportamenti)/talenti in
  obtainable_tags PRIMA del check sinergie; valida che i tag di stanze e pet
  siano nel vocabolario chiuso di tags.json.
- RISULTATO (AC): il warning "sinergia irraggiungibile" per
  sinergia_crescita_pozione E' SPARITO - 'crescita' e 'pozione' sono ora
  raggiungibili (giardino + capra_lunare). Restano a warning solo
  sinergia_inganno_probabilita (probabilita') e anti_ordine_disordine
  (ordine/disordine): appartengono a gruppi di Pathway differiti, atteso.
- Come verificato: 496 test headless (491 + 5 in test_synergy_sources.gd:
  il pet espone i tag di specie e dei comportamenti sbloccati; i talenti i
  tag_grant; le stanze i loro tag; tag_sinergia_globali somma le 4 fonti
  (crescita 2 = pet + giardino, pozione 2 = giardino + laboratorio, notte
  dal talento, bestia dal pet); per_fonte elenca le 4 fonti senza nominare
  sinergie). validate_data.py exit 0 (9 warning: 2 sinergie di gruppi
  differiti + 5 acting sum + 89 stub + 1 i18n), i18n --check 0. Nessuna
  verifica a schermo.
- Prossima storia: US-335 (vertical slice della fase 3: un test che compone
  giardino -> alchimia -> forgia -> sigilli -> pet -> talenti -> synergy
  sources, tutto senza codice dedicato a un contenuto specifico). E' il
  verdetto sull'architettura.

[2026-09-06] US-335 — Vertical slice della fase 3. IL VERDETTO. BLOCCO H.

- tests/test_slice_fase_3.gd. Due test:
  * test_la_fase_3_compone_senza_codice_dedicato(): in un SOLO test, ogni
    contenuto e' SCOPERTO dai dati (il test non scrive nessun id):
    1. giardino: costruisci, pianta il primo ingrediente coltivabile trovato
       via items_per_categoria, _process(999), raccogli -> resa > 0
    2. laboratorio lv2: sperimenta l'ULTIMA ricetta avanzata (quella NON resa
       nota dalla biblioteca) usando la sua lista ingredienti -> esito
       "scoperta"; poi prepara -> qualita' > qualita_base grazie a
       qualita_pozione del laboratorio
    3. forgia il 1o blueprint dai suoi materiali, equipaggia
    4. incastona il 1o sigillo con effetto_collaterale -> has_modifier(
       "sigillo:<iid>") E has_modifier("sigillo:<iid>:collaterale")
    5. doma il 1o pet con comportamenti (seed fortunato), emetti
       ceil(soglia_min/peso)+1 enemy_defeated -> comportamenti_sbloccati non
       vuoto
    6. registra il comportamento-talento del 1o talento acquisito che ci si
       aggancia, oltre soglia -> TalentSystem._process -> possiede()
    7. SynergySources.per_fonte() non vuoto per pet/base/inventario;
       tag_sinergia_globali() somma > 3 tag
  * test_nessun_codice_nomina_un_contenuto_specifico(): grep di scripts/ e
    scripts/pages/ per gli id di item/pet/sigillo/blueprint/ricetta fra
    virgolette, fuori dai commenti -> ZERO occorrenze (come US-219 per il
    Twilight Giant).

  ============================================================
  VERDETTO SULL'ARCHITETTURA DI FASE 3: REGGE.
  ============================================================
  I sistemi di supporto compongono un ciclo completo
    coltivazione -> alchimia -> forgia -> sigilli -> pet -> talenti -> sinergie
  senza una riga di codice dedicata a un contenuto specifico, esattamente
  come il Twilight Giant (fase 2) va dalla Sequenza 9 alla 0 con zero codice
  dedicato. Le uniche stringhe di dominio nel codice sono nomi di VOCABOLARI
  CHIUSI (tipi di stanza in PotionSystem/Forge, eventi in EventTracker,
  comportamenti-talento negli emettitori, stat, tag) che i sistemi
  indirizzano PER CHIAVE - come get_stat("forza"), non come un if su un
  contenuto. Il punto di controllo della fase 5 (aggiungere i 9 Pathway
  rimanenti = dati, non codice) non e' minacciato da nulla della fase 3.

- Come verificato: 498 test headless (496 + 2), validate_data.py exit 0.
  Nessuna regressione sui test di fase 2. NB trovato ed eviato: la slice
  creava un player node e non lo liberava nel 2o test -> test_vertical_slice
  e test_talent_system vedevano due nodi "player" e leggevano quello
  sbagliato (hp_max 100 invece di 120). Fix: prepara() libera ogni nodo
  "player" pregresso, ogni test chiama _fine().
- Prossima e ULTIMA storia della fase 3: US-336 (aggiornamento roadmap.md,
  design-master.md Appendice C, CLAUDE.md, README.md; il validator ha un
  check per ogni file di dati nuovo della fase).

[2026-09-06] US-336 — Aggiornamento roadmap e validator di chiusura. FASE 3 CHIUSA.

- Documenti:
  * 006_PRD/roadmap.md: sezione fase 3 -> "CHIUSA (35 story, 498 test)".
    US-301..336 in 8 blocchi, save da schema_version 12 a 19, verdetto US-335,
    lista completa dei vocabolari chiusi nuovi e dei formati nuovi.
  * 006_PRD/design-master.md Appendice C: 9 righe nuove nella tabella dei
    vocabolari (item_categories, equip_slots, room_types + bonus, tracked_
    talents, sigil_effect_types, sigillato_effect_types, experiment_outcomes),
    e tolto "(questa sessione)" da damage_tags/time ormai vecchi.
  * CLAUDE.md: fase 3 CHIUSA (conteggi + verdetto + vocabolari + SynergySources);
    "Fase corrente: 4 - Sinergie (da aprire)".
  * README.md: paragrafo "Fase 3 - Sistemi di supporto: chiusa" con il riassunto
    dei sistemi e il verdetto.
- validate_data.py: check di CHIUSURA - FASE_3_DATA elenca i 23 file di
  dati/schema introdotti nella fase; err se uno manca (una story futura non
  deve cancellarli per sbaglio). Ogni singolo file ha gia' il suo blocco di
  validazione dedicato, aggiunto story per story lungo la fase.
- Come verificato: validate_data.py exit 0 (9 warning: 5 somme acting del TG
  + 89 sequenze stub + 2 sinergie di gruppi differiti + 1 conteggio TODO i18n
  - tutti attesi e documentati). 498 test headless invariati, tutti verdi.

============================================================
FASE 3 — SISTEMI DI SUPPORTO: CHIUSA. 35/35 story (US-301..336).
============================================================
- Blocchi: A inventario/equip, B alchimia, C forgiatura/sigilli, D strutture,
  E pet, F base building, G talenti, H integrazione.
- Save: schema_version da 12 (fine fase 2) a 19. Ogni bump ha la sua
  _migra_N_a_N+1 e il suo test; la catena da v1 e' verde.
- 498 test headless (erano 399 a fine fase 2 sul ramo main verificato).
- Nessuna primitiva nuova (registro 28+3). I 12 tracked_events invariati.
  I comportamenti-talento vivono nel vocabolario separato tracked_talents.json.
- Verdetto architettura (US-335): i sistemi di supporto compongono un ciclo
  completo (coltivazione -> alchimia -> forgia -> sigilli -> pet -> talenti ->
  sinergie) con ZERO righe di codice dedicate a un contenuto specifico.
  Il grep di scripts/ per id di item/pet/sigillo/ricetta/blueprint: zero.
- Autoload nuovi: Inventory, Equipment, StructureRegistry, PetSystem,
  BaseSystem, TalentTracker, TalentSystem, SynergySources.
- Punto di controllo di fase 5 (aggiungere i 9 Pathway = dati, non codice):
  NON minacciato. I sistemi per Death/Paragon/Moon/Mother (evocazioni) e
  Hermit (oggetti con stored_ability_id) sono pronti.

PROSSIMO: aprire la fase 4 (Sinergie, ~16 story) con /prd. SynergySources.
tag_sinergia_globali() e' la materia prima; data/synergies/core.json ha 3
esempi (sinergia_crescita_pozione ora raggiungibile).

[2026-09-06] FASE 4 APERTA — Sinergie

- PRD generato: 006_PRD/prd-fase-4-sinergie.md (16 story US-401..416 in 3
  blocchi). Domande di design risposte dall'utente:
  * TUTTI E 6 i tipi di effetto, incluso modifica_primitiva (hook nel motore
    delle primitive: delta ai parametri prima di eseguire, su una copia).
  * Registro a 3 livelli (visibile / nascosta / lore) col fog of war,
    persistito nel save (solo le 'viste'; le attive si riderivano dai tag).
  * Rivalutazione su segnale delle fonti + poll di sicurezza ogni ~0.5 s.
  * Priorita'/conflitti: effetti cumulativi si sommano (priorita' fissa
    l'ordine), effetti esclusivi -> priorita' desc poi id asc. Anti-sinergie
    con campo 'neutralizza' esplicito nei dati.
  * Contenuto: US-412 scrive sinergia_colpo_del_caso e toglie lo stub;
    US-413 batch 1 (8 sinergie, una per meccanismo + il criterio di uscita
    pet+stanza+talento); US-414 batch 2 (~10 raggiungibili); US-415 batch 3
    (~15 di gruppi differiti, inattive, warning atteso raggruppato in una riga).
- SAVE: fase 4 bumpa schema_version 19 -> 20 UNA sola volta (campo
  sinergie.viste), _migra_19_a_20.
- prd.json sostituito (fase 3 -> fase 4). La fase 3 (35 story, tutte
  passes:true) e progress.txt archiviati in
  archive/2026-09-06-fase-3-sistemi-di-supporto/.
- Autoload nuovo previsto: SynergyEngine (scripts/synergy_engine.gd), DOPO
  tutte le fonti in project.godot (si connette ai loro segnali in _ready).
- Criterio di uscita (test_slice_fase_4.gd): una sinergia che richiede tag da
  pet + stanza + talento insieme si attiva solo con le tre fonti in gioco, il
  suo effetto si misura, zero righe di codice la nominano (grep, come
  US-219/US-335).
- Prossima storia: US-401 (schema esteso + SynergyEngine: risoluzione dei tag).

[2026-09-06] US-401 — Schema esteso e SynergyEngine. BLOCCO A.

- data/schema/synergy.schema.json (doc, non enforced): campo 'priorita'
  (int, default 0); 'effetto' e' un oneOf per i 6 tipo coi loro campi:
  * modifica_stat        { stat, valore, moltiplicativo }
  * modifica_follia      { delta_al_minuto }  (negativo = riduce nel tempo)
  * modifica_qualita_crafting { categoria: pozioni|forgia, delta: INT }
  * sblocca_ricetta      { recipe_id }
  * aggiungi_abilita     { ability_id }
  * modifica_primitiva   { primitiva, parametro, delta, moltiplicativo }
  Campo 'neutralizza': [id] per le anti-sinergie (US-407). Campo 'stub'.
  La regola di risoluzione conflitti e' scritta nel _comment: cumulativi
  (stat/follia/primitiva) si sommano (priorita' = solo ordine); esclusivi
  (qualita_crafting per categoria, sblocca_ricetta/aggiungi_abilita per id)
  -> priorita' desc, poi id asc. Convenzione: anti a priorita 100.
- data/synergies/core.json: sinergia_crescita_pozione delta 0.20 -> 1
  (passi interi di qualita', non piu' un moltiplicatore). Nuova
  sinergia_studio_sereno (esempio di riferimento per esclude_tag +
  modifica_stat): stanza_rituale + biblioteca, esclude corruzione ->
  spiritualita_max +10%.
- scripts/synergy_engine.gd (autoload SynergyEngine, DOPO SynergySources in
  project.godot - in _ready() US-402 si connettera' ai segnali delle fonti):
  * tag_globali() -> da SynergySources.tag_sinergia_globali(), o da
    _tag_override nei test.
  * attive() -> [id] soddisfatti (ogni richiede_tag >= soglia, nessun
    esclude_tag presente), ordine lessicografico.
  * e_attiva(id), tag_mancanti(id) -> { tag: quanti ne mancano }.
  * pulisci() azzera l'override; imposta_override_tag(d) e' SOLO per i test.
  US-401 fa solo la RISOLUZIONE: nessun effetto applicato (US-403+), nessun
  segnale (US-402), niente anti-sinergie (US-407).
- scripts/synergy_sources.gd: per_fonte() aggiunge la fonte "sequenza" = i
  tag del Pathway attivo (Progression.pathway() -> GameData.get_pathway()
  .tags). 'ingrediente' resta nell'enum dello schema senza una fonte in
  fase 4 (documentato).
- scripts/game_data.gd: synergy_ids() (get_synergy c'era gia').
- tools/validate_data.py: nel blocco sinergie - 'priorita' intero; 'effetto'
  ben formato per tipo (stat nota; categoria pozioni|forgia + delta int;
  recipe_id in recipes.json; ability_id in ability_ids o stub:true;
  primitiva attiva-non-differita + parametro nei suoi params; delta numerico);
  'neutralizza' solo su anti:true, e ogni id elencato risolve a una sinergia
  esistente.
- Come verificato: 504 test headless (498 + 6 in test_synergy_engine.gd:
  synergy_ids espone le sinergie; richiede_tag soddisfatti -> attiva;
  sotto soglia -> non attiva + tag_mancanti dice quanto manca; esclude_tag
  presente -> blocca; attive() ordinata; per_fonte include 'sequenza' coi tag
  del Pathway). validate_data.py exit 0 (9 warning: le 2 sinergie di gruppi
  differiti + 5 acting TG + 89 stub + 1 i18n). Nessuna verifica a schermo.
- Prossima storia: US-402 (rivalutazione reattiva: SynergyEngine si connette
  ai segnali delle fonti + poll ogni ~0.5s, emette sinergia_attivata/
  disattivata sui cambi reali).

[2026-09-06] US-402 — Rivalutazione reattiva delle sinergie. BLOCCO A.

- SynergyEngine._ready() si connette ai segnali di cambiamento delle fonti:
  * Equipment.equip_cambiato  (gia' emesso su equipaggia / rimuovi_slot /
    incastona / rimuovi_sigillo - 4 punti)
  * PetSystem.{pet_impostato, pet_liberato, pet_morto, bond_cambiato,
    pet_avanzato}
  * TalentSystem.talento_sbloccato
  * BaseSystem.{stanza_costruita, stanza_potenziata}
  * Progression.sequence_changed
  _collega(path, [segnali]) usa has_signal() + Callable.unbind(arita) cosi'
  un handler unico (_su_fonte_cambiata) accetta qualsiasi firma. Nessun
  segnale mancante fra le fonti di fase 3: se ne mancasse uno sarebbe un bug
  di quella fonte (micro-story), non un if in SynergyEngine.
- _process(delta): _accumulo += delta; ogni 0.5s (_POLL_S) rivaluta()
  comunque - rete di sicurezza se una fonte dimentica di segnalare.
- rivaluta(): ora = attive(); per ogni id in ora non in _attive_prec ->
  sinergia_attivata.emit(id); per ogni id in _attive_prec non in ora ->
  sinergia_disattivata.emit(id); _attive_prec = ora. Idempotente: due
  chiamate consecutive sullo stesso stato di tag non emettono niente.
- pulisci() azzera anche _attive_prec e _accumulo (isolamento dei test).
- Come verificato: 506 test headless (504 + 2 in test_synergy_engine.gd:
  rivaluta emette solo sui cambi reali (attivata la 1a volta, niente al 2o
  giro, disattivata togliendo un tag); un solo segnale di una fonte
  (costruisco giardino + laboratorio, imposta_pet capra_lunare) fa
  rivalutare da solo -> sinergia_crescita_pozione risulta attiva senza aver
  chiamato rivaluta() a mano, e sinergia_attivata e' stato emesso).
  validate_data.py exit 0 (9 warning invariati). Nessuna verifica a schermo.
- Prossima storia: US-403 (effetti modifica_stat e modifica_follia:
  su sinergia_attivata -> modificatore 'synergy:<id>' sullo StatsComponent /
  contributo in Madness; su sinergia_disattivata -> rimozione; riapplica()).

[2026-09-06] US-403 — Effetti modifica_stat e modifica_follia. BLOCCO A.

- SynergyEngine._ready() connette anche i PROPRI segnali sinergia_attivata/
  disattivata a _su_attivata/_su_disattivata: il DIFF (rivaluta) e
  l'APPLICAZIONE (effetti) restano separati.
- _applica_effetto(id) su match effetto.tipo:
  * modifica_stat -> _stats().apply_modifier("synergy:<id>", {stat: delta}),
    delta = valore, o valore * get_base(stat) se moltiplicativo. Due sinergie
    sulla stessa stat -> due modificatori distinti, lo StatsComponent li
    somma (i conflitti CUMULATIVI funzionano gia'; US-406 formalizza la
    regola e aggiunge gli esclusivi).
  * modifica_follia -> _follia_attive[id] = delta_al_minuto.
- _process: in piu' al poll da 0.5s, un accumulatore _acc_follia da 1s versa
  per ogni sinergia in _follia_attive delta_al_minuto/60 * elapsed in
  Madness.add(q, "synergy:<id>", tramite_ancore=false). Negativo -> una
  sinergia positiva RIDUCE la follia nel tempo; positivo -> una anti-sinergia
  l'accelera.
- _rimuovi_effetto(id): remove_modifier("synergy:<id>") + _follia_attive.erase.
- riapplica(): rimette i modificatori di stat di tutte le _attive_prec - da
  chiamare quando compare il player in scena o dopo un load. Il gancio in
  GameState.applica() e' US-408 (SAVE).
- pulisci(): toglie ogni modificatore "synergy:<id>" dal player e azzera
  _follia_attive / _acc_follia.
- Come verificato: 509 test headless (506 + 3 in test_synergy_engine.gd:
  modifica_stat +10% spiritualita_max si applica e si toglie alla
  disattivazione; riapplica() rimette i modificatori dopo un clear_modifiers
  (giocatore appena comparso); modifica_follia versa 0.5 in Madness in un
  minuto simulato (_process(60.0)) e si ferma alla disattivazione).
  validate_data.py exit 0 (9 warning invariati). Nessuna verifica a schermo.
- Prossima storia: US-404 (effetti modifica_qualita_crafting -> PotionSystem/
  Forge; sblocca_ricetta -> KnowledgeStore; aggiungi_abilita ->
  AbilityEngine.grant_permanente).
